# Frontend Capability Recomposition Matrix

Status: **Normative current-surface contract**

## 1. Purpose

The current frontend ships the Customer request/account portal, Agent Queue, a capability-gated write-capable Ticket Workspace, minimum staff login, Admin Operations routes, and common denied/not-found states. This matrix preserves the contracts needed to rebuild deferred screens without retaining their previous React pages, CSS, fixtures, or screenshots.

Backend implementation status is independent from UI delivery status. `DEFERRED_UI` below means “not routed or rendered in this frontend,” not “the server capability is absent.” Core and Platform OpenAPI remain unchanged by ADR 0039.

## 2. Current surface

| Surface | Operations | Actor/capability | Projection and audit | Current evidence |
|---|---|---|---|---|
| Minimum staff login | `getStaffCsrfToken`, `createStaffSession`, `deleteStaffSession`, `getCurrentStaff` | anonymous staff → authenticated AGENT/ADMIN | Session principal is authoritative; expected-actor guard may only confirm it | `StaffSessionContext.test.tsx`, `access-surface.spec.ts` |
| Customer request and account portal | `createCustomerRequest`, `getAnonymousRequest`, `addCustomerRequestComment`, `getCustomerAccessMode`, `requestCustomerMagicLink`, `consumeCustomerMagicLink`, `getCustomerCsrfToken`, `deleteCustomerSession`, `getCurrentCustomer`, `listCustomerRequests`, `getCustomerRequest`, `addAuthenticatedCustomerComment`, `createAuthenticatedCustomerAttachmentUpload`, `downloadAuthenticatedCustomerAttachment` | anonymous/customer; anonymous proof is ticket-scoped `X-Request-Access-Token`, authenticated access uses the server customer session and customer mutation CSRF | Browser removes `#token=` before any follow-up request and retains it only in ticket-scoped session storage; customer projections and DOM exclude INTERNAL, child, staff, and audit fields; comment `clientCommandId` and CLEAN attachment IDs are stable for ambiguous retry | `docs/evidence/p0/FE-P0-CUSTOMER.md`, `CustomerRequestPortalIntegrationTest`, customer unit/Storybook, `customer-portal.spec.ts` |
| Agent Queue and saved views | `listAgentViews`, saved-view create/update/delete/preview/reorder, `listTicketsInView`, `listTicketAssignmentOptions`, `executeAgentTicketBatch` | AGENT or ADMIN + `AGENT_WORKSPACE`; shared mutation remains server capability-gated | PERSONAL/SHARED/SYSTEM definitions and counts are server-held; queue uses opaque cursor history, server assignment options and SLA projection. Bulk is limited to 100 explicit rows, sends expected versions/stable command IDs, requires a transfer reason, and preserves item-level partial results. | `AgentViewsPage.test.tsx`, `ViewConfigurationDrawer.stories.tsx`, `BulkTicketActionPanel.stories.tsx`, Queue Storybook interactions, `agent-views-workspace.spec.ts` |
| Agent search | `searchAgentWorkspace`, `getAgentTicket` | AGENT/ADMIN staff projection | Raw query is POST-body/component-memory only. Exact count and opaque cursors come from the server; result open forwards `X-Origin-Search-Event-Id` into the deliberate ticket read. | `AgentSearchPage.test.tsx`, `AgentSearchPage.stories.tsx`, `api/client.test.ts` |
| Agent Ticket Workspace | `getAgentTicket`, `updateAgentTicket`, agent attachment upload/download, external-reference list/create/delete | AGENT or ADMIN + `AGENT_WORKSPACE`; detail `UPDATE` and external-reference `canManage` capabilities are authoritative and SECURITY_AUDITOR is denied | Server projection is the only field/comment source; PUBLIC and INTERNAL drafts and CLEAN attachment handles stay separate, customer projection never receives INTERNAL attachments, and 409 requires per-field recovery. External references lazy-load and only backend-returned `safeDeepLink` values can open with `noopener,noreferrer`. `NAVIGATION` records one semantic view and `BACKGROUND` records none. | `docs/evidence/p0/FE-P0-AGENT-WRITE.md`, `AgentTicketWorkspacePage.test.tsx`, `AgentTicketEditorWorkspace.test.tsx`, `AttachmentUploadField.test.tsx`, `TicketContextPanel.stories.tsx`, `agent-ticket-write.spec.ts` |
| Customer attachments | initial request multipart submission; request-token `createCustomerAttachmentUpload`/`downloadCustomerAttachment`/`addCustomerRequestComment`; session `createAuthenticatedCustomerAttachmentUpload`/`downloadAuthenticatedCustomerAttachment`/`addAuthenticatedCustomerComment` | anonymous/customer intake, ticket-scoped request-token PUBLIC follow-up, or authenticated requester-owned CUSTOMER_REQUEST + customer CSRF for upload | Pending/rejected/expired handles block submission; only own-ticket PUBLIC CLEAN IDs enter comments; upload navigation warns and other customer/ticket, INTERNAL, unlinked, quarantined, infected, failed, deleted or expired download fails closed. Authenticated download records required CUSTOMER_SESSION access audit before bytes. | `AttachmentPipelineIntegrationTest`, `CustomerRequestPortalIntegrationTest`, `CustomerRequestDetailPage.test.tsx`, `customerPortalClient.test.ts`, `AttachmentUploadField.test.tsx`, customer follow-up Storybook states, `customer-portal.spec.ts` |
| Admin Operations | `getOutboundMailSummary`, `listOutboundMailIntents`, `getOutboundMailIntent`, `retryOutboundMailIntent`, organization/access-mode/schedule/SLA policy operation groups | ADMIN + `ADMIN_MANAGE`; browser guard only avoids an unnecessary protected call and the server remains authoritative | Mail projection is fail-closed and masked-only; retry requires reason + CSRF; access mode and immutable schedule/SLA versions retain local input on 409/412 | `docs/evidence/p0/FE-P0-ADMIN-OPS.md`, `Admin*.stories.tsx`, `admin-operations.spec.ts`, `access-surface.spec.ts`; focused/full Storybook MCP interaction/a11y and representative previews: PASS |
| Create staff ticket (AGT-005) | `createAgentTicket`, `searchAgentCustomers`, `listTicketAssignmentOptions` | AGENT or ADMIN + `AGENT_WORKSPACE` | Ticket creation commits `TICKET_CREATED`+`COMMENT_CREATED` as one audit; requester search is a routine audited search (`CUSTOMER_SEARCH_EXECUTED`) with protected ciphertext query; `clientCommandId` rotates on definite validation/conflict/success and is reused on ambiguous 5xx/network failure; `customerId` reuses an existing customer instead of minting a duplicate unverified one | `features/ticket-create/CreateAgentTicketPage.test.tsx`, `RequesterSearchField.stories.tsx`, `api/client.test.ts`, `AgentTicketCommandIntegrationTest`, `AgentCustomerSearchIntegrationTest`, `AgentTicketReadIntegrationTest` (assignment-options) |
| Audit Explorer and export request/status (AUD-001/AUD-002) | `listAuditActivities`, `getAuditActivity`, `createAuditExport`, `getAuditExport`, `downloadAuditExport` | SECURITY_AUDITOR only (role never receives `AGENT_WORKSPACE`); `audit:activity:read` is role-granted, `audit:export` is a deny-by-default explicit grant checked reactively | Cursor is opaque/server-bound; list/detail never contain comment body or decrypted raw query; the worker moves owner-scoped export jobs through REQUESTED/RUNNING/READY/FAILED/EXPIRED. The UI polls until a terminal state or route unmount, downloads only READY artifacts, and offers regeneration after expiry; the server rechecks owner/current capability and sends `Cache-Control: no-store`. | `features/audit/AuditExplorerPage.test.tsx`, `features/audit/AuditExportStatusPage.test.tsx`, `useAuditExportStatus.test.ts`, `AuditExportStatus.stories.tsx`, `api/client.test.ts`, `AuditExplorerIntegrationTest` |

## 3. Deferred UI capabilities

| Capability / UI state | Endpoint operation IDs | Actor, scope, projection, audit | Idempotency, concurrency, failures | Preserved headless seam and tests | Delivery |
|---|---|---|---|---|---|
| Explicit anonymous-ticket claim | `claimCustomerRequest` | authenticated customer plus a valid independent proof; matching email alone is not proof of ownership | Customer request/account portal never infers or submits a claim; a future explicit proof UX must preserve 403/409/expiry recovery and audit semantics | `features/customer-portal/api/customerPortalClient.ts`, `CustomerRequestPortalIntegrationTest` | `DEFERRED_UI` |
| Customer access-mode and organization administration | `getCustomerAccessModeSetting`, `updateCustomerAccessModeSetting`, `listStaffAccounts`, `createStaffAccount`, `disableStaffAccount`, `listGroups`, `createGroup`, `updateGroup`, `disableGroup`, membership operations | ADMIN only; resource and active-assignment constraints; mutation and admin/security audit commit together | CSRF and expected version required where specified; 409 retains server truth and no partial mutation | `/admin/staff`, `/admin/groups`, `/admin/settings/customer-access-mode`; `AdminStaffPage`, `AdminGroupsPage`, `AdminCustomerAccessModePage`, focused unit/Storybook tests | `SHIPPED` |
| Outbound mail operations | `getOutboundMailSummary`, `listOutboundMailIntents`, `getOutboundMailIntent`, `retryOutboundMailIntent` | ADMIN only; server commits retry and admin/security audit together | Masked projection only; same terminal intent is requeued with CSRF and required reason; conflict preserves typed reason | `/admin/operations/mail`, `AdminMailPage`, `admin-operations.spec.ts`, `client.test.ts` | `SHIPPED` |
| Protected search-query reveal and projection rebuild | `revealAuditSearchQuery`, `rebuildAuditActivityProjection` | SECURITY_AUDITOR plus explicit `audit:search-query:reveal`/`audit:projection:rebuild` grant; reveal needs reason and self-audit; failed required audit persistence fails closed | Reveal additionally requires session `authenticatedAt` within a 15-minute policy window; no dedicated reauthentication endpoint exists yet (only full re-login rotates the session), so this UI stays deferred until a reauthentication vertical slice lands; 403/409/503 remain distinct | Core OpenAPI, `SecurityAuditorAuthorizationIntegrationTest`, `AuditExplorerIntegrationTest` | `DEFERRED_UI` |
| Integration clients and external systems | integration-client and external-system operation groups; Platform `platformCreateTicket`, `platformGetTicket`, `platformUpdateTicket`, `platformAddInternalComment` | ADMIN for configuration; `INTEGRATION_CLIENT` plus scope/resource constraints for Platform API; machine actor cannot impersonate staff | Platform writes require `Idempotency-Key`; updates require `If-Match`; secret is shown once; external URLs retain SSRF boundaries | Core/Platform OpenAPI, `api/types.ts`, `PlatformTicketIntegrationTest`, integration-client and external-reference tests | `DEFERRED_UI` |
| Business schedules and First Reply SLA | schedule and SLA policy operation groups, `getFirstReplySlaAnalytics` | ADMIN policy mutation; staff-visible computed projection; policy version and audit retained | Versioned policy activation and preview; schedule/time-zone validation; rebuild is idempotent | `/admin/business-rules/schedules`, `/admin/business-rules/sla`, `AdminBusinessSchedulesPage`, `AdminFirstReplySlaPage`, focused Storybook states | `SHIPPED` |
| Ticket transfer and child creation (`updateAgentTicket`, bulk transfer, and external-reference controls are shipped — see §2) | `transferAgentTicket`, `createChildTicket` | AGENT/ADMIN subject to group membership and resource authorization; one ticket command produces one ordered TicketAudit | Existing child details use the shipped INTERNAL-only composer. Single-ticket transfer and child creation controls remain hidden until their complete operation flows are recomposed. | `api/client*`, backend ticket command/transfer tests | `DEFERRED_UI` |

## 4. Recomposition gate

A deferred capability may return only in its own vertical slice. That slice must:

1. freeze or confirm the listed OpenAPI operation IDs without inventing endpoints;
2. cite its actor, capability/scope, projection, and audit events;
3. reuse or extend the headless seam before composing React UI;
4. implement loading, empty, error, denied, not-found, stale/conflict states as applicable;
5. add current-design Storybook, keyboard/axe, and browser evidence without restoring legacy tokens, shells, screenshots, or compatibility aliases;
6. cite at least one `REQ-*` and an applicable gate from `docs/21-minimum-verification-gates.md`.
