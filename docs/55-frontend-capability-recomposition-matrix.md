# Frontend Capability Recomposition Matrix

Status: **Normative current-surface contract**

## 1. Purpose

The current frontend ships the Customer request/account portal, Agent Queue, a capability-gated write-capable Ticket Workspace, minimum staff login, and common denied/not-found states. This matrix preserves the contracts needed to rebuild deferred screens without retaining their previous React pages, CSS, fixtures, or screenshots.

Backend implementation status is independent from UI delivery status. `DEFERRED_UI` below means “not routed or rendered in this frontend,” not “the server capability is absent.” Core and Platform OpenAPI remain unchanged by ADR 0039.

## 2. Current surface

| Surface | Operations | Actor/capability | Projection and audit | Current evidence |
|---|---|---|---|---|
| Minimum staff login | `getStaffCsrfToken`, `createStaffSession`, `deleteStaffSession`, `getCurrentStaff` | anonymous staff → authenticated AGENT/ADMIN | Session principal is authoritative; expected-actor guard may only confirm it | `StaffSessionContext.test.tsx`, `access-surface.spec.ts` |
| Customer request and account portal | `createCustomerRequest`, `getAnonymousRequest`, `addCustomerRequestComment`, `getCustomerAccessMode`, `requestCustomerMagicLink`, `consumeCustomerMagicLink`, `getCustomerCsrfToken`, `deleteCustomerSession`, `getCurrentCustomer`, `listCustomerRequests`, `getCustomerRequest`, `addAuthenticatedCustomerComment` | anonymous/customer; anonymous proof is ticket-scoped `X-Request-Access-Token`, authenticated access uses the server customer session | Browser removes `#token=` before any follow-up request and retains it only in ticket-scoped session storage; customer projections and DOM exclude INTERNAL, child, staff, and audit fields; comment `clientCommandId` is stable for ambiguous retry | `docs/evidence/p0/FE-P0-CUSTOMER.md`, customer unit/Storybook, `customer-portal.spec.ts` |
| Agent Queue | `listAgentViews`, `listTicketsInView` | AGENT or ADMIN + `AGENT_WORKSPACE` | Staff-visible queue projection; list/prefetch never emits semantic `TICKET_VIEWED` | `AgentViewsPage.test.tsx`, Queue Storybook interactions, `agent-views-workspace.spec.ts` |
| Agent Ticket Workspace | `getAgentTicket`, `updateAgentTicket` | AGENT or ADMIN + `AGENT_WORKSPACE`; detail `UPDATE` capability is authoritative and SECURITY_AUDITOR is denied | Server projection is the only field/comment source; PUBLIC and INTERNAL drafts stay separate, a child detail is INTERNAL-only, one atomic command carries `expectedVersion` and a stable `clientCommandId`, and 409 requires per-field recovery. `NAVIGATION` records one semantic view and `BACKGROUND` records none. | `docs/evidence/p0/FE-P0-AGENT-WRITE.md`, `AgentTicketWorkspacePage.test.tsx`, `AgentTicketEditorWorkspace.test.tsx`, editor Storybook states, `agent-ticket-write.spec.ts`, `agent-views-workspace.spec.ts` |
| Create staff ticket (AGT-005) | `createAgentTicket`, `searchAgentCustomers`, `listTicketAssignmentOptions` | AGENT or ADMIN + `AGENT_WORKSPACE` | Ticket creation commits `TICKET_CREATED`+`COMMENT_CREATED` as one audit; requester search is a routine audited search (`CUSTOMER_SEARCH_EXECUTED`) with protected ciphertext query; `clientCommandId` rotates on definite validation/conflict/success and is reused on ambiguous 5xx/network failure; `customerId` reuses an existing customer instead of minting a duplicate unverified one | `features/ticket-create/CreateAgentTicketPage.test.tsx`, `RequesterSearchField.stories.tsx`, `api/client.test.ts`, `AgentTicketCommandIntegrationTest`, `AgentCustomerSearchIntegrationTest`, `AgentTicketReadIntegrationTest` (assignment-options) |
| Audit Explorer and export request/status (AUD-001/AUD-002) | `listAuditActivities`, `getAuditActivity`, `createAuditExport`, `getAuditExport` | SECURITY_AUDITOR only (role never receives `AGENT_WORKSPACE`); `audit:activity:read` is role-granted, `audit:export` is a deny-by-default explicit grant checked reactively (session does not expose per-authority grants, so the UI does not pre-hide the export action) | Cursor is opaque/server-bound; list/detail never contain comment body or decrypted raw query; export job status is requester-scoped (404 for other owners); export status polling is bounded (5 attempts) with manual refresh after exhaustion since no generation worker exists yet | `features/audit/AuditExplorerPage.test.tsx`, `features/audit/AuditExportStatusPage.test.tsx`, `AuditExplorer.stories.tsx`, `AuditActivityDetailDrawer.stories.tsx`, `CreateAuditExportDrawer.stories.tsx`, `AuditExportStatus.stories.tsx`, `api/client.test.ts`, `AuditExplorerIntegrationTest`, `SecurityAuditorAuthorizationIntegrationTest` |

## 3. Deferred UI capabilities

| Capability / UI state | Endpoint operation IDs | Actor, scope, projection, audit | Idempotency, concurrency, failures | Preserved headless seam and tests | Delivery |
|---|---|---|---|---|---|
| Explicit anonymous-ticket claim | `claimCustomerRequest` | authenticated customer plus a valid independent proof; matching email alone is not proof of ownership | Customer request/account portal never infers or submits a claim; a future explicit proof UX must preserve 403/409/expiry recovery and audit semantics | `features/customer-portal/api/customerPortalClient.ts`, `CustomerRequestPortalIntegrationTest` | `DEFERRED_UI` |
| Customer access-mode and organization administration | `getCustomerAccessModeSetting`, `updateCustomerAccessModeSetting`, `listStaffAccounts`, `createStaffAccount`, `disableStaffAccount`, `listGroups`, `createGroup`, `updateGroup`, `disableGroup`, membership operations | ADMIN only; resource and active-assignment constraints; mutation and admin/security audit commit together | CSRF and expected version required where specified; 409 retains server truth and no partial mutation | `api/client.ts`, `api/types.ts`, Core OpenAPI, `AdminOrganizationIntegrationTest`, `CustomerAccessModeIntegrationTest` | `DEFERRED_UI` |
| Protected search-query reveal and projection rebuild | `revealAuditSearchQuery`, `rebuildAuditActivityProjection` | SECURITY_AUDITOR plus explicit `audit:search-query:reveal`/`audit:projection:rebuild` grant; reveal needs reason and self-audit; failed required audit persistence fails closed | Reveal additionally requires session `authenticatedAt` within a 15-minute policy window; no dedicated reauthentication endpoint exists yet (only full re-login rotates the session), so this UI stays deferred until a reauthentication vertical slice lands; 403/409/503 remain distinct | Core OpenAPI, `SecurityAuditorAuthorizationIntegrationTest`, `AuditExplorerIntegrationTest` | `DEFERRED_UI` |
| Agent search | `searchAgentWorkspace` | AGENT/ADMIN staff projection; routine audit is content-free; exact query only through protected ciphertext reveal | Stable search session and result-open linkage; background/detail intent must not invent semantic views | `api/uuid.ts` (`createOpaqueUuid`), `api/client.ts`, `AgentTicketSearchIntegrationTest`, `SearchQueryProtectionTest` | `DEFERRED_UI` |
| Integration clients and external systems | integration-client and external-system operation groups; Platform `platformCreateTicket`, `platformGetTicket`, `platformUpdateTicket`, `platformAddInternalComment` | ADMIN for configuration; `INTEGRATION_CLIENT` plus scope/resource constraints for Platform API; machine actor cannot impersonate staff | Platform writes require `Idempotency-Key`; updates require `If-Match`; secret is shown once; external URLs retain SSRF boundaries | Core/Platform OpenAPI, `api/types.ts`, `PlatformTicketIntegrationTest`, integration-client and external-reference tests | `DEFERRED_UI` |
| Business schedules and First Reply SLA | schedule and SLA policy operation groups, `getFirstReplySlaAnalytics` | ADMIN policy mutation; staff-visible computed projection; policy version and audit retained | Versioned policy activation and preview; schedule/time-zone validation; rebuild is idempotent | Core OpenAPI, `api/types.ts`, `BusinessScheduleAdminIntegrationTest`, `FirstReplySlaIntegrationTest` | `DEFERRED_UI` |
| Ticket transfer, child creation, and external-reference mutation UI (`createAgentTicket`/`searchAgentCustomers`/`listTicketAssignmentOptions` and `updateAgentTicket` are shipped — see §2) | `transferAgentTicket`, `createChildTicket`, external-reference create/delete | AGENT/ADMIN subject to group membership and resource authorization; one ticket command produces one ordered TicketAudit | Existing child details use the shipped INTERNAL-only `updateAgentTicket` composer. Transfer, child creation, and external-reference controls remain hidden until their complete operation flows are recomposed. | `api/client*`, backend ticket command/transfer tests | `DEFERRED_UI` |

## 4. Recomposition gate

A deferred capability may return only in its own vertical slice. That slice must:

1. freeze or confirm the listed OpenAPI operation IDs without inventing endpoints;
2. cite its actor, capability/scope, projection, and audit events;
3. reuse or extend the headless seam before composing React UI;
4. implement loading, empty, error, denied, not-found, stale/conflict states as applicable;
5. add current-design Storybook, keyboard/axe, and browser evidence without restoring legacy tokens, shells, screenshots, or compatibility aliases;
6. cite at least one `REQ-*` and an applicable gate from `docs/21-minimum-verification-gates.md`.
