openapi: 3.1.0
info:
  title: Deskseed Core API Outline
  version: 0.6.0-implementation-ready
  description: Deskseed의 커밋된 OpenAPI 3.1 계약입니다. API 목적과 데이터 경계는 한국어로 설명하며 예시는 모두 합성 데이터입니다. 실제 비밀번호, 세션, API key 또는 고객 정보를
    문서에 입력하거나 저장하지 마세요.
x-deskseed-staff-actor-consistency:
  appliesTo: every operation whose security includes staffSession
  implementedOperationBinding: Implemented operations carry the parameter and 400/409 responses directly so OpenAPI generators
    and contract checks can discover them without interpreting this extension.
  blueprintOnlyBinding: Blueprint-only staffSession operations inherit this policy extension and receive the standard operation-level
    bindings when their contracts are frozen for implementation.
  blueprintOnlyOperationIds:
  - listCustomerConsentPolicies
  - createCustomerConsentPolicy
  - getCustomerConsentPolicy
  - updateCustomerConsentPolicyDraft
  - publishCustomerConsentPolicy
  - archiveCustomerConsentPolicy
  - listTicketAudits
  - getAdminSetting
  - updateAdminSetting
  parameter:
    $ref: '#/components/parameters/ExpectedStaffActorHeader'
  invalidHeaderResponse:
    $ref: '#/components/responses/InvalidExpectedStaffActor'
  actorMismatchResponse:
    $ref: '#/components/responses/StaffSessionActorMismatch'
  establishmentExceptions:
  - getStaffCsrfToken before sign-in; authenticated token acquisition sends the guard
  - createStaffSession
  - getCurrentStaff when verifying a just-created session
  description: 브라우저는 새 세션을 만들고 확인하는 동안에만 이 헤더를 생략합니다. getStaffCsrfToken은 로그인 전에는 생략하고 인증 후 변경 요청에는 동일한 actor 값을 보내는 이중 용도
    API입니다. 비브라우저 클라이언트는 헤더를 생략할 수 있지만, 전달할 때는 하나의 정규 UUID여야 합니다. 불일치는 제공된 actor 선택, 세션 무효화, 활동 시각 갱신 또는 Controller 진입 없이
    실패로 처리됩니다.
servers:
- url: http://localhost:8080
  description: 로컬 개발 서버
tags:
- name: Customer Requests
  description: 로그인 없이 문의를 접수하고 접근 토큰으로 공개 대화만 확인하는 API입니다.
- name: Customer Portal
  description: 인증된 고객이 자신의 문의와 PUBLIC 댓글만 조회·작성하는 API입니다.
- name: Staff Session
  description: 직원 로그인 세션과 CSRF 토큰을 관리하는 API입니다.
- name: Agent Views
  description: 상담사가 접근 가능한 티켓 큐와 저장된 보기를 조회하는 API입니다.
- name: Agent Tickets
  description: 상담사용 티켓 조회·변경·이관·내부 협업 API입니다.
- name: Agent Search
  description: 권한 범위 안의 티켓을 검색하고 검색 감사를 남기는 API입니다.
- name: Agent Customers
  description: 상담사가 신규 티켓 생성 시 요청자로 선택할 기존 고객을 검색하고 검색 감사를 남기는 API입니다.
- name: Admin
  description: 직원, 그룹, 권한과 제품 설정을 관리하는 ADMIN 전용 API입니다.
- name: Integration Admin
  description: 연동 클라이언트·외부 시스템·외부 참조를 관리하는 API입니다.
- name: Audit
  description: 감사 활동 조회·보호 콘텐츠 공개·내보내기를 처리하며 조회 자체도 감사하는 API입니다.
- name: Customer Auth
  description: 고객 인증 상태와 세션을 처리하는 API입니다.
- name: Admin Schedule
  description: 버전이 고정된 영업 시간표를 생성·검증·활성화하는 API입니다.
- name: Admin SLA
  description: 최초 답변 SLA 정책의 불변 버전을 생성·검증·활성화하는 API입니다.
- name: Analytics
  description: 감사 가능한 불변 사실을 기준으로 SLA 성과를 집계하는 API입니다.
paths:
  /api/v1/requests:
    post:
      summary: 고객 문의 접수
      description: '고객 문의 접수 API입니다. JSON과 multipart의 request JSON part는 같은 CreateCustomerRequest command를 사용합니다.
        익명 요청은 requester가 필수이고 격리된 미검증 Customer를 만들며 같은 이메일만으로 기존 profile을 병합하거나 claim하지 않습니다.
        유효한 고객 세션 요청은 requester를 생략하고 서버 세션 identity를 사용하며 충돌하는 requester를 거부합니다. 최종 transaction에서
        current published form/version, customer visibility/editability, requiredness, typed option/value와 REQUEST_SUBMISSION 필수 policy version을
        다시 검증합니다. candidate projection은 authorization token이 아닙니다. hidden/readonly 값은 ADR 0041에 따라 버리고 선택한 form/version은
        값이 없어도 보존합니다. Ticket, 첫 PUBLIC comment, 정규화한 값, 동의 acceptance, CLEAN attachment link, 하나의 ordered TicketAudit과
        필요한 audit은 함께 commit/rollback하며 외부 network call은 transaction 밖에서 처리합니다. 응답은 customer projection만 반환하고
        INTERNAL comment, child relation, 직원 전용 field와 audit metadata를 노출하지 않습니다. 두 identity mode 모두 ticket 범위 조회 또는
        명시적 claim에만 쓰는 일회성 request access token을 생성 시 반환하고 ownership claim 때 revoke합니다. Required clientCommandId와
        canonical request/ordered attachment-manifest hash는 동일 payload 재시도를 같은 logical ticket으로 replay하고 fresh access grant만 발급합니다.
        같은 command ID의 다른 payload는 business mutation 없는 409이며 transaction lock으로 동시 finalization을 single-winner로 만듭니다.
        Receipt는 raw command ID/request/access token 없이 7일 보존합니다. 익명 multipart는 서버가 plannedCustomerId를 만들고 Customer row 없이
        파일 격리·검사를 끝낸 뒤 최종 transaction에서 같은 UUID의 Customer와 Ticket을 함께 생성합니다. 이메일·신뢰된 실제 client 주소·전체
        요청 수에는 PostgreSQL 고정 창 제한을 적용하고 제한 버킷에는 원문 이메일/IP를 저장하지 않습니다.'
      operationId: createCustomerRequest
      tags:
      - Customer Requests
      security:
      - {}
      - customerSession: []
      x-deskseed-phase: M1
      x-deskseed-requirements:
      - REQ-TKT-001
      - REQ-TKT-002
      - REQ-TKT-003
      - REQ-TKT-006
      - REQ-AUD-007
      - REQ-FILE-001
      - REQ-CONSENT-002
      - REQ-CFG-014
      x-deskseed-audit-failure: FAIL_CLOSED
      x-deskseed-ticket-audit: ONE_TICKET_AUDIT_WITH_ORDERED_EVENTS
      x-deskseed-ticket-audit-events: [TICKET_CREATED, COMMENT_CREATED, ATTACHMENT_ADDED]
      x-deskseed-security-audit-events: [CUSTOMER_CONSENT_ACCEPTED]
      x-deskseed-idempotency:
        identity: CLIENT_COMMAND_ID
        scope: CUSTOMER_OR_ANONYMOUS_DESTINATION
        payloadHash: CANONICAL_REQUEST_AND_ORDERED_ATTACHMENT_MANIFEST
        samePayload: SAME_TICKET_FRESH_ACCESS_GRANT
        differentPayload: NON_MUTATING_409
        concurrency: SINGLE_WINNER
        receiptRetention: P7D
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCustomerRequest'
            example:
              clientCommandId: 08a85f1c-9939-43f7-a90a-657ac5acb935
              requester:
                name: 김민아
                email: mina.kim@example.test
              subject: 주문 ORD-2026-1042 상태 확인 요청
              message: 합성 테스트 주문의 현재 상태를 확인해 주세요.
              formId: 00000000-0000-4000-8000-000000000001
              formVersion: 3
              fieldValues:
                request.type: {optionId: 00000000-0000-4000-8000-000000000101}
                order.reference: {shortTextValue: ORD-2026-1042}
              acceptedPolicies:
                - {policyKey: test-inquiry-data-processing, version: 2}
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CreateCustomerRequestMultipart'
            encoding:
              request:
                contentType: application/json
              attachments:
                style: form
                explode: true
      parameters:
      - name: X-CSRF-TOKEN
        in: header
        required: false
        description: customerSession으로 제출할 때 필수이며 현재 세션에 귀속됩니다. 익명 제출에는 사용하지 않습니다.
        schema:
          type: string
          minLength: 16
          maxLength: 256
      x-deskseed-generic-problem: &id006
        content: &id001
          application/problem+json:
            schema:
              $ref: '#/components/schemas/Problem'
      x-deskseed-no-store-problem: &id002
        content: *id001
        headers: &id003
          Cache-Control:
            description: 고객 데이터가 포함될 수 있는 capability 보호 응답이므로 항상 `no-store`를 사용합니다.
            schema:
              type: string
              enum:
              - no-store
      responses:
        '201':
          headers:
            Cache-Control:
              description: 응답에 한 번만 제공되는 접근 토큰이 포함되므로 항상 `no-store`를 사용합니다.
              schema:
                type: string
                enum:
                - no-store
            Location:
              description: 접근 토큰을 포함하지 않는 고객용 안전한 문의 URL입니다.
              schema:
                type: string
            X-Request-Id:
              schema:
                type: string
            X-Correlation-Id:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateCustomerRequestResult'
              example:
                ticketNumber: 1001
                status: NEW
                accessToken: YWJjZGVmZ2hpamtsbW5vcHFyc3R1dnd4eXowMTIzNDU
                createdAt: '2026-08-10T00:00:00Z'
                replayed: false
        '400': {$ref: '#/components/responses/CustomerRequestValidation'}
        '403': {$ref: '#/components/responses/CustomerRequestForbidden'}
        '409': {$ref: '#/components/responses/CustomerRequestConflict'}
        '429': {$ref: '#/components/responses/CustomerRequestRateLimited'}
        '503': {$ref: '#/components/responses/CustomerRequestConfigurationUnavailable'}
        '500': *id002
  /api/v1/requests/{ticketNumber}:
    get:
      summary: 접근 토큰으로 고객 문의 조회
      operationId: getAnonymousRequest
      x-deskseed-contract-status: FROZEN
      tags:
      - Customer Requests
      security: []
      x-deskseed-phase: M1
      x-deskseed-requirements:
      - REQ-TKT-003
      - REQ-TKT-008
      parameters:
      - &id004
        name: ticketNumber
        in: path
        required: true
        schema:
          type: integer
          format: int64
          minimum: 1
      - &id005
        name: X-Request-Access-Token
        in: header
        required: true
        schema:
          type: string
          minLength: 32
          maxLength: 256
      responses:
        '200':
          headers:
            Cache-Control:
              description: 응답에 고객 콘텐츠가 포함되므로 항상 `no-store`를 사용합니다.
              schema:
                type: string
            X-Request-Id:
              schema:
                type: string
            X-Correlation-Id:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerRequestDetail'
              example:
                ticketNumber: 1001
                subject: 결제가 되지 않아요
                status: NEW
                createdAt: '2026-08-10T00:00:00Z'
                updatedAt: '2026-08-10T00:00:00Z'
                comments:
                - id: 9cc38543-3937-4aa1-a34e-ef45f7b606f8
                  authorDisplayName: 고객
                  body: 결제 버튼을 누르면 오류가 발생합니다.
                  createdAt: '2026-08-10T00:00:00Z'
        '400': *id002
        '404':
          headers:
            Cache-Control:
              description: 고객 데이터가 포함될 수 있는 capability 보호 응답이므로 항상 `no-store`를 사용합니다.
              schema:
                type: string
                enum:
                - no-store
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
              example:
                type: /problems/request-not-found
                title: Request not found
                status: 404
                detail: The ticket number and access token do not identify a visible request.
                instance: /api/v1/requests/1001
                requestId: 9f3c4bbc-e78c-4a12-8673-7e39821e591f
        '429': *id002
        '503': *id002
      description: 접근 토큰으로 고객 문의 조회 API입니다. 고객용 projection만 반환하며 INTERNAL 댓글, 자식 관계, 직원 전용 필드와 감사 메타데이터를 노출하지 않습니다.
  /api/v1/requests/{ticketNumber}/comments:
    post:
      summary: 고객 문의에 공개 답변 추가
      description: '접근 토큰으로 식별한 고객 문의에 PUBLIC 댓글을 추가하는 API입니다. 토큰은 요청 헤더로만 전달하며 URL, 응답 본문, 감사 메타데이터에 다시 노출하지 않습니다. PENDING 문의는 OPEN으로 전환됩니다. SOLVED와 CLOSED는 자동 재개하지 않고 409를 반환합니다. 같은 ticketNumber·clientCommandId·본문의 재전송은 기존 댓글을 반환하며, 같은 식별자를 다른 티켓 또는 본문에 재사용하면 409를 반환합니다. 댓글, 상태 변경, TicketAudit, 새 접근 grant, request-received 메일 intent는 하나의 트랜잭션에서 함께 저장됩니다.'
      operationId: addCustomerRequestComment
      x-deskseed-contract-status: FROZEN
      tags:
      - Customer Requests
      security: []
      x-deskseed-phase: M1
      x-deskseed-requirements:
      - REQ-TKT-003
      - REQ-TKT-005
      - REQ-TKT-008
      - REQ-NOTIF-001
      - REQ-CHAN-003
      - REQ-FILE-001
      parameters:
      - *id004
      - *id005
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddCustomerComment'
            example:
              body: 결제 카드 승인 내역을 추가로 확인해 주세요.
              clientCommandId: 77777777-7777-4777-8777-777777777777
      responses:
        '201':
          headers:
            Cache-Control:
              description: capability 기반 고객 응답이므로 항상 `no-store`를 사용합니다.
              schema:
                type: string
                enum:
                - no-store
            Location:
              description: 접근 토큰을 포함하지 않는 새 PUBLIC 댓글 리소스 위치입니다.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PublicComment'
        '400': *id002
        '404': *id002
        '409': *id002
        '429': *id002
        '503': *id002
  /api/v1/requests/{ticketNumber}/attachments/uploads:
    post:
      summary: 고객 문의 PUBLIC 첨부파일 quarantine upload
      operationId: createCustomerAttachmentUpload
      x-deskseed-contract-status: FROZEN
      tags:
      - Customer Requests
      x-deskseed-phase: P1
      x-deskseed-requirements:
      - REQ-FILE-001
      - REQ-TKT-003
      - REQ-TKT-008
      security: []
      parameters:
      - *id004
      - *id005
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AttachmentUploadForm'
      responses:
        '201':
          headers:
            Cache-Control:
              schema:
                type: string
                const: no-store
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttachmentUpload'
        '400': *id002
        '404': *id002
        '413': *id002
        '415': *id002
        '422': *id002
        '429': *id002
        '503': *id002
      description: ticket-scoped request access token을 확인한 뒤 bounded stream을 private quarantine에 기록하고 SHA-256, MIME, malware scan을 끝낸 CLEAN object만 반환합니다. 고객 upload는 PUBLIC link에만 쓸 수 있으며 object key·public URL·원본 bytes는 응답에 포함하지 않습니다.
  /api/v1/requests/{ticketNumber}/attachments/{attachmentId}/download:
    get:
      summary: 고객 PUBLIC 첨부파일 다운로드
      operationId: downloadCustomerAttachment
      x-deskseed-contract-status: FROZEN
      tags:
      - Customer Requests
      x-deskseed-phase: P1
      x-deskseed-requirements:
      - REQ-FILE-001
      - REQ-TKT-008
      security: []
      parameters:
      - *id004
      - *id005
      - $ref: '#/components/parameters/AttachmentId'
      responses:
        '200':
          headers:
            Cache-Control:
              schema:
                type: string
                const: no-store
            Content-Disposition:
              schema:
                type: string
            Content-Type:
              schema:
                type: string
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400': *id002
        '404': *id002
        '409': *id002
        '503': *id002
      description: 현재 request token이 소유한 ticket의 PUBLIC comment에 link된 CLEAN attachment만 다운로드합니다. INTERNAL, quarantine, infected, failed, deleted 또는 expired object는 같은 not-found-safe 응답으로 차단하고 성공 download는 audit됩니다.
  /api/v1/customer/requests:
    get:
      summary: 인증 고객의 문의 목록 조회
      operationId: listCustomerRequests
      x-deskseed-contract-status: FROZEN
      tags:
      - Customer Portal
      x-deskseed-phase: P1
      x-deskseed-requirements:
      - REQ-TKT-004
      - REQ-TKT-005
      security:
      - customerSession: []
      parameters:
      - name: status
        in: query
        schema:
          type: string
      - name: cursor
        in: query
        schema:
          type: string
      - name: limit
        in: query
        schema:
          type: integer
          minimum: 1
          maximum: 100
          default: 25
      responses:
        '200':
          headers:
            Cache-Control:
              schema:
                type: string
                enum:
                - no-store
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerRequestPage'
        '401': *id006
      description: 인증 고객의 문의 목록 조회 API입니다. 고객용 projection만 반환하며 INTERNAL 댓글, 자식 관계, 직원 전용 필드와 감사 메타데이터를 노출하지 않습니다.
  /api/v1/customer/access-mode:
    get:
      summary: 고객 문의 접근 정책 조회
      operationId: getCustomerAccessMode
      x-deskseed-contract-status: FROZEN
      tags:
      - Customer Portal
      security: []
      x-deskseed-phase: P1
      x-deskseed-requirements:
      - REQ-TKT-004
      responses:
        '200':
          headers:
            Cache-Control:
              schema:
                type: string
                enum:
                - no-store
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerAccessModeResponse'
      description: 고객 문의 접근 정책 조회 API입니다. 고객용 projection만 반환하며 INTERNAL 댓글, 자식 관계, 직원 전용 필드와 감사 메타데이터를 노출하지 않습니다.
  /api/v1/agent/csrf:
    get:
      summary: 직원 세션용 CSRF 토큰 발급
      description: '직원 세션용 CSRF 토큰 발급 API입니다. 인증된 직원의 역할과 티켓 접근·변경 권한을 서버에서 확인한 뒤 처리합니다. 기존 계약의 세부 조건: Dual-use endpoint.
        A pre-sign-in caller omits the expected-actor header. An authenticated unsafe workflow supplies its confirmed actor
        snapshot so a cross-tab session change is rejected before the subsequent mutation.'
      operationId: getStaffCsrfToken
      x-deskseed-contract-status: FROZEN
      tags:
      - Staff Session
      security: []
      x-deskseed-phase: M2
      x-deskseed-requirements:
      - REQ-AUTH-005
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      responses:
        '200':
          headers:
            Cache-Control:
              schema:
                type: string
                enum:
                - no-store
            Set-Cookie:
              description: HttpOnly 직원 세션 쿠키이며 운영 환경에서는 Secure, SameSite=Lax를 사용합니다.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CsrfToken'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActor'
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
  /api/v1/agent/session:
    post:
      summary: 직원 로그인 세션 생성
      operationId: createStaffSession
      x-deskseed-contract-status: FROZEN
      tags:
      - Staff Session
      x-deskseed-phase: M2
      x-deskseed-requirements:
      - REQ-AUTH-005
      - REQ-AUTH-006
      parameters:
      - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StaffLogin'
      responses:
        '204':
          headers:
            Set-Cookie:
              description: HttpOnly, SameSite=Lax 쿠키이며 운영 환경에서는 Secure를 사용합니다. 유휴 만료는 60분, 절대 만료는 12시간입니다.
              schema:
                type: string
        '400': *id006
        '401': *id006
        '429': *id006
      description: 직원 로그인 세션 생성 API입니다. 인증된 직원의 역할과 티켓 접근·변경 권한을 서버에서 확인한 뒤 처리합니다.
    delete:
      summary: 현재 직원 세션 종료
      operationId: deleteStaffSession
      x-deskseed-contract-status: FROZEN
      tags:
      - Staff Session
      x-deskseed-phase: M2
      x-deskseed-requirements:
      - REQ-AUTH-005
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/CsrfHeader'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      responses:
        '204': {}
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActor'
        '401': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
      description: 현재 직원 세션 종료 API입니다. 인증된 직원의 역할과 티켓 접근·변경 권한을 서버에서 확인한 뒤 처리합니다.
  /api/v1/agent/me:
    get:
      summary: 현재 직원과 권한 조회
      operationId: getCurrentStaff
      x-deskseed-contract-status: FROZEN
      tags:
      - Staff Session
      x-deskseed-phase: M2
      x-deskseed-requirements:
      - REQ-AUTH-005
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CurrentStaff'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActor'
        '401': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
      description: 현재 직원과 권한 조회 API입니다. 인증된 직원의 역할과 티켓 접근·변경 권한을 서버에서 확인한 뒤 처리합니다.
  /api/v1/agent/views:
    get:
      summary: 상담사가 사용할 수 있는 티켓 보기 조회
      operationId: listAgentViews
      x-deskseed-contract-status: FROZEN
      tags:
      - Agent Views
      x-deskseed-phase: M2
      x-deskseed-requirements:
      - REQ-UI-002
      - REQ-PERM-001
      - REQ-VIEW-001
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/SavedView'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActor'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
      description: 상담사가 실행할 수 있는 SYSTEM, PERSONAL, SHARED saved view 정의를 조회합니다. 반환 순서의 처음 20개 visible view만 같은 권한 predicate와 condition compiler로 계산한 exact ticketCount를 포함합니다. 목록·count는 서버 ticket-read authorization을 적용하며, 태그나 script 조건은 이 계약에 존재하지 않습니다.
    post:
      summary: PERSONAL 또는 권한 있는 SHARED saved view 생성
      operationId: createAgentSavedView
      x-deskseed-contract-status: FROZEN
      tags:
      - Agent Views
      x-deskseed-phase: P1
      x-deskseed-requirements:
      - REQ-VIEW-001
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateSavedView'
      responses:
        '201':
          headers:
            ETag:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SavedView'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
        '503': *id006
      description: PERSONAL은 현재 owner만 생성할 수 있습니다. SHARED는 `saved-view:shared:manage` capability를 가진 ADMIN만 생성할 수 있으며, 설정 변경과 보안 감사가 함께 commit되지 않으면 생성되지 않습니다.
  /api/v1/agent/views/preview:
    post:
      summary: 저장 전 또는 변경 전 saved view 조건 preview
      operationId: previewAgentSavedView
      x-deskseed-contract-status: FROZEN
      tags:
      - Agent Views
      x-deskseed-phase: P1
      x-deskseed-requirements:
      - REQ-VIEW-001
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - $ref: '#/components/parameters/CsrfHeader'
      - $ref: '#/components/parameters/AgentInteractionHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SavedViewDefinition'
      responses:
        '200':
          headers:
            Cache-Control:
              schema:
                type: string
                const: no-store
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SavedViewPreview'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
        '503': *id006
      description: preview는 저장하지 않지만 조회 자체와 조건 fingerprint를 `VIEW_EXECUTED` access audit으로 기록합니다. sample rows와 exact count는 실행 endpoint와 같은 parameterized compiler 및 authorization predicate를 사용합니다.
  /api/v1/agent/views/reorder:
    post:
      summary: PERSONAL 또는 SHARED saved view 표시 순서 변경
      operationId: reorderAgentSavedViews
      x-deskseed-contract-status: FROZEN
      tags:
      - Agent Views
      x-deskseed-phase: P1
      x-deskseed-requirements:
      - REQ-VIEW-001
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReorderSavedViews'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SavedViewOrder'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
        '503': *id006
      description: reorder 요청은 해당 owner/scope의 현재 orderVersion을 요구합니다. 대상 목록은 모든 mutable visible view를 정확히 한 번 포함해야 하며 SYSTEM view의 순서는 바꾸지 않습니다.
  /api/v1/agent/views/{viewKey}:
    patch:
      summary: PERSONAL 또는 SHARED saved view 정의 갱신
      operationId: updateAgentSavedView
      x-deskseed-contract-status: FROZEN
      tags:
      - Agent Views
      x-deskseed-phase: P1
      x-deskseed-requirements:
      - REQ-VIEW-001
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - $ref: '#/components/parameters/CsrfHeader'
      - $ref: '#/components/parameters/SavedViewKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateSavedView'
      responses:
        '200':
          headers:
            ETag:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SavedView'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
        '503': *id006
      description: PERSONAL은 owner만, SHARED는 현재 capability를 가진 ADMIN만 갱신할 수 있습니다. SYSTEM definition은 immutable이며 version mismatch는 current definition을 반사하지 않는 conflict로 끝납니다.
    delete:
      summary: PERSONAL 또는 SHARED saved view 삭제
      operationId: deleteAgentSavedView
      x-deskseed-contract-status: FROZEN
      tags:
      - Agent Views
      x-deskseed-phase: P1
      x-deskseed-requirements:
      - REQ-VIEW-001
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - $ref: '#/components/parameters/CsrfHeader'
      - $ref: '#/components/parameters/SavedViewKey'
      - $ref: '#/components/parameters/SavedViewIfMatch'
      responses:
        '204':
          description: definition과 대응 설정 감사가 함께 삭제/기록되었습니다.
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
        '412':
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/TicketVersionPreconditionProblem'
        '503': *id006
      description: SYSTEM definition은 삭제할 수 없습니다. If-Match는 definition version을 나타내며, owner/capability/audit 검증 실패 시 definition을 삭제하지 않습니다.
  /api/v1/agent/views/{viewKey}/tickets:
    get:
      summary: 선택한 보기의 티켓 목록 조회
      operationId: listTicketsInView
      x-deskseed-contract-status: FROZEN
      tags:
      - Agent Views
      x-deskseed-phase: M2
      x-deskseed-requirements:
      - REQ-UI-002
      - REQ-TKT-010
      - REQ-PERM-001
      - REQ-VIEW-001
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - $ref: '#/components/parameters/SavedViewKey'
      - name: cursor
        in: query
        schema:
          type: string
      - name: limit
        in: query
        schema:
          type: integer
          minimum: 1
          maximum: 100
          default: 50
      - name: sort
        in: query
        schema:
          type: string
          enum:
          - updatedAt:desc,ticketNumber:desc
          default: updatedAt:desc,ticketNumber:desc
      - name: status
        in: query
        schema:
          $ref: '#/components/schemas/AgentTicketStatus'
      - name: priority
        in: query
        schema:
          $ref: '#/components/schemas/TicketPriority'
      - name: groupId
        in: query
        schema:
          type: string
          format: uuid
      - name: assigneeId
        in: query
        schema:
          type: string
          pattern: ^(me|unassigned|[0-9a-fA-F-]{36})$
      - name: slaState
        in: query
        schema:
          $ref: '#/components/schemas/FirstReplySlaDisplayState'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TicketSummaryPage'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
      description: 선택한 SYSTEM/PERSONAL/SHARED definition을 실행합니다. 조건 AST와 요청 filter는 parameterized SQL로 컴파일되고 현재 actor의 ticket-read predicate를 SQL 단계에서 함께 적용합니다. 실행은 `VIEW_EXECUTED` access audit을 먼저 기록하며 실패하면 보호된 queue를 반환하지 않습니다.
  /api/v1/agent/tickets:
    post:
      summary: 상담사 발신 티켓 생성
      operationId: createAgentTicket
      x-deskseed-contract-status: FROZEN
      tags:
      - Agent Tickets
      x-deskseed-phase: M3
      x-deskseed-requirements:
      - REQ-TKT-009
      - REQ-TKT-006
      - REQ-FILE-001
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAgentTicket'
      responses:
        '201':
          headers:
            Location:
              schema:
                type: string
            ETag:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TicketCommandResult'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
        '422': *id006
        '503': *id006
      description: 상담사 발신 티켓 생성 API입니다. 인증된 직원의 역할과 티켓 접근·변경 권한을 서버에서 확인한 뒤 처리합니다. requester.customerId가 기존
        고객을 가리키지 않으면 404 /problems/agent-ticket-requester-not-found를 반환합니다.
  /api/v1/agent/tickets/batch-commands:
    post:
      summary: 명시 선택 티켓의 bounded batch command 실행
      operationId: executeAgentTicketBatch
      x-deskseed-contract-status: FROZEN
      tags:
      - Agent Tickets
      x-deskseed-phase: P1
      x-deskseed-requirements:
      - REQ-BULK-001
      - REQ-TKT-010
      - REQ-TKT-011
      - REQ-TKT-012
      - REQ-AUD-001
      - REQ-AUD-007
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentTicketBatchCommand'
      responses:
        '200':
          headers:
            Cache-Control:
              schema:
                type: string
                const: no-store
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentTicketBatchResult'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
        '503': *id006
      description: 최대 100개 서로 다른 ticketNumber를 명시적으로 선택한 요청만 허용합니다. 각 item은 expectedVersion과 재전송에 유지되는 clientCommandId를 가지며 독립 transaction으로 기존 단일 ticket command/one TicketAudit를 재사용합니다. item 결과는 부분 성공을 표현하고, PUBLIC/INTERNAL comment는 이 schema에서 표현할 수 없습니다. group 변경은 TRANSFER와 reason을 사용해야 합니다.
  /api/v1/agent/attachments/uploads:
    post:
      summary: 상담사용 private attachment quarantine upload
      operationId: createAgentAttachmentUpload
      x-deskseed-contract-status: FROZEN
      tags:
      - Agent Tickets
      x-deskseed-phase: P1
      x-deskseed-requirements:
      - REQ-FILE-001
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AttachmentUploadForm'
      responses:
        '201':
          headers:
            Cache-Control:
              schema:
                type: string
                const: no-store
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttachmentUpload'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
        '413': *id006
        '415': *id006
        '422': *id006
        '503': *id006
      description: active staff만 upload할 수 있습니다. 서버는 bounded stream을 private quarantine에 기록한 뒤 SHA-256, claimed/detected MIME, malware scan을 검증합니다. CLEAN 결과만 future ticket create/comment command의 attachmentIds에 넣을 수 있고, object key나 signed/public URL은 반환하지 않습니다.
  /api/v1/agent/attachments/{attachmentId}/download:
    get:
      summary: 상담사용 ticket-linked attachment 다운로드
      operationId: downloadAgentAttachment
      x-deskseed-contract-status: FROZEN
      tags:
      - Agent Tickets
      x-deskseed-phase: P1
      x-deskseed-requirements:
      - REQ-FILE-001
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - $ref: '#/components/parameters/AttachmentId'
      - $ref: '#/components/parameters/AgentInteractionHeader'
      responses:
        '200':
          headers:
            Cache-Control:
              schema:
                type: string
                const: no-store
            Content-Disposition:
              schema:
                type: string
            Content-Type:
              schema:
                type: string
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
        '503': *id006
      description: 서버가 current staff ticket-read policy와 linked comment visibility를 검사한 뒤 CLEAN attachment만 stream합니다. quarantine, infected, failed, unlinked, deleted, expired attachment은 bytes를 반환하지 않으며 성공 view/download는 접근 감사 실패 시에도 반환하지 않습니다.
  /api/v1/agent/tickets/{ticketNumber}:
    get:
      summary: 상담사용 티켓 상세 조회
      operationId: getAgentTicket
      x-deskseed-contract-status: FROZEN
      tags:
      - Agent Tickets
      x-deskseed-phase: M2
      x-deskseed-requirements:
      - REQ-TKT-007
      - REQ-AUD-003
      - REQ-UI-003
      - REQ-SLA-001
      - REQ-INT-005
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - *id004
      - &id008
        name: X-Interaction-Id
        in: header
        required: true
        schema:
          type: string
          format: uuid
      - name: X-Deskseed-Read-Intent
        in: header
        required: true
        schema:
          type: string
          enum:
          - NAVIGATION
          - BACKGROUND
      - name: X-Origin-Search-Event-Id
        in: header
        required: false
        schema:
          type: string
          format: uuid
      responses:
        '200':
          headers:
            ETag:
              schema:
                type: string
            Cache-Control:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentTicketDetail'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActor'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
        '503': *id006
      description: 상담사용 티켓 상세 조회 API입니다. 인증된 직원의 역할과 티켓 접근·변경 권한을 서버에서 확인한 뒤 처리합니다.
  /api/v1/agent/tickets/{ticketNumber}/commands:
    post:
      summary: 티켓 변경 사항을 하나의 명령으로 저장
      description: '티켓 변경 사항을 하나의 명령으로 저장 API입니다. 인증된 직원의 역할과 티켓 접근·변경 권한을 서버에서 확인한 뒤 처리합니다. 기존 계약의 세부 조건: INTERNAL_CHILD
        tickets accept INTERNAL comments only; attempts to add a PUBLIC comment are rejected without mutation or audit. When
        clientCommandId is supplied, an exact retry by the same authenticated staff actor returns the original TicketCommandResult
        without another comment, audit, or version mutation. Reusing that ID for another canonical payload, ticket, or staff
        ticket operation returns /problems/client-command-id-reused.'
      operationId: updateAgentTicket
      x-deskseed-contract-status: FROZEN
      tags:
      - Agent Tickets
      x-deskseed-phase: M3
      x-deskseed-requirements:
      - REQ-TKT-010
      - REQ-TKT-013
      - REQ-TKT-014
      - REQ-CHILD-002
      - REQ-AUD-001
      - REQ-FILE-001
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - $ref: '#/components/parameters/CsrfHeader'
      - *id004
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTicketCommand'
      responses:
        '200':
          headers:
            ETag:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TicketCommandResult'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '409':
          headers:
            Cache-Control:
              schema:
                type: string
                const: no-store
          content:
            application/problem+json:
              schema:
                oneOf:
                - $ref: '#/components/schemas/TicketFieldConflictProblem'
                - $ref: '#/components/schemas/ClientCommandIdReusedProblem'
                - $ref: '#/components/schemas/StaffSessionActorMismatchProblem'
              examples:
                fieldConflict:
                  value:
                    type: /problems/ticket-field-conflict
                    title: Ticket fields changed concurrently
                    status: 409
                    detail: Some fields were changed by another actor.
                    requestId: req-123
                    currentVersion: 14
                    conflictingFields:
                    - assigneeId
                clientCommandIdReused:
                  value:
                    type: /problems/client-command-id-reused
                    title: Client command ID was already used
                    status: 409
                    detail: The client command ID identifies a different ticket command.
                    requestId: req-456
                staffSessionActorMismatch:
                  value:
                    type: /problems/staff-session-actor-mismatch
                    title: Staff session actor changed
                    status: 409
                    detail: Refresh the staff session before continuing.
                    requestId: req-actor-409
        '422': *id006
        '503': *id006
  /api/v1/agent/tickets/{ticketNumber}/transfer:
    post:
      summary: 기존 티켓의 담당 그룹 또는 상담사 이관
      operationId: transferAgentTicket
      x-deskseed-contract-status: FROZEN
      tags:
      - Agent Tickets
      x-deskseed-phase: M4
      x-deskseed-requirements:
      - REQ-TKT-011
      - REQ-TKT-012
      - REQ-AUD-001
      - REQ-AUD-007
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - $ref: '#/components/parameters/CsrfHeader'
      - *id004
      - &id007
        name: If-Match
        in: header
        required: true
        schema:
          type: string
          pattern: ^"?[0-9]+"?$
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TransferTicketCommand'
      responses:
        '200':
          headers:
            ETag:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TicketCommandResult'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
        '412':
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/TicketVersionPreconditionProblem'
        '422': *id006
      description: 기존 티켓의 담당 그룹 또는 상담사 이관 API입니다. 인증된 직원의 역할과 티켓 접근·변경 권한을 서버에서 확인한 뒤 처리합니다.
  /api/v1/agent/tickets/{ticketNumber}/children:
    post:
      summary: 내부 협업용 자식 티켓 생성
      operationId: createChildTicket
      x-deskseed-contract-status: FROZEN
      tags:
      - Agent Tickets
      x-deskseed-phase: M5
      x-deskseed-requirements:
      - REQ-CHILD-001
      - REQ-CHILD-002
      - REQ-CHILD-003
      - REQ-CHILD-004
      - REQ-CHILD-006
      - REQ-CHILD-007
      - REQ-AUD-001
      - REQ-AUD-007
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - $ref: '#/components/parameters/CsrfHeader'
      - *id004
      - *id007
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateChildTicketCommand'
      responses:
        '201':
          headers:
            ETag:
              description: 관계 감사가 커밋된 뒤의 부모 티켓 ETag입니다.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateChildTicketResult'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
        '412':
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/TicketVersionPreconditionProblem'
        '422': *id006
      description: 내부 협업용 자식 티켓 생성 API입니다. 인증된 직원의 역할과 티켓 접근·변경 권한을 서버에서 확인한 뒤 처리합니다.
  /api/v1/agent/tickets/{ticketNumber}/external-references:
    get:
      summary: 티켓에 연결된 외부 참조 조회
      description: '티켓에 연결된 외부 참조 조회 API입니다. 인증된 직원의 역할과 티켓 접근·변경 권한을 서버에서 확인한 뒤 처리합니다. 기존 계약의 세부 조건: Returns stored-only
        reference context. This operation never fetches, resolves, or redirects through an external URL. It records a required
        staff resource-read audit and never emits semantic TICKET_VIEWED for background tab loading.'
      operationId: listAgentTicketExternalReferences
      x-deskseed-contract-status: FROZEN
      tags:
      - Agent Tickets
      x-deskseed-phase: I2
      x-deskseed-requirements:
      - REQ-INT-005
      - REQ-UI-004
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - *id004
      - *id008
      responses:
        '200':
          headers:
            Cache-Control:
              schema:
                type: string
                enum:
                - no-store
            ETag:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExternalReferenceContext'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
        '503': *id006
    post:
      summary: 티켓에 외부 시스템 객체 연결
      description: '티켓에 외부 시스템 객체 연결 API입니다. 인증된 직원의 역할과 티켓 접근·변경 권한을 서버에서 확인한 뒤 처리합니다. 기존 계약의 세부 조건: Validates an active
        registry system, exact allowed HTTPS hostname, bounded metadata, ticket write scope, and ticket version before atomically
        storing one reference and one TicketAudit. At most 100 references may be linked to one ticket; the next create returns
        EXTERNAL_REFERENCE_LIMIT_REACHED without truncating the list. No external request is made.'
      operationId: createAgentTicketExternalReference
      x-deskseed-contract-status: FROZEN
      tags:
      - Agent Tickets
      x-deskseed-phase: I2
      x-deskseed-requirements:
      - REQ-INT-005
      - REQ-UI-004
      - REQ-AUD-001
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - $ref: '#/components/parameters/CsrfHeader'
      - *id004
      - *id007
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateExternalReferenceCommand'
      responses:
        '201':
          headers:
            Cache-Control:
              schema:
                type: string
                enum:
                - no-store
            ETag:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExternalReferenceCommandResult'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
        '412':
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/TicketVersionPreconditionProblem'
        '422': *id006
        '503': *id006
  /api/v1/agent/tickets/{ticketNumber}/external-references/{referenceId}:
    delete:
      summary: 티켓의 외부 참조 연결 해제
      description: '티켓의 외부 참조 연결 해제 API입니다. 인증된 직원의 역할과 티켓 접근·변경 권한을 서버에서 확인한 뒤 처리합니다. 기존 계약의 세부 조건: Removes only the Deskseed
        reference and atomically records one TicketAudit. No external delete, fetch, or callback is performed.'
      operationId: deleteAgentTicketExternalReference
      x-deskseed-contract-status: FROZEN
      tags:
      - Agent Tickets
      x-deskseed-phase: I2
      x-deskseed-requirements:
      - REQ-INT-005
      - REQ-UI-004
      - REQ-AUD-001
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - $ref: '#/components/parameters/CsrfHeader'
      - *id004
      - *id007
      - $ref: '#/components/parameters/ExternalReferenceId'
      responses:
        '200':
          headers:
            Cache-Control:
              schema:
                type: string
                enum:
                - no-store
            ETag:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeleteExternalReferenceResult'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
        '412':
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/TicketVersionPreconditionProblem'
        '503': *id006
  /api/v1/agent/tickets/{ticketNumber}/audits:
    get:
      summary: 티켓 변경 이력 조회
      operationId: listTicketAudits
      tags:
      - Agent Tickets
      x-deskseed-phase: M3
      x-deskseed-requirements:
      - REQ-AUD-001
      security:
      - staffSession: []
      parameters:
      - *id004
      - name: cursor
        in: query
        schema:
          type: string
      - name: limit
        in: query
        schema:
          type: integer
          minimum: 1
          maximum: 100
          default: 50
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TicketAuditPage'
        '401': *id006
        '403': *id006
        '404': *id006
      description: 티켓 변경 이력 조회 API입니다. 인증된 직원의 역할과 티켓 접근·변경 권한을 서버에서 확인한 뒤 처리합니다.
  /api/v1/agent/search:
    post:
      summary: 상담사가 접근 가능한 티켓 검색
      operationId: searchAgentWorkspace
      x-deskseed-contract-status: FROZEN
      tags:
      - Agent Search
      x-deskseed-phase: R1
      x-deskseed-requirements:
      - REQ-AUD-003
      - REQ-AUD-004
      - REQ-AUD-005
      - REQ-AUD-008
      - REQ-PERM-001
      - REQ-SRCH-001
      - REQ-SLA-001
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - *id008
      - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentTicketSearchRequest'
      responses:
        '200':
          headers:
            Cache-Control:
              description: 결과가 보호된 직원용 projection이므로 항상 no-store를 사용합니다.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentTicketSearchPage'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
        '503': *id006
      description: 상담사가 현재 server-side ticket-read authorization 범위에서 검색합니다. query는 request body에서만 받고 보호된 encrypted/fingerprint audit으로 처리하며 URL, cursor, ordinary log에 포함하지 않습니다. score/ticketNumber cursor는 normalized query/filter/sort와 첫 snapshot에 서명 결합되고 exact resultCount와 nextCursor를 반환합니다.
  /api/v1/agent/customers/search:
    post:
      summary: 상담사가 요청자 후보로 선택할 기존 고객 검색
      operationId: searchAgentCustomers
      x-deskseed-contract-status: FROZEN
      tags:
      - Agent Customers
      x-deskseed-phase: M3
      x-deskseed-requirements:
      - REQ-SRCH-002
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - *id008
      - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentCustomerSearchRequest'
      responses:
        '200':
          headers:
            Cache-Control:
              description: 결과가 보호된 직원용 projection이므로 항상 no-store를 사용합니다.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentCustomerSearchPage'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
        '503': *id006
      description: 상담사가 신규 티켓 생성 시 요청자로 선택할 기존 고객을 이름·이메일 부분 문자열로 검색하는 API입니다. 조회 자체가 검색 감사로 기록되며, 인증된 직원의 역할을 서버에서
        확인한 뒤 처리합니다.
  /api/v1/agent/assignment-options:
    get:
      summary: 티켓 생성·배정에 사용할 활성 그룹과 구성원 목록 조회
      operationId: listTicketAssignmentOptions
      x-deskseed-contract-status: FROZEN
      tags:
      - Agent Tickets
      x-deskseed-phase: M3
      x-deskseed-requirements:
      - REQ-TKT-009
      - REQ-TKT-011
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      responses:
        '200':
          headers:
            Cache-Control:
              description: 활성 그룹·구성원 목록이 자주 바뀔 수 있는 직원용 projection이므로 항상 no-store를 사용합니다.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TicketAssignmentOptions'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActor'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
      description: 아직 티켓이 존재하지 않는 신규 생성 화면에서도 배정 가능한 활성 그룹과 그 활성 구성원 목록을 조회하는 API입니다. 티켓 상세 응답에 내장된 assignmentOptions와
        동일한 스키마를 사용합니다.
  /api/v1/admin/staff:
    get:
      summary: 직원 계정 목록 조회
      operationId: listStaffAccounts
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin
      x-deskseed-phase: M6
      x-deskseed-requirements:
      - REQ-PERM-002
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - $ref: '#/components/parameters/AdminPageNumber'
      - $ref: '#/components/parameters/AdminPageSize'
      responses:
        '200':
          headers:
            X-Page-Number:
              $ref: '#/components/headers/PageNumber'
            X-Page-Size:
              $ref: '#/components/headers/PageSize'
            X-Total-Count:
              $ref: '#/components/headers/TotalCount'
            X-Total-Pages:
              $ref: '#/components/headers/TotalPages'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/StaffAccount'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
      description: 직원 계정 목록 조회 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
    post:
      summary: 직원 계정 생성
      operationId: createStaffAccount
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin
      x-deskseed-phase: M6
      x-deskseed-requirements:
      - REQ-PERM-002
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/CsrfHeader'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateStaffAccount'
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StaffAccount'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
      description: 직원 계정 생성 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
  /api/v1/admin/staff/{staffId}:
    delete:
      summary: 직원 계정 비활성화
      operationId: disableStaffAccount
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin
      x-deskseed-phase: M6
      x-deskseed-requirements:
      - REQ-AUTH-005
      - REQ-PERM-002
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/StaffId'
      - $ref: '#/components/parameters/CsrfHeader'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      responses:
        '204': {}
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActor'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
      description: 직원 계정 비활성화 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
  /api/v1/admin/staff/{staffId}/audit-authorities/{authority}:
    put:
      summary: 보안 감사 세부 권한 부여
      operationId: grantStaffAuditAuthority
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin
      x-deskseed-phase: M6
      x-deskseed-requirements:
      - REQ-AUD-002
      - REQ-AUD-006
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/StaffId'
      - $ref: '#/components/parameters/GrantableAuditAuthorityParam'
      - $ref: '#/components/parameters/CsrfHeader'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      responses:
        '204': {}
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
        '503': *id006
      description: 보안 감사 세부 권한 부여 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
    delete:
      summary: 보안 감사 세부 권한 회수
      operationId: revokeStaffAuditAuthority
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin
      x-deskseed-phase: M6
      x-deskseed-requirements:
      - REQ-AUD-002
      - REQ-AUD-006
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/StaffId'
      - $ref: '#/components/parameters/GrantableAuditAuthorityParam'
      - $ref: '#/components/parameters/CsrfHeader'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      responses:
        '204': {}
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
        '503': *id006
      description: 보안 감사 세부 권한 회수 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
  /api/v1/admin/groups:
    get:
      summary: 지원 그룹 목록 조회
      operationId: listGroups
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin
      x-deskseed-phase: M6
      x-deskseed-requirements:
      - REQ-PERM-002
      - REQ-TKT-011
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - $ref: '#/components/parameters/AdminPageNumber'
      - $ref: '#/components/parameters/AdminPageSize'
      responses:
        '200':
          headers:
            X-Page-Number:
              $ref: '#/components/headers/PageNumber'
            X-Page-Size:
              $ref: '#/components/headers/PageSize'
            X-Total-Count:
              $ref: '#/components/headers/TotalCount'
            X-Total-Pages:
              $ref: '#/components/headers/TotalPages'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Group'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
      description: 지원 그룹 목록 조회 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
    post:
      summary: 지원 그룹 생성
      operationId: createGroup
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin
      x-deskseed-phase: M6
      x-deskseed-requirements:
      - REQ-PERM-002
      - REQ-TKT-011
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/CsrfHeader'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateGroup'
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Group'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
      description: 지원 그룹 생성 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
  /api/v1/admin/groups/{groupId}:
    patch:
      summary: 지원 그룹 이름 변경
      operationId: updateGroup
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin
      x-deskseed-phase: M6
      x-deskseed-requirements:
      - REQ-PERM-002
      - REQ-TKT-011
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/GroupId'
      - $ref: '#/components/parameters/CsrfHeader'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateGroup'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Group'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
      description: 지원 그룹 이름 변경 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
    delete:
      summary: 사용하지 않는 지원 그룹 비활성화
      operationId: disableGroup
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin
      x-deskseed-phase: M6
      x-deskseed-requirements:
      - REQ-PERM-002
      - REQ-TKT-011
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/GroupId'
      - $ref: '#/components/parameters/CsrfHeader'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      responses:
        '204': {}
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActor'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
      description: 사용하지 않는 지원 그룹 비활성화 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
  /api/v1/admin/groups/{groupId}/members:
    get:
      summary: 지원 그룹의 활성 구성원 조회
      operationId: listGroupMembers
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin
      x-deskseed-phase: M6
      x-deskseed-requirements:
      - REQ-PERM-002
      - REQ-TKT-011
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/GroupId'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - $ref: '#/components/parameters/AdminPageNumber'
      - $ref: '#/components/parameters/AdminPageSize'
      responses:
        '200':
          headers:
            X-Page-Number:
              $ref: '#/components/headers/PageNumber'
            X-Page-Size:
              $ref: '#/components/headers/PageSize'
            X-Total-Count:
              $ref: '#/components/headers/TotalCount'
            X-Total-Pages:
              $ref: '#/components/headers/TotalPages'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/GroupMembership'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
      description: 지원 그룹의 활성 구성원 조회 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
    post:
      summary: 지원 그룹에 직원 추가
      operationId: createGroupMembership
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin
      x-deskseed-phase: M6
      x-deskseed-requirements:
      - REQ-PERM-002
      - REQ-TKT-011
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/GroupId'
      - $ref: '#/components/parameters/CsrfHeader'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateGroupMembership'
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GroupMembership'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
      description: 지원 그룹에 직원 추가 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
  /api/v1/admin/groups/{groupId}/members/{staffId}:
    delete:
      summary: 지원 그룹에서 직원 제거
      operationId: deleteGroupMembership
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin
      x-deskseed-phase: M6
      x-deskseed-requirements:
      - REQ-PERM-002
      - REQ-TKT-011
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/GroupId'
      - $ref: '#/components/parameters/StaffId'
      - $ref: '#/components/parameters/CsrfHeader'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      responses:
        '204': {}
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActor'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
      description: 지원 그룹에서 직원 제거 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
  /api/v1/admin/integration-clients:
    get:
      summary: 연동 클라이언트 목록 조회
      operationId: listIntegrationClients
      x-deskseed-contract-status: FROZEN
      tags:
      - Integration Admin
      x-deskseed-phase: I1
      x-deskseed-requirements:
      - REQ-INT-002
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - $ref: '#/components/parameters/AdminPageNumber'
      - $ref: '#/components/parameters/AdminPageSize'
      responses:
        '200':
          headers:
            Cache-Control:
              schema:
                type: string
                enum:
                - no-store
            X-Page-Number:
              $ref: '#/components/headers/PageNumber'
            X-Page-Size:
              $ref: '#/components/headers/PageSize'
            X-Total-Count:
              $ref: '#/components/headers/TotalCount'
            X-Total-Pages:
              $ref: '#/components/headers/TotalPages'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/IntegrationClient'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
      description: 연동 클라이언트 목록 조회 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
    post:
      summary: 연동 클라이언트와 최초 API 키 발급
      operationId: createIntegrationClient
      x-deskseed-contract-status: FROZEN
      tags:
      - Integration Admin
      x-deskseed-phase: I1
      x-deskseed-requirements:
      - REQ-INT-002
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/CsrfHeader'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateIntegrationClient'
      responses:
        '201':
          headers:
            Cache-Control:
              schema:
                type: string
                enum:
                - no-store
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntegrationCredentialIssue'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
        '503': *id006
      description: 연동 클라이언트와 최초 API 키 발급 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
  /api/v1/admin/integration-clients/{clientId}:
    get:
      summary: 연동 클라이언트 상세 조회
      operationId: getIntegrationClient
      x-deskseed-contract-status: FROZEN
      tags:
      - Integration Admin
      x-deskseed-phase: I1
      x-deskseed-requirements:
      - REQ-INT-002
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/IntegrationClientId'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      responses:
        '200':
          headers:
            Cache-Control:
              schema:
                type: string
                enum:
                - no-store
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntegrationClient'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActor'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
      description: 연동 클라이언트 상세 조회 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
  /api/v1/admin/integration-clients/{clientId}/disable:
    post:
      summary: 연동 클라이언트 즉시 비활성화
      operationId: disableIntegrationClient
      x-deskseed-contract-status: FROZEN
      tags:
      - Integration Admin
      x-deskseed-phase: I1
      x-deskseed-requirements:
      - REQ-INT-002
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/IntegrationClientId'
      - $ref: '#/components/parameters/CsrfHeader'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      responses:
        '200':
          headers:
            Cache-Control:
              schema:
                type: string
                enum:
                - no-store
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntegrationClient'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActor'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
        '503': *id006
      description: 연동 클라이언트 즉시 비활성화 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
  /api/v1/admin/integration-clients/{clientId}/revoke:
    post:
      summary: 연동 클라이언트와 자격 증명 영구 폐기
      operationId: revokeIntegrationClient
      x-deskseed-contract-status: FROZEN
      tags:
      - Integration Admin
      x-deskseed-phase: I1
      x-deskseed-requirements:
      - REQ-INT-002
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/IntegrationClientId'
      - $ref: '#/components/parameters/CsrfHeader'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      responses:
        '200':
          headers:
            Cache-Control:
              schema:
                type: string
                enum:
                - no-store
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntegrationClient'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActor'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
        '503': *id006
      description: 연동 클라이언트와 자격 증명 영구 폐기 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
  /api/v1/admin/integration-clients/{clientId}/rotate:
    post:
      summary: 연동 API 키 교체
      operationId: rotateIntegrationClientCredential
      x-deskseed-contract-status: FROZEN
      tags:
      - Integration Admin
      x-deskseed-phase: I1
      x-deskseed-requirements:
      - REQ-INT-002
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/IntegrationClientId'
      - $ref: '#/components/parameters/CsrfHeader'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RotateIntegrationCredential'
      responses:
        '200':
          headers:
            Cache-Control:
              schema:
                type: string
                enum:
                - no-store
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntegrationCredentialIssue'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
        '503': *id006
      description: 연동 API 키 교체 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
  /api/v1/admin/external-systems:
    get:
      summary: 외부 시스템 등록 목록 조회
      operationId: listAdminExternalSystems
      x-deskseed-contract-status: FROZEN
      tags:
      - Integration Admin
      x-deskseed-phase: I2
      x-deskseed-requirements:
      - REQ-INT-005
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      responses:
        '200':
          headers:
            Cache-Control:
              schema:
                type: string
                enum:
                - no-store
          content:
            application/json:
              schema:
                type: array
                maxItems: 100
                items:
                  $ref: '#/components/schemas/ExternalSystem'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActor'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
      description: 외부 시스템 등록 목록 조회 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
    post:
      summary: 외부 시스템과 허용 호스트 등록
      description: '외부 시스템과 허용 호스트 등록 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다. 기존 계약의 세부 조건: Registers
        one system while the bounded registry contains fewer than 100 entries. The next create returns EXTERNAL_SYSTEM_LIMIT_REACHED;
        list responses are never silently truncated.'
      operationId: createExternalSystem
      x-deskseed-contract-status: FROZEN
      tags:
      - Integration Admin
      x-deskseed-phase: I2
      x-deskseed-requirements:
      - REQ-INT-005
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateExternalSystem'
      responses:
        '201':
          headers:
            Cache-Control:
              schema:
                type: string
                enum:
                - no-store
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExternalSystem'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
        '503': *id006
  /api/v1/admin/external-systems/{systemId}:
    put:
      summary: 외부 시스템 설정 변경
      operationId: updateExternalSystem
      x-deskseed-contract-status: FROZEN
      tags:
      - Integration Admin
      x-deskseed-phase: I2
      x-deskseed-requirements:
      - REQ-INT-005
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExternalSystemId'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - $ref: '#/components/parameters/CsrfHeader'
      - $ref: '#/components/parameters/ExternalSystemIfMatch'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateExternalSystem'
      responses:
        '200':
          headers:
            Cache-Control:
              schema:
                type: string
                enum:
                - no-store
            ETag:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExternalSystem'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
        '412':
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/ExternalSystemVersionPreconditionProblem'
        '503': *id006
      description: 외부 시스템 설정 변경 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
  /api/v1/admin/settings/{key}:
    get:
      summary: 관리자 설정 조회
      operationId: getAdminSetting
      tags:
      - Admin
      x-deskseed-phase: M6
      x-deskseed-requirements:
      - REQ-TKT-004
      - REQ-AUD-009
      security:
      - staffSession: []
      parameters:
      - name: key
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminSetting'
        '401': *id006
        '403': *id006
        '404': *id006
      description: 관리자 설정 조회 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
    patch:
      summary: 관리자 설정 변경
      operationId: updateAdminSetting
      tags:
      - Admin
      x-deskseed-phase: M6
      x-deskseed-requirements:
      - REQ-TKT-004
      - REQ-AUD-009
      security:
      - staffSession: []
      parameters:
      - name: key
        in: path
        required: true
        schema:
          type: string
      - name: If-Match
        in: header
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAdminSetting'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminSetting'
        '400': *id006
        '401': *id006
        '403': *id006
        '404': *id006
        '409': *id006
        '412': *id006
      description: 관리자 설정 변경 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
  /api/v1/audit/activities:
    get:
      summary: 통합 감사 활동 검색
      operationId: listAuditActivities
      x-deskseed-contract-status: FROZEN
      tags:
      - Audit
      x-deskseed-phase: R2
      x-deskseed-requirements:
      - REQ-AUD-002
      - REQ-AUD-006
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - name: from
        in: query
        schema:
          type: string
          format: date-time
      - name: to
        in: query
        schema:
          type: string
          format: date-time
      - name: ledger
        in: query
        schema:
          $ref: '#/components/schemas/AuditLedgerType'
      - name: action
        in: query
        schema:
          type: string
          minLength: 1
          maxLength: 80
      - name: actorType
        in: query
        schema:
          type: string
          enum:
          - CUSTOMER
          - STAFF
          - INTEGRATION_CLIENT
          - TRIGGER
          - AUTOMATION
          - SYSTEM
      - name: actorId
        in: query
        schema:
          type: string
          format: uuid
      - name: ticketNumber
        in: query
        schema:
          type: integer
          format: int64
          minimum: 1
      - name: groupId
        in: query
        schema:
          type: string
          format: uuid
      - name: field
        in: query
        schema:
          type: string
          minLength: 1
          maxLength: 60
      - name: source
        in: query
        schema:
          type: string
          enum:
          - CUSTOMER_PORTAL
          - AGENT_UI
          - ADMIN_UI
          - PLATFORM_API
          - TRIGGER
          - AUTOMATION
          - SYSTEM_JOB
      - name: outcome
        in: query
        schema:
          $ref: '#/components/schemas/AuditOutcome'
      - name: requestId
        in: query
        schema:
          type: string
          minLength: 1
          maxLength: 100
      - name: correlationId
        in: query
        schema:
          type: string
          minLength: 1
          maxLength: 100
      - name: searchFingerprint
        in: query
        schema:
          type: string
          minLength: 1
          maxLength: 100
      - name: cursor
        in: query
        schema:
          type: string
      - name: limit
        in: query
        schema:
          type: integer
          minimum: 1
          maximum: 100
          default: 50
      - $ref: '#/components/parameters/AuditInteractionHeader'
      responses:
        '200':
          headers:
            Cache-Control:
              schema:
                type: string
                enum:
                - no-store
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditActivityPage'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
        '503': *id006
      description: 통합 감사 활동 검색 API입니다. 별도 감사 권한을 가진 직원만 사용할 수 있고 감사 화면 조회·공개·내보내기 행위도 다시 감사됩니다.
  /api/v1/audit/activities/{activityId}:
    get:
      summary: 감사 활동 상세 조회
      operationId: getAuditActivity
      x-deskseed-contract-status: FROZEN
      tags:
      - Audit
      x-deskseed-phase: R2
      x-deskseed-requirements:
      - REQ-AUD-002
      - REQ-AUD-004
      - REQ-AUD-006
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/AuditActivityId'
      - $ref: '#/components/parameters/AuditInteractionHeader'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      responses:
        '200':
          headers:
            Cache-Control:
              schema:
                type: string
                enum:
                - no-store
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditActivityDetail'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActor'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
        '503': *id006
      description: 감사 활동 상세 조회 API입니다. 별도 감사 권한을 가진 직원만 사용할 수 있고 감사 화면 조회·공개·내보내기 행위도 다시 감사됩니다.
  /api/v1/audit/activities/{activityId}/search-query-reveal:
    post:
      summary: 보호된 검색어 원문 공개
      operationId: revealAuditSearchQuery
      x-deskseed-contract-status: FROZEN
      tags:
      - Audit
      x-deskseed-phase: R2
      x-deskseed-requirements:
      - REQ-AUD-005
      - REQ-AUD-006
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/AuditActivityId'
      - $ref: '#/components/parameters/AuditInteractionHeader'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SearchQueryRevealRequest'
      responses:
        '200':
          headers:
            Cache-Control:
              schema:
                type: string
                enum:
                - no-store
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SearchQueryRevealResult'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
        '422': *id006
        '503': *id006
      description: 보호된 검색어 원문 공개 API입니다. 별도 감사 권한을 가진 직원만 사용할 수 있고 감사 화면 조회·공개·내보내기 행위도 다시 감사됩니다.
  /api/v1/audit/exports:
    post:
      summary: 감사 활동 내보내기 요청
      operationId: createAuditExport
      x-deskseed-contract-status: FROZEN
      description: '감사 활동 projection을 short-lived CSV 또는 JSONL artifact로 내보내기 요청합니다. 현재 `audit:export` capability, owner, bounded field/filter/reason을 검증하고 permission snapshot과 self-audit을 job과 함께 저장합니다. worker와 artifact store 실패는 READY를 만들지 않으며 protected content와 raw query는 export field set에 존재하지 않습니다.'
      tags:
      - Audit
      x-deskseed-phase: R2
      x-deskseed-requirements:
      - REQ-AUD-006
      - REQ-EXP-001
      - REQ-AUDX-001
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/AuditInteractionHeader'
      - $ref: '#/components/parameters/CsrfHeader'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAuditExport'
      responses:
        '202':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExportJob'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
        '429': *id006
        '503': *id006
  /api/v1/audit/exports/{jobId}:
    get:
      summary: 감사 내보내기 작업 상태 조회
      operationId: getAuditExport
      x-deskseed-contract-status: FROZEN
      tags:
      - Audit
      x-deskseed-phase: R2
      x-deskseed-requirements:
      - REQ-AUD-006
      - REQ-AUDX-001
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - name: jobId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - $ref: '#/components/parameters/AuditInteractionHeader'
      responses:
        '200':
          headers:
            Cache-Control:
              schema:
                type: string
                enum:
                - no-store
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExportJob'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActor'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
        '503': *id006
      description: 감사 내보내기 작업 상태 조회 API입니다. 별도 감사 권한을 가진 직원만 사용할 수 있고 감사 화면 조회·공개·내보내기 행위도 다시 감사됩니다.
  /api/v1/audit/exports/{jobId}/download:
    get:
      summary: 준비된 감사 내보내기 artifact 다운로드
      operationId: downloadAuditExport
      x-deskseed-contract-status: FROZEN
      tags:
      - Audit
      x-deskseed-phase: P1
      x-deskseed-requirements:
      - REQ-AUD-006
      - REQ-AUDX-001
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - name: jobId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - $ref: '#/components/parameters/AuditInteractionHeader'
      responses:
        '200':
          headers:
            Cache-Control:
              schema:
                type: string
                const: no-store
            Content-Disposition:
              schema:
                type: string
            Content-Type:
              schema:
                type: string
            X-Content-Checksum-SHA256:
              schema:
                type: string
                pattern: ^[0-9a-f]{64}$
          content:
            text/csv:
              schema:
                type: string
                format: binary
            application/x-ndjson:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActor'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
        '410': *id006
        '503': *id006
      description: 요청자 본인이며 현재도 `audit:export` capability를 가진 경우에만 READY artifact를 private store에서 stream합니다. 응답은 항상 no-store이고 다운로드 access/self-audit이 실패하면 bytes를 반환하지 않습니다. EXPIRED/삭제 artifact는 410 또는 not-found-safe 응답이며, CSV는 formula injection을 escape한 서버 생성 데이터만 포함합니다.
  /api/v1/audit/projection/rebuild:
    post:
      summary: 감사 활동 조회 프로젝션 재구축
      operationId: rebuildAuditActivityProjection
      x-deskseed-contract-status: FROZEN
      tags:
      - Audit
      x-deskseed-phase: R2
      x-deskseed-requirements:
      - REQ-AUD-002
      - REQ-AUD-006
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/AuditInteractionHeader'
      - $ref: '#/components/parameters/CsrfHeader'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditProjectionRebuildResult'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActor'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
        '503': *id006
      description: 감사 활동 조회 프로젝션 재구축 API입니다. 별도 감사 권한을 가진 직원만 사용할 수 있고 감사 화면 조회·공개·내보내기 행위도 다시 감사됩니다.
  /api/v1/customer/requests/{ticketNumber}:
    parameters:
    - &id010
      name: ticketNumber
      in: path
      required: true
      schema:
        type: integer
        format: int64
        minimum: 1
    get:
      summary: 인증 고객의 문의 상세 조회
      operationId: getCustomerRequest
      x-deskseed-contract-status: FROZEN
      tags:
      - Customer Portal
      x-deskseed-phase: P1
      x-deskseed-requirements:
      - REQ-TKT-003
      - REQ-TKT-005
      security: &id009
      - customerSession: []
      responses:
        '200':
          headers:
            Cache-Control:
              schema:
                type: string
                enum:
                - no-store
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerRequestDetail'
        '401':
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '404':
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
      description: 인증 고객의 문의 상세 조회 API입니다. 고객용 projection만 반환하며 INTERNAL 댓글, 자식 관계, 직원 전용 필드와 감사 메타데이터를 노출하지 않습니다.
  /api/v1/customer/requests/{ticketNumber}/comments:
    parameters:
    - *id010
    post:
      summary: 인증 고객의 공개 답변 추가
      operationId: addAuthenticatedCustomerComment
      description: '인증 고객의 공개 답변 추가 API입니다. 고객용 projection만 반환하며 INTERNAL 댓글, 자식 관계, 직원 전용 필드와 감사 메타데이터를 노출하지 않습니다. 기존 계약의
        세부 조건: Creates one PUBLIC customer comment, one canonical ticket audit, and one durable request-received mail intent
        in the same transaction. An exact requester/clientCommandId replay returns the original comment; reuse for a different
        ticket or body returns 409. SOLVED and CLOSED requests are not reopened by this operation.'
      x-deskseed-contract-status: FROZEN
      tags:
      - Customer Portal
      x-deskseed-phase: P1
      x-deskseed-requirements:
      - REQ-TKT-005
      security: *id009
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddAuthenticatedCustomerComment'
      responses:
        '201':
          headers:
            Cache-Control:
              schema:
                type: string
                enum:
                - no-store
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PublicComment'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401':
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '404':
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '409':
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
  /api/v1/customer/requests/{ticketNumber}/attachments/uploads:
    parameters:
    - *id010
    post:
      summary: 인증 고객 문의의 PUBLIC 첨부파일 업로드
      operationId: createAuthenticatedCustomerAttachmentUpload
      x-deskseed-contract-status: FROZEN
      tags:
      - Customer Portal
      x-deskseed-phase: P1
      x-deskseed-requirements:
      - REQ-FILE-001
      - REQ-TKT-003
      - REQ-TKT-008
      security: *id009
      parameters:
      - $ref: '#/components/parameters/CustomerCsrfHeader'
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AttachmentUploadForm'
      responses:
        '201':
          headers:
            Cache-Control:
              schema:
                type: string
                const: no-store
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttachmentUpload'
        '400': *id002
        '401': *id002
        '403': *id002
        '404': *id002
        '413': *id002
        '415': *id002
        '422': *id002
        '429': *id002
        '503': *id002
      description: 인증 고객 세션과 CSRF를 확인하고 requester, ticket number, CUSTOMER_REQUEST kind가 모두 일치하는 own ticket을 먼저 찾습니다. 그 뒤 bounded stream을 private quarantine에 기록하고 MIME·checksum·malware scan을 통과한 CLEAN handle을 CUSTOMER actor, CUSTOMER_PORTAL source, PUBLIC visibility와 해당 ticket에 묶어 반환합니다. 다른 고객이나 없는 ticket은 같은 not-found-safe 응답으로 처리하며 object key, checksum, scan detail, public URL 또는 bytes는 JSON에 포함하지 않습니다.
  /api/v1/customer/requests/{ticketNumber}/attachments/{attachmentId}/download:
    parameters:
    - *id010
    - $ref: '#/components/parameters/AttachmentId'
    get:
      summary: 인증 고객 문의의 PUBLIC 첨부파일 다운로드
      operationId: downloadAuthenticatedCustomerAttachment
      x-deskseed-contract-status: FROZEN
      tags:
      - Customer Portal
      x-deskseed-phase: P1
      x-deskseed-requirements:
      - REQ-FILE-001
      - REQ-TKT-005
      - REQ-TKT-008
      security: *id009
      responses:
        '200':
          headers:
            Cache-Control:
              schema:
                type: string
                const: no-store
            Content-Disposition:
              schema:
                type: string
            Content-Type:
              schema:
                type: string
                const: application/octet-stream
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400': *id002
        '401': *id002
        '404': *id002
        '409': *id002
        '503': *id002
      description: 인증 고객이 소유한 path ticket을 attachment보다 먼저 확인한 뒤 그 ticket의 PUBLIC comment에 link된 CLEAN, non-expired object만 stream합니다. 다른 고객·ticket, INTERNAL, unlinked, quarantined, infected, failed, deleted, expired object는 같은 not-found-safe 응답으로 bytes를 차단합니다. 성공 stream 전 ATTACHMENT_DOWNLOADED access audit을 CUSTOMER_SESSION auth type과 purpose-bound session fingerprint로 commit하며 필수 감사 저장이 실패하면 성공 응답을 반환하지 않습니다.
  /api/v1/admin/settings/customer-access-mode:
    get:
      summary: 고객 접근 모드 설정 조회
      operationId: getCustomerAccessModeSetting
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin
      x-deskseed-phase: P1
      x-deskseed-requirements:
      - REQ-TKT-004
      - REQ-AUD-009
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerAccessModeSetting'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActor'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
      description: 고객 접근 모드 설정 조회 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
    put:
      summary: 고객 접근 모드 설정 변경
      operationId: updateCustomerAccessModeSetting
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin
      x-deskseed-phase: P1
      x-deskseed-requirements:
      - REQ-TKT-004
      - REQ-AUD-009
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/CsrfHeader'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateCustomerAccessMode'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerAccessModeSetting'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActor'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
      description: 고객 접근 모드 설정 변경 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
  /api/v1/admin/mail/summary:
    get:
      summary: 아웃바운드 메일 운영 상태 조회
      operationId: getOutboundMailSummary
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin
      x-deskseed-phase: P0
      x-deskseed-requirements:
      - REQ-NOTIF-001
      - REQ-CHAN-003
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      responses:
        '200':
          headers:
            Cache-Control:
              schema:
                type: string
                const: no-store
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OutboundMailOperationsSummary'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
        '503':
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
      description: ADMIN이 전송 활성화·스케줄·전송 방식과 상태별 건수만 확인하는 안전한 운영 진단 API입니다. SMTP 호스트, 자격 증명, 수신자, 본문과 보호된 콘텐츠는 반환하지 않습니다.
  /api/v1/admin/mail/intents:
    get:
      summary: 아웃바운드 메일 의도 목록 조회
      operationId: listOutboundMailIntents
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin
      x-deskseed-phase: P0
      x-deskseed-requirements:
      - REQ-NOTIF-001
      - REQ-CHAN-003
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - name: status
        in: query
        required: false
        description: 지정하면 해당 전송 상태만 키셋 페이지로 조회합니다. cursor와 함께 사용할 때는 첫 요청의 상태를 바꾸면 안 됩니다.
        schema:
          $ref: '#/components/schemas/OutboundMailIntentStatus'
      - name: cursor
        in: query
        required: false
        description: 이전 응답의 nextCursor입니다. 서명되며 상태 필터에 결합됩니다.
        schema:
          type: string
          maxLength: 2000
      - name: limit
        in: query
        required: false
        description: 한 페이지의 최대 의도 수입니다.
        schema:
          type: integer
          minimum: 1
          maximum: 100
          default: 50
      responses:
        '200':
          headers:
            Cache-Control:
              schema:
                type: string
                const: no-store
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OutboundMailIntentPage'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
        '503':
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
      description: ADMIN이 마스킹된 수신자와 안전한 상태·시도·시간 메타데이터만 조회하는 키셋 목록 API입니다. 원본 수신자, 제목, 본문, 토큰, 암호문, nonce와 provider 응답은 반환하지 않습니다.
  /api/v1/admin/mail/intents/{intentId}:
    get:
      summary: 아웃바운드 메일 의도 상세 조회
      operationId: getOutboundMailIntent
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin
      x-deskseed-phase: P0
      x-deskseed-requirements:
      - REQ-NOTIF-001
      - REQ-CHAN-003
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - name: intentId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          headers:
            Cache-Control:
              schema:
                type: string
                const: no-store
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OutboundMailIntent'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '404':
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
        '503':
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
      description: ADMIN이 한 전송 의도와 시도 이력을 마스킹된 운영 projection으로 조회합니다. providerMessageId, provider 응답, 원본 수신자와 메일 콘텐츠는 계약상 제외됩니다.
  /api/v1/admin/mail/intents/{intentId}/retry:
    post:
      summary: 실패한 아웃바운드 메일 의도 재시도
      operationId: retryOutboundMailIntent
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin
      x-deskseed-phase: P0
      x-deskseed-requirements:
      - REQ-NOTIF-001
      - REQ-CHAN-003
      security:
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/CsrfHeader'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - name: intentId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RetryOutboundMailIntent'
      responses:
        '200':
          headers:
            Cache-Control:
              schema:
                type: string
                const: no-store
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OutboundMailIntent'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '404':
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
        '503':
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
      description: ADMIN이 terminal FAILED 상태의 기존 전송 의도를 같은 intent로 다시 QUEUED 상태로 전환합니다. 하나의 intent만 잠그며 별도 티켓·comment·의도를 만들지 않고, 전환과 보안 감사는 같은 트랜잭션에서 성공해야 합니다.
  /api/v1/admin/business-schedules:
    get:
      summary: 영업 시간표 목록 조회
      operationId: listBusinessSchedules
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin Schedule
      x-deskseed-phase: P3
      x-deskseed-requirements:
      - REQ-SLA-002
      security: &id011
      - staffSession: []
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/BusinessSchedule'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActor'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
      description: 영업 시간표 목록 조회 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
    post:
      summary: 영업 시간표 생성
      operationId: createBusinessSchedule
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin Schedule
      x-deskseed-phase: P3
      x-deskseed-requirements:
      - REQ-SLA-002
      security: *id011
      parameters:
      - $ref: '#/components/parameters/CsrfHeader'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BusinessScheduleInput'
      responses:
        '201':
          headers:
            ETag:
              schema:
                type: string
              description: 따옴표로 감싼 일정 aggregate 버전입니다.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BusinessSchedule'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
      description: 영업 시간표 생성 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
  /api/v1/admin/business-schedules/preview:
    post:
      summary: 저장 전 영업 시간표 검증 및 미리보기
      operationId: previewBusinessSchedule
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin Schedule
      x-deskseed-phase: P3
      x-deskseed-requirements:
      - REQ-SLA-002
      security: *id011
      parameters:
      - $ref: '#/components/parameters/CsrfHeader'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BusinessTimePreviewRequest'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BusinessTimePreviewResult'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
      description: 저장 전 영업 시간표 검증 및 미리보기 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
  /api/v1/admin/business-schedules/{scheduleId}:
    parameters:
    - &id012
      name: scheduleId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    get:
      summary: 영업 시간표 최신 버전 조회
      operationId: getBusinessSchedule
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin Schedule
      x-deskseed-phase: P3
      x-deskseed-requirements:
      - REQ-SLA-002
      security: *id011
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      responses:
        '200':
          headers:
            ETag:
              schema:
                type: string
              description: 따옴표로 감싼 일정 aggregate 버전입니다.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BusinessSchedule'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActor'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
      description: 영업 시간표 최신 버전 조회 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
  /api/v1/admin/business-schedules/{scheduleId}/versions:
    parameters:
    - *id012
    get:
      summary: 영업 시간표 버전 이력 조회
      operationId: listBusinessScheduleVersions
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin Schedule
      x-deskseed-phase: P3
      x-deskseed-requirements:
      - REQ-SLA-002
      security: *id011
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/BusinessSchedule'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActor'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
      description: 영업 시간표 버전 이력 조회 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
    post:
      summary: 영업 시간표 새 버전 생성
      operationId: createBusinessScheduleVersion
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin Schedule
      x-deskseed-phase: P3
      x-deskseed-requirements:
      - REQ-SLA-002
      security: *id011
      parameters:
      - $ref: '#/components/parameters/CsrfHeader'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - &id013
        name: If-Match
        in: header
        required: true
        schema:
          type: string
          pattern: ^"[0-9]+"$
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BusinessScheduleInput'
      responses:
        '201':
          headers:
            ETag:
              schema:
                type: string
              description: 따옴표로 감싼 일정 aggregate 버전입니다.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BusinessSchedule'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
        '412': *id006
      description: 영업 시간표 새 버전 생성 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
  /api/v1/admin/business-schedules/{scheduleId}/versions/{version}/activation:
    parameters:
    - *id012
    - name: version
      in: path
      required: true
      schema:
        type: integer
        minimum: 1
    put:
      summary: 영업 시간표 버전 활성화
      operationId: activateBusinessScheduleVersion
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin Schedule
      x-deskseed-phase: P3
      x-deskseed-requirements:
      - REQ-SLA-002
      security: *id011
      parameters:
      - $ref: '#/components/parameters/CsrfHeader'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - *id013
      responses:
        '200':
          headers:
            ETag:
              schema:
                type: string
              description: 따옴표로 감싼 일정 aggregate 버전입니다.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BusinessSchedule'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
        '412': *id006
      description: 영업 시간표 버전 활성화 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
  /api/v1/admin/sla-policies:
    get:
      summary: 최초 답변 SLA 정책 목록 조회
      operationId: listSlaPolicies
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin SLA
      x-deskseed-phase: P3
      x-deskseed-requirements:
      - REQ-SLA-001
      - REQ-SLA-003
      security: *id011
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/FirstReplySlaPolicy'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActor'
        '401':
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '403':
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
      description: 최초 답변 SLA 정책 목록 조회 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
    post:
      summary: 최초 답변 SLA 정책 생성
      operationId: createSlaPolicy
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin SLA
      x-deskseed-phase: P3
      x-deskseed-requirements:
      - REQ-SLA-001
      - REQ-SLA-003
      security: *id011
      parameters:
      - $ref: '#/components/parameters/CsrfHeader'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FirstReplySlaPolicyInput'
      responses:
        '201':
          headers:
            ETag:
              schema:
                type: string
              description: 따옴표로 감싼 정책 aggregate 버전입니다.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FirstReplySlaPolicy'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401':
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '403':
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
      description: 최초 답변 SLA 정책 생성 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
  /api/v1/admin/sla-policies/preview:
    post:
      summary: 최초 답변 SLA 정책 적용 결과 미리보기
      operationId: previewFirstReplySlaPolicy
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin SLA
      x-deskseed-phase: P3
      x-deskseed-requirements:
      - REQ-SLA-001
      - REQ-SLA-003
      security: *id011
      parameters:
      - $ref: '#/components/parameters/CsrfHeader'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FirstReplySlaPreviewRequest'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FirstReplySlaPreviewResult'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
      description: 최초 답변 SLA 정책 적용 결과 미리보기 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
  /api/v1/admin/sla-policies/{policyId}:
    parameters:
    - &id014
      name: policyId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    get:
      summary: 최초 답변 SLA 정책 최신 버전 조회
      operationId: getSlaPolicy
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin SLA
      x-deskseed-phase: P3
      x-deskseed-requirements:
      - REQ-SLA-001
      - REQ-SLA-003
      security: *id011
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      responses:
        '200':
          headers:
            ETag:
              schema:
                type: string
              description: 따옴표로 감싼 정책 aggregate 버전입니다.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FirstReplySlaPolicy'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActor'
        '401':
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '403':
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '404':
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
      description: 최초 답변 SLA 정책 최신 버전 조회 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
  /api/v1/admin/sla-policies/{policyId}/versions:
    parameters:
    - *id014
    get:
      summary: 최초 답변 SLA 정책 버전 이력 조회
      operationId: listSlaPolicyVersions
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin SLA
      x-deskseed-phase: P3
      x-deskseed-requirements:
      - REQ-SLA-001
      - REQ-SLA-003
      security: *id011
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/FirstReplySlaPolicy'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActor'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
      description: 최초 답변 SLA 정책 버전 이력 조회 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
    post:
      summary: 최초 답변 SLA 정책 새 버전 생성
      operationId: createSlaPolicyVersion
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin SLA
      x-deskseed-phase: P3
      x-deskseed-requirements:
      - REQ-SLA-001
      - REQ-SLA-003
      security: *id011
      parameters:
      - $ref: '#/components/parameters/CsrfHeader'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - name: If-Match
        in: header
        required: true
        schema:
          type: string
          pattern: ^"[0-9]+"$
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FirstReplySlaPolicyInput'
      responses:
        '201':
          headers:
            ETag:
              schema:
                type: string
              description: 따옴표로 감싼 정책 aggregate 버전입니다.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FirstReplySlaPolicy'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
        '412': *id006
      description: 최초 답변 SLA 정책 새 버전 생성 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
  /api/v1/admin/sla-policies/{policyId}/versions/{version}/activation:
    parameters:
    - *id014
    - name: version
      in: path
      required: true
      schema:
        type: integer
        minimum: 1
    put:
      summary: 최초 답변 SLA 정책 버전 활성화
      operationId: activateSlaPolicyVersion
      x-deskseed-contract-status: FROZEN
      tags:
      - Admin SLA
      x-deskseed-phase: P3
      x-deskseed-requirements:
      - REQ-SLA-001
      - REQ-SLA-003
      security: *id011
      parameters:
      - $ref: '#/components/parameters/CsrfHeader'
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - name: If-Match
        in: header
        required: true
        schema:
          type: string
          pattern: ^"[0-9]+"$
      responses:
        '200':
          headers:
            ETag:
              schema:
                type: string
              description: 따옴표로 감싼 정책 aggregate 버전입니다.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FirstReplySlaPolicy'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActorOrBadRequest'
        '401': *id006
        '403': *id006
        '404': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatchOrConflict'
        '412': *id006
      description: 최초 답변 SLA 정책 버전 활성화 API입니다. ADMIN 또는 명시된 관리 권한이 있는 직원만 사용할 수 있으며 변경은 보안 감사와 함께 처리됩니다.
  /api/v1/analytics/first-reply-sla:
    get:
      summary: 최초 답변 SLA 성과 집계 조회
      operationId: getFirstReplySlaAnalytics
      x-deskseed-contract-status: FROZEN
      tags:
      - Analytics
      x-deskseed-phase: P3
      x-deskseed-requirements:
      - REQ-SLA-001
      security: *id011
      parameters:
      - $ref: '#/components/parameters/ExpectedStaffActorHeader'
      - name: policyId
        in: query
        schema:
          type: string
          format: uuid
      - name: priority
        in: query
        schema:
          $ref: '#/components/schemas/TicketPriority'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FirstReplySlaAnalytics'
        '400':
          $ref: '#/components/responses/InvalidExpectedStaffActor'
        '401': *id006
        '403': *id006
        '409':
          $ref: '#/components/responses/StaffSessionActorMismatch'
      description: 최초 답변 SLA 성과 집계 조회 API입니다. 인증된 직원의 역할과 티켓 접근·변경 권한을 서버에서 확인한 뒤 처리합니다.
components:
  securitySchemes:
    staffSession:
      type: apiKey
      in: cookie
      name: DESKSEED_SESSION
      description: HttpOnly 서버 세션입니다. 브라우저 요청은 선택적인 ExpectedStaffActorHeader 일관성 guard도 사용하지만, 이 헤더는 자격 증명이 아니며 인증 actor를
        결정하지 않습니다.
    customerSession:
      type: apiKey
      in: cookie
      name: DESKSEED_CUSTOMER_SESSION
  parameters:
    CsrfHeader:
      name: X-CSRF-TOKEN
      in: header
      required: true
      description: getStaffCsrfToken이 반환하며 브라우저 세션에 귀속되는 토큰입니다.
      schema:
        type: string
        minLength: 16
        maxLength: 256
    CustomerCsrfHeader:
      name: X-CSRF-TOKEN
      in: header
      required: true
      description: getCustomerCsrfToken이 반환하며 현재 customerSession에 귀속되는 토큰입니다. 세션 cookie 원문이나 이 토큰은 로그 또는 감사 메타데이터에 저장하지 않습니다.
      schema:
        type: string
        minLength: 16
        maxLength: 256
    ExpectedStaffActorHeader:
      name: X-Deskseed-Expected-Staff-Id
      in: header
      required: false
      description: staffSession 작업을 위한 심층 방어용 브라우저 일관성 guard입니다. 전달할 때는 이미 인증된 세션 주체와 같은 하나의 정규 UUID여야 합니다. 생략은 호환되지만 형식 오류나
        중복은 InvalidExpectedStaffActor, 불일치는 Controller 실행·감사 성공·변경·세션 활동 갱신 전에 StaffSessionActorMismatch를 반환합니다. 로그인 시작과 로그인
        직후 신원 확인에서는 의도적으로 생략합니다.
      schema:
        type: string
        format: uuid
    AdminPageNumber:
      name: page
      in: query
      required: false
      description: 0부터 시작하는 페이지 번호입니다.
      schema:
        type: integer
        minimum: 0
        default: 0
    AdminPageSize:
      name: size
      in: query
      required: false
      description: 관리자 목록 응답 한 페이지에서 반환할 수 있는 제한된 행 수입니다.
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 50
    IntegrationClientId:
      name: clientId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    ExternalSystemId:
      name: systemId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    ExternalReferenceId:
      name: referenceId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    ExternalSystemIfMatch:
      name: If-Match
      in: header
      required: true
      description: 외부 시스템용 ETag이며 expectedVersion과 같은 버전을 가리켜야 합니다.
      schema:
        type: string
        pattern: ^"?[0-9]+"?$
    StaffId:
      name: staffId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    GrantableAuditAuthorityParam:
      name: authority
      in: path
      required: true
      schema:
        $ref: '#/components/schemas/GrantableAuditAuthority'
    GroupId:
      name: groupId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    AuditActivityId:
      name: activityId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    AuditInteractionHeader:
      name: X-Interaction-Id
      in: header
      required: true
      description: 감사자의 탐색·필터 상호작용 식별자이며 cursor 요청은 첫 페이지의 값을 재사용합니다.
      schema:
        type: string
        format: uuid
    AgentInteractionHeader:
      name: X-Interaction-Id
      in: header
      required: true
      description: 직원이 의도적으로 view preview 또는 attachment를 연 상호작용 식별자입니다. background prefetch는 semantic view/download를 만들지 않는 별도 intent를 사용합니다.
      schema:
        type: string
        format: uuid
    SavedViewKey:
      name: viewKey
      in: path
      required: true
      description: SYSTEM slug 또는 생성된 opaque saved-view key입니다.
      schema:
        type: string
        minLength: 1
        maxLength: 100
        pattern: ^[A-Za-z0-9._:-]+$
    SavedViewIfMatch:
      name: If-Match
      in: header
      required: true
      description: 저장된 definition version을 인용 부호 포함/미포함 정수 ETag로 보냅니다.
      schema:
        type: string
        pattern: ^"?[0-9]+"?$
    AttachmentId:
      name: attachmentId
      in: path
      required: true
      schema:
        type: string
        format: uuid
  headers:
    PageNumber:
      description: 응답으로 반환된 0부터 시작하는 페이지 번호입니다.
      schema:
        type: integer
        minimum: 0
    PageSize:
      description: 이 페이지에서 요청한 최대 행 수입니다.
      schema:
        type: integer
        minimum: 1
        maximum: 100
    TotalCount:
      description: 목록 조건에 일치하는 전체 행 수입니다.
      schema:
        type: integer
        format: int64
        minimum: 0
    TotalPages:
      description: 요청한 페이지 크기를 기준으로 계산한 전체 페이지 수입니다.
      schema:
        type: integer
        minimum: 0
  responses:
    InvalidExpectedStaffActor:
      description: 선택적인 직원 actor 확인 헤더가 전달됐지만 하나의 정규 UUID 값이 아닙니다.
      headers:
        Cache-Control:
          schema:
            type: string
            const: no-store
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/InvalidExpectedStaffActorProblem'
          example:
            type: /problems/invalid-staff-session-actor
            title: Invalid staff session actor
            status: 400
            detail: The expected staff actor header is invalid.
            requestId: req-actor-400
    StaffSessionActorMismatch:
      description: 브라우저가 확인한 actor와 인증된 세션 주체가 다릅니다.
      headers:
        Cache-Control:
          schema:
            type: string
            const: no-store
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/StaffSessionActorMismatchProblem'
          example:
            type: /problems/staff-session-actor-mismatch
            title: Staff session actor changed
            status: 409
            detail: Refresh the staff session before continuing.
            requestId: req-actor-409
    InvalidExpectedStaffActorOrBadRequest:
      description: 정규 UUID가 아닌 expected-actor 헤더를 포함한 RFC 9457 요청 검증 실패 응답입니다.
      headers:
        Cache-Control:
          schema:
            type: string
            const: no-store
      content:
        application/problem+json:
          schema:
            anyOf:
            - $ref: '#/components/schemas/InvalidExpectedStaffActorProblem'
            - $ref: '#/components/schemas/Problem'
          examples:
            invalidExpectedStaffActor:
              value:
                type: /problems/invalid-staff-session-actor
                title: Invalid staff session actor
                status: 400
                detail: The expected staff actor header is invalid.
                requestId: req-actor-400
    StaffSessionActorMismatchOrConflict:
      description: 브라우저가 확인한 actor와 인증 세션 주체가 다른 경우를 포함한 RFC 9457 비즈니스 충돌 응답입니다.
      headers:
        Cache-Control:
          schema:
            type: string
            const: no-store
      content:
        application/problem+json:
          schema:
            anyOf:
            - $ref: '#/components/schemas/StaffSessionActorMismatchProblem'
            - $ref: '#/components/schemas/Problem'
          examples:
            staffSessionActorMismatch:
              value:
                type: /problems/staff-session-actor-mismatch
                title: Staff session actor changed
                status: 409
                detail: Refresh the staff session before continuing.
                requestId: req-actor-409
  schemas:
    Problem:
      type: object
      required:
      - type
      - title
      - status
      properties:
        type:
          type: string
          format: uri-reference
        title:
          type: string
        status:
          type: integer
        detail:
          type: string
        instance:
          type: string
          format: uri-reference
        requestId:
          type: string
        code:
          type: string
        fieldErrors:
          type: array
          items:
            $ref: '#/components/schemas/FieldError'
        currentVersion:
          type: integer
          format: int64
        conflictingFields:
          type: array
          items:
            type: string
      additionalProperties: true
    InvalidExpectedStaffActorProblem:
      allOf:
      - $ref: '#/components/schemas/Problem'
      - type: object
        required:
        - type
        - title
        - status
        properties:
          type:
            type: string
            const: /problems/invalid-staff-session-actor
          title:
            type: string
            const: Invalid staff session actor
          status:
            type: integer
            const: 400
    StaffSessionActorMismatchProblem:
      allOf:
      - $ref: '#/components/schemas/Problem'
      - type: object
        required:
        - type
        - title
        - status
        properties:
          type:
            type: string
            const: /problems/staff-session-actor-mismatch
          title:
            type: string
            const: Staff session actor changed
          status:
            type: integer
            const: 409
    TicketFieldConflictProblem:
      allOf:
      - $ref: '#/components/schemas/Problem'
      - type: object
        required:
        - currentVersion
        - conflictingFields
        properties:
          type:
            type: string
            const: /problems/ticket-field-conflict
          status:
            type: integer
            const: 409
          currentVersion:
            type: integer
            format: int64
          conflictingFields:
            type: array
            minItems: 1
            uniqueItems: true
            items:
              type: string
              enum:
              - status
              - priority
              - groupId
              - assigneeId
    ClientCommandIdReusedProblem:
      allOf:
      - $ref: '#/components/schemas/Problem'
      - type: object
        properties:
          type:
            type: string
            const: /problems/client-command-id-reused
          status:
            type: integer
            const: 409
    TicketVersionPreconditionProblem:
      allOf:
      - $ref: '#/components/schemas/Problem'
      - type: object
        required:
        - currentVersion
        properties:
          type:
            type: string
            const: /problems/ticket-version-precondition-failed
          status:
            type: integer
            const: 412
          currentVersion:
            type: integer
            format: int64
    FieldError:
      type: object
      required:
      - field
      - message
      properties:
        field:
          type: string
        message:
          type: string
        code:
          type: string
    CustomerRequestRequester:
      type: object
      additionalProperties: false
      required: [name, email]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 100
          description: 익명 문의에서 격리된 미검증 Customer를 표시할 이름입니다.
        email:
          type: string
          format: email
          maxLength: 254
          description: 익명 문의의 연락·rate-limit 대상 이메일입니다. 입력만으로 기존 Customer를 병합하거나 ticket을 claim하지 않습니다.
    AcceptedCustomerPolicyVersion:
      type: object
      additionalProperties: false
      required: [policyKey, version]
      properties:
        policyKey:
          type: string
          pattern: '^[a-z][a-z0-9]*(?:-[a-z0-9]+)*$'
          maxLength: 100
          description: REQUEST_SUBMISSION context에서 고객이 확인한 current published policy의 stable key입니다.
        version:
          type: integer
          minimum: 1
          description: 고객이 실제로 확인하고 수락한 immutable published version입니다.
    CreateCustomerRequest:
      type: object
      additionalProperties: false
      required: [clientCommandId, subject, message, fieldValues, acceptedPolicies]
      dependentRequired:
        formId: [formVersion]
        formVersion: [formId]
      properties:
        clientCommandId:
          type: string
          format: uuid
          description: First-party browser가 생성해 transport retry 동안 유지하는 high-entropy command identity입니다. 서버는 raw 값을 저장하지 않습니다.
        requester:
          $ref: '#/components/schemas/CustomerRequestRequester'
          description: 익명 요청에는 필수이고 customerSession 요청에는 생략합니다. 인증 세션과 함께 전달하면 identity 충돌로 거부합니다.
        subject:
          type: string
          minLength: 1
          maxLength: 200
          description: 생성되는 고객 문의의 제목입니다.
        message:
          type: string
          minLength: 1
          maxLength: 20000
          description: Ticket.description이 아니라 생성 Ticket의 첫 번째 PUBLIC comment body로 저장합니다.
        formId:
          type: string
          format: uuid
          description: 서버가 projection한 current published customer form ID입니다. formVersion과 함께 보내거나 둘 다 생략합니다.
        formVersion:
          type: integer
          minimum: 1
          description: 최종 transaction에서 current published version과 다시 비교하고 ticket에 값이 없어도 보존할 form version입니다.
        fieldValues:
          type: object
          maxProperties: 100
          propertyNames:
            pattern: '^[a-z][a-z0-9]*(?:[.-][a-z0-9]+)*$'
            maxLength: 120
          additionalProperties:
            $ref: '#/components/schemas/CustomerTicketFieldValue'
          description: customer-visible/editable field machine key에 대응하는 typed candidate 값입니다. unknown/staff-only 입력은 존재를 누출하지 않는 validation 실패입니다.
        acceptedPolicies:
          type: array
          maxItems: 20
          uniqueItems: true
          items:
            $ref: '#/components/schemas/AcceptedCustomerPolicyVersion'
          description: REQUEST_SUBMISSION context의 required current policy version 목록입니다. 중복 policyKey와 stale/missing version은 최종 transaction에서 거부합니다.
      description: 익명 또는 customerSession 고객이 form-aware 문의를 생성하는 동일 domain command입니다. 익명은 requester가 필수이고 인증 고객은 세션 identity만 사용합니다.
      x-deskseed-documentation-review: MANUAL
      example:
        clientCommandId: 08a85f1c-9939-43f7-a90a-657ac5acb935
        requester:
          name: 김민아
          email: mina.kim@example.test
        subject: 주문 ORD-2026-1042 상태 확인 요청
        message: 합성 테스트 주문의 현재 상태를 확인해 주세요.
        formId: 00000000-0000-4000-8000-000000000001
        formVersion: 3
        fieldValues:
          request.type: {optionId: 00000000-0000-4000-8000-000000000101}
          order.reference: {shortTextValue: ORD-2026-1042}
        acceptedPolicies:
          - {policyKey: test-inquiry-data-processing, version: 2}
    CreateCustomerRequestResult:
      type: object
      additionalProperties: false
      required:
      - ticketNumber
      - status
      - accessToken
      - createdAt
      - replayed
      properties:
        ticketNumber:
          type: integer
          format: int64
          description: 사람이 식별하고 검색할 때 사용하는 티켓 번호입니다.
        status:
          $ref: '#/components/schemas/TicketStatus'
        accessToken:
          type: string
          description: 익명·인증 모드 모두 생성 시에만 반환하는 티켓 범위 조회·연결 복구 토큰입니다. 서버는 digest만 저장하고 소유권이 명시적으로 연결되면 활성 토큰을 폐기합니다.
        createdAt:
          type: string
          format: date-time
        replayed:
          type: boolean
          description: 기존 logical ticket의 exact command replay인지 나타냅니다. true여도 accessToken은 저장된 raw capability 재사용이 아닌 fresh bounded grant입니다.
    CreateCustomerRequestMultipart:
      type: object
      additionalProperties: false
      required: [request]
      properties:
        request:
          $ref: '#/components/schemas/CreateCustomerRequest'
          description: application/json part로 보내는 CreateCustomerRequest이며 JSON endpoint와 같은 command와 validation을 사용합니다.
        attachments:
          type: array
          maxItems: 5
          items:
            type: string
            format: binary
      description: CreateCustomerRequest JSON part와 최대 5개 파일을 함께 보내는 요청입니다. 익명 요청은 서버가 client가 보낼 수 없는 plannedCustomerId를 생성하고 Customer row를 만들지 않은 채 그 opaque UUID 소유로 bounded streaming, quarantine, MIME·malware 검사를 끝냅니다. 최종 검증 뒤 같은 최종 transaction에서 Customer(id = plannedCustomerId), Ticket, CLEAN attachment link, acceptance, audit, fresh access grant와 mail intent를 commit합니다. 파일 실패나 stale form/policy는 Customer/Ticket을 남기지 않으며 먼저 성공한 파일은 unlinked TTL cleanup 대상입니다.
      x-deskseed-anonymous-attachment-owner: SERVER_PLANNED_CUSTOMER_UUID
      x-deskseed-documentation-review: MANUAL
      example:
        request:
          clientCommandId: 08a85f1c-9939-43f7-a90a-657ac5acb935
          requester: {name: 김민아, email: mina.kim@example.test}
          subject: 주문 ORD-2026-1042 승인 내역 확인 요청
          message: 합성 테스트 승인 내역 PDF를 함께 보냅니다.
          formId: 00000000-0000-4000-8000-000000000001
          formVersion: 3
          fieldValues:
            order.reference: {shortTextValue: ORD-2026-1042}
          acceptedPolicies:
            - {policyKey: test-inquiry-data-processing, version: 2}
        attachments:
        - approval-history.pdf
    AttachmentUploadForm:
      type: object
      additionalProperties: false
      required:
      - file
      properties:
        file:
          type: string
          format: binary
      description: 이미 인증되었거나 ticket-scoped request token을 검증한 actor가 하나의 파일을 quarantine upload할 때 사용하는 multipart 본문입니다. filename 확장자만 신뢰하지 않으며 서버가 streaming size, MIME, checksum과 malware scan을 검사합니다.
      x-deskseed-documentation-review: MANUAL
      example:
        file: approval-history.pdf
    AddCustomerComment:
      type: object
      additionalProperties: false
      required:
      - body
      - clientCommandId
      properties:
        body:
          type: string
          minLength: 1
          maxLength: 20000
          description: 고객이 문의 대화에 추가하는 PUBLIC 댓글 본문입니다. INTERNAL 메모는 지정할 수 없습니다.
        attachmentIds:
          type: array
          maxItems: 5
          uniqueItems: true
          items:
            type: string
            format: uuid
          description: 같은 ticket request-token으로 upload한 CLEAN PUBLIC attachment handle입니다. link는 이 comment/TicketAudit transaction에서만 수행됩니다.
        clientCommandId:
          type: string
          minLength: 1
          maxLength: 100
          pattern: ^[A-Za-z0-9._:-]+$
          description: 네트워크 재전송에서도 유지하는 고객 명령 식별자입니다. 동일 식별자는 같은 티켓과 같은 본문에만 재사용할 수 있습니다.
      description: 접근 토큰을 가진 고객이 자신의 문의에 PUBLIC 댓글을 추가할 때 사용하는 명령입니다.
      x-deskseed-documentation-review: MANUAL
      example:
        body: 결제 카드 승인 내역을 추가로 확인해 주세요.
        attachmentIds:
        - 55555555-5555-4555-8555-555555555555
        clientCommandId: 77777777-7777-4777-8777-777777777777
    AddAuthenticatedCustomerComment:
      type: object
      additionalProperties: false
      required:
      - body
      - clientCommandId
      properties:
        body:
          type: string
          minLength: 1
          maxLength: 20000
          description: 인증 고객이 자신의 문의 대화에 추가하는 PUBLIC 댓글 본문입니다. INTERNAL 메모는 지정할 수 없습니다.
        clientCommandId:
          type: string
          minLength: 1
          maxLength: 100
          pattern: ^[A-Za-z0-9._:-]+$
          description: 네트워크 재전송에도 유지하는 고객 명령 식별자입니다. 동일 식별자는 같은 requester, ticket, body와 attachment ID 집합에만 재사용할 수 있습니다.
        attachmentIds:
          type: array
          maxItems: 5
          uniqueItems: true
          items:
            type: string
            format: uuid
          description: 같은 인증 고객이 이 ticket에 PUBLIC visibility로 upload한 CLEAN handle입니다. link는 이 comment, TicketAudit, ordered events, mail intent transaction에서만 수행됩니다.
      description: 인증된 고객이 자신에게 공개된 문의에 PUBLIC 댓글을 남길 때 사용하는 명령입니다. clientCommandId는 전송 재시도에도 같은 값을 유지해 중복 댓글 생성을 막습니다.
      x-deskseed-documentation-review: MANUAL
      example:
        body: 결제 카드 승인 내역을 첨부했습니다. 확인 부탁드립니다.
        attachmentIds:
        - 55555555-5555-4555-8555-555555555555
        clientCommandId: 77777777-7777-4777-8777-777777777777
    PublicComment:
      type: object
      additionalProperties: false
      required:
      - id
      - authorDisplayName
      - body
      - createdAt
      properties:
        id:
          type: string
          format: uuid
        authorDisplayName:
          type: string
        body:
          type: string
        createdAt:
          type: string
          format: date-time
        attachments:
          type: array
          items:
            $ref: '#/components/schemas/TicketAttachment'
    PublicAttachment:
      deprecated: true
      description: 호환을 위해 남은 PUBLIC-only alias입니다. 새 customer/staff projection은 `TicketAttachment`을 사용하며 public URL을 제공하지 않습니다.
      allOf:
      - $ref: '#/components/schemas/TicketAttachment'
    TicketAttachment:
      type: object
      additionalProperties: false
      required:
      - id
      - fileName
      - sizeBytes
      - contentType
      properties:
        id:
          type: string
          format: uuid
        fileName:
          type: string
          minLength: 1
          maxLength: 255
        sizeBytes:
          type: integer
          format: int64
          minimum: 0
        contentType:
          type: string
          maxLength: 127
      description: CLEAN 상태로 comment에 link된 metadata입니다. object key, scan detail, checksum, public/signed URL은 포함하지 않으며 download endpoint가 server authorization을 다시 검사합니다.
    AttachmentUpload:
      type: object
      additionalProperties: false
      required:
      - id
      - fileName
      - sizeBytes
      - contentType
      - scanStatus
      - expiresAt
      properties:
        id:
          type: string
          format: uuid
        fileName:
          type: string
          minLength: 1
          maxLength: 255
        sizeBytes:
          type: integer
          format: int64
          minimum: 0
        contentType:
          type: string
          maxLength: 127
        scanStatus:
          type: string
          const: CLEAN
        expiresAt:
          type: string
          format: date-time
      description: 아직 ticket/comment에 link되지 않은 CLEAN upload handle입니다. expiresAt 전의 정상 ticket create/comment transaction에서만 one-time link할 수 있고 private storage locator는 반환하지 않습니다.
    CustomerRequestDetail:
      type: object
      additionalProperties: false
      required:
      - ticketNumber
      - subject
      - status
      - createdAt
      - updatedAt
      - comments
      properties:
        ticketNumber:
          type: integer
          format: int64
          description: 사람이 식별하고 검색할 때 사용하는 티켓 번호입니다.
        subject:
          type: string
        status:
          $ref: '#/components/schemas/TicketStatus'
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        comments:
          type: array
          items:
            $ref: '#/components/schemas/PublicComment'
    CustomerRequestPage:
      type: object
      additionalProperties: false
      required:
      - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/CustomerRequestSummary'
        nextCursor:
          type:
          - string
          - 'null'
    CustomerRequestSummary:
      type: object
      additionalProperties: false
      required:
      - ticketNumber
      - subject
      - status
      - createdAt
      - updatedAt
      properties:
        ticketNumber:
          type: integer
          format: int64
          description: 사람이 식별하고 검색할 때 사용하는 티켓 번호입니다.
        subject:
          type: string
        status:
          $ref: '#/components/schemas/TicketStatus'
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    CustomerAccessMode:
      type: string
      enum:
      - ANONYMOUS_ALLOWED
      - REGISTRATION_OPTIONAL
      - REGISTRATION_REQUIRED
    CustomerAccessModeResponse:
      type: object
      additionalProperties: false
      required:
      - mode
      properties:
        mode:
          $ref: '#/components/schemas/CustomerAccessMode'
    CustomerAccessModeSetting:
      type: object
      additionalProperties: false
      required:
      - mode
      - version
      - updatedAt
      properties:
        mode:
          $ref: '#/components/schemas/CustomerAccessMode'
        version:
          type: integer
          format: int64
          minimum: 0
        updatedAt:
          type: string
          format: date-time
    UpdateCustomerAccessMode:
      type: object
      additionalProperties: false
      required:
      - mode
      - expectedVersion
      properties:
        mode:
          $ref: '#/components/schemas/CustomerAccessMode'
        expectedVersion:
          type: integer
          format: int64
          minimum: 0
      description: 고객 문의 접근 정책을 변경할 때 사용하는 요청입니다. expectedVersion은 현재 설정의 버전과 일치해야 하며, 정책 변경은 관리자 보안 감사와 함께 처리됩니다.
      x-deskseed-documentation-review: MANUAL
      example:
        mode: REGISTRATION_OPTIONAL
        expectedVersion: 3
    OutboundMailIntentStatus:
      type: string
      enum:
      - QUEUED
      - SENDING
      - RETRY_WAIT
      - SENT
      - FAILED
    OutboundMailAttemptStatus:
      type: string
      enum:
      - IN_PROGRESS
      - SUCCEEDED
      - RETRYABLE_FAILED
      - PERMANENT_FAILED
      - ABANDONED
    OutboundMailOperationsSummary:
      type: object
      additionalProperties: false
      required:
      - deliveryEnabled
      - schedulingEnabled
      - transport
      - queuedCount
      - sendingCount
      - retryWaitCount
      - failedCount
      - sentCount
      - oldestPendingAt
      properties:
        deliveryEnabled:
          type: boolean
          description: 실제 delivery worker가 활성화되었는지 나타내는 opt-in 상태입니다.
        schedulingEnabled:
          type: boolean
          description: worker 실행을 위한 내부 스케줄러 활성화 상태입니다.
        transport:
          type: string
          enum:
          - DISABLED
          - SMTP
          - FAKE
          description: 선택된 provider-neutral 전송 방식입니다. 호스트나 자격 증명은 포함하지 않습니다.
        queuedCount:
          type: integer
          format: int64
          minimum: 0
        sendingCount:
          type: integer
          format: int64
          minimum: 0
        retryWaitCount:
          type: integer
          format: int64
          minimum: 0
        failedCount:
          type: integer
          format: int64
          minimum: 0
        sentCount:
          type: integer
          format: int64
          minimum: 0
        oldestPendingAt:
          type:
          - string
          - 'null'
          format: date-time
          description: QUEUED, SENDING 또는 RETRY_WAIT 의도 중 가장 오래된 queuedAt입니다.
      description: 메일 본문·수신자·자격 증명 없이 운영 상태를 확인하기 위한 ADMIN projection입니다.
    OutboundMailAttempt:
      type: object
      additionalProperties: false
      required:
      - attemptNumber
      - retryCycle
      - cycleAttemptNumber
      - status
      - failureClass
      - failureCode
      - startedAt
      - finishedAt
      - nextRetryAt
      properties:
        attemptNumber:
          type: integer
          minimum: 1
        retryCycle:
          type: integer
          minimum: 0
        cycleAttemptNumber:
          type: integer
          minimum: 1
        status:
          $ref: '#/components/schemas/OutboundMailAttemptStatus'
        failureClass:
          type:
          - string
          - 'null'
          maxLength: 40
        failureCode:
          type:
          - string
          - 'null'
          maxLength: 80
          description: provider 원문이 아닌 정규화된 안전 오류 코드입니다.
        startedAt:
          type: string
          format: date-time
        finishedAt:
          type:
          - string
          - 'null'
          format: date-time
        nextRetryAt:
          type:
          - string
          - 'null'
          format: date-time
    OutboundMailIntent:
      type: object
      additionalProperties: false
      required:
      - id
      - template
      - templateVersion
      - status
      - recipientMasked
      - attemptCount
      - maxAttempts
      - retryCycle
      - manualRetryCount
      - nextAttemptAt
      - leaseExpiresAt
      - lastErrorCode
      - queuedAt
      - sentAt
      - failedAt
      - attempts
      properties:
        id:
          type: string
          format: uuid
        template:
          type: string
          enum:
          - CUSTOMER_MAGIC_LINK
          - REQUEST_RECEIVED
          - PUBLIC_AGENT_REPLY
        templateVersion:
          type: integer
          minimum: 1
        status:
          $ref: '#/components/schemas/OutboundMailIntentStatus'
        recipientMasked:
          type: string
          maxLength: 254
          description: 로컬 파트를 숨긴 수신자 표시값입니다. 원본 mailbox는 이 API에 포함되지 않습니다.
          example: '***@example.com'
        attemptCount:
          type: integer
          minimum: 0
        maxAttempts:
          type: integer
          minimum: 1
        retryCycle:
          type: integer
          minimum: 0
        manualRetryCount:
          type: integer
          minimum: 0
        nextAttemptAt:
          type:
          - string
          - 'null'
          format: date-time
        leaseExpiresAt:
          type:
          - string
          - 'null'
          format: date-time
        lastErrorCode:
          type:
          - string
          - 'null'
          maxLength: 80
          description: provider 원문이 아닌 마지막 안전 오류 코드입니다.
        queuedAt:
          type: string
          format: date-time
        sentAt:
          type:
          - string
          - 'null'
          format: date-time
        failedAt:
          type:
          - string
          - 'null'
          format: date-time
        attempts:
          type: array
          maxItems: 1000
          items:
            $ref: '#/components/schemas/OutboundMailAttempt'
      description: 안전한 운영 조회용 전송 의도입니다. sender, raw recipient, subject, body, protected ciphertext/nonce/key version, request/correlation IDs, provider message ID와 provider response는 의도적으로 제외됩니다.
    OutboundMailIntentPage:
      type: object
      additionalProperties: false
      required:
      - items
      - nextCursor
      properties:
        items:
          type: array
          maxItems: 100
          items:
            $ref: '#/components/schemas/OutboundMailIntent'
        nextCursor:
          type:
          - string
          - 'null'
          maxLength: 2000
          description: 다음 페이지에만 사용할 수 있는 상태 필터 결합 서명 cursor입니다.
    RetryOutboundMailIntent:
      type: object
      additionalProperties: false
      required:
      - reason
      properties:
        reason:
          type: string
          minLength: 1
          maxLength: 500
          description: 운영자가 terminal FAILED 의도를 다시 시도하는 이유입니다. 응답에는 되돌려 주지 않습니다.
      description: 고객에게 공개된 답변 메일이 영구 실패한 뒤 주소 또는 SMTP 설정을 확인한 운영자가 같은 전송 의도를 재큐잉할 때 사용합니다. 상태 전환과 보안 감사는 함께 commit됩니다.
      x-deskseed-documentation-review: MANUAL
      example:
        reason: SMTP 수신 거부 원인을 확인해 주소를 정정한 뒤 같은 접수 완료 메일 재시도
    StaffLogin:
      type: object
      additionalProperties: false
      required:
      - email
      - password
      properties:
        email:
          type: string
          format: email
          maxLength: 254
        password:
          type: string
          format: password
          minLength: 1
          maxLength: 128
          writeOnly: true
      description: 직원이 이메일과 password로 브라우저 세션을 시작할 때 사용하는 요청입니다. password는 인증에만 사용하며 문서·로그·감사 기록에 원문을 남기지 않습니다.
      x-deskseed-documentation-review: MANUAL
      example:
        email: agent.park@example.test
        password: not-a-real-password
    CsrfToken:
      type: object
      additionalProperties: false
      required:
      - token
      - headerName
      properties:
        token:
          type: string
          minLength: 16
          maxLength: 256
        headerName:
          type: string
          enum:
          - X-CSRF-TOKEN
    CurrentStaff:
      type: object
      additionalProperties: false
      required:
      - id
      - email
      - displayName
      - role
      - capabilities
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
          format: email
        displayName:
          type: string
        role:
          $ref: '#/components/schemas/StaffRole'
        capabilities:
          type: array
          items:
            type: string
    SavedView:
      type: object
      additionalProperties: false
      required:
      - id
      - key
      - name
      - scope
      - active
      - definitionVersion
      - orderVersion
      - conditions
      - columns
      - sort
      - readScope
      properties:
        id:
          type: string
          format: uuid
        key:
          type: string
          maxLength: 100
        name:
          type: string
          minLength: 1
          maxLength: 120
        description:
          type: string
          maxLength: 500
          description: 상담사가 View의 목적과 사용 범위를 기록하는 plain-text 설명입니다. 검색 조건으로 해석하지 않으며 제어문자를 허용하지 않습니다.
        scope:
          $ref: '#/components/schemas/SavedViewScope'
        ownerStaffId:
          type:
          - string
          - 'null'
          format: uuid
        active:
          type: boolean
        definitionVersion:
          type: integer
          format: int64
          minimum: 1
        orderVersion:
          type: integer
          format: int64
          minimum: 1
        categoryPath:
          type: array
          maxItems: 4
          items:
            type: string
            maxLength: 80
        conditions:
          $ref: '#/components/schemas/SavedViewConditions'
        columns:
          type: array
          minItems: 1
          maxItems: 12
          uniqueItems: true
          items:
            $ref: '#/components/schemas/SavedViewColumn'
        sort:
          $ref: '#/components/schemas/SavedViewSort'
        ticketCount:
          type:
          - integer
          - 'null'
          format: int64
          minimum: 0
        ticketCountState:
          type: string
          enum:
          - EXACT
          - OMITTED_VISIBLE_LIMIT
        ticketCountAsOf:
          type:
          - string
          - 'null'
          format: date-time
          description: EXACT 건수를 같은 batch에서 평가할 때 서버가 사용한 기준 시각입니다. 건수를 생략한 항목과 create/update 응답에서는 null입니다.
        readScope:
          type: string
          enum:
          - ALL_TICKETS
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      description: versioned saved-view definition입니다. `key`는 실행 URL의 opaque 식별자이고 id 목록이나 executable expression을 담지 않습니다. SYSTEM scope는 read-only입니다.
    SavedViewScope:
      type: string
      enum:
      - PERSONAL
      - SHARED
      - SYSTEM
    SavedViewConditionField:
      type: string
      enum:
      - STATUS
      - PRIORITY
      - GROUP
      - ASSIGNEE
      - FIRST_REPLY_SLA_STATE
      - TICKET_KIND
      - UPDATED_AT
    SavedViewConditionOperator:
      type: string
      enum:
      - EQUALS
      - NOT_EQUALS
      - IN
      - NOT_IN
      - IS_CURRENT_ACTOR
      - IS_UNASSIGNED
      - IS_CURRENT_ACTOR_GROUP
      - LESS_THAN_SOLVED
      - WITHIN_LAST_DAYS
    SavedViewCondition:
      type: object
      additionalProperties: false
      required:
      - field
      - operator
      - values
      properties:
        field:
          $ref: '#/components/schemas/SavedViewConditionField'
        operator:
          $ref: '#/components/schemas/SavedViewConditionOperator'
        values:
          type: array
          minItems: 0
          maxItems: 10
          uniqueItems: true
          items:
            type: string
            minLength: 1
            maxLength: 100
      description: field/operator 허용 조합과 values 형식은 서버 allowlist가 다시 검증합니다. IS_CURRENT_ACTOR, IS_UNASSIGNED, IS_CURRENT_ACTOR_GROUP, LESS_THAN_SOLVED는 빈 values만 허용하고 UPDATED_AT/WITHIN_LAST_DAYS는 bounded 정수 하루 수 하나만 허용합니다. raw SQL, SpEL, JavaScript, script expression은 허용하지 않습니다.
    SavedViewConditions:
      type: object
      additionalProperties: false
      required:
      - version
      - all
      - any
      properties:
        version:
          type: integer
          const: 1
        all:
          type: array
          maxItems: 12
          items:
            $ref: '#/components/schemas/SavedViewCondition'
        any:
          type: array
          maxItems: 12
          items:
            $ref: '#/components/schemas/SavedViewCondition'
      description: '`(all AND) AND (any OR)` 형태의 version 1 allowlisted condition AST입니다. 빈 all 또는 any는 각각 identity 조건으로 해석하며 두 배열이 모두 비어 있는 definition은 거부합니다.'
    SavedViewColumn:
      type: string
      enum:
      - TICKET_NUMBER
      - SUBJECT
      - STATUS
      - PRIORITY
      - GROUP
      - ASSIGNEE
      - UPDATED_AT
      - FIRST_REPLY_SLA
    SavedViewSort:
      type: string
      enum:
      - updatedAt:desc,ticketNumber:desc
    SavedViewDefinition:
      type: object
      additionalProperties: false
      required:
      - name
      - conditions
      - columns
      - sort
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 120
        description:
          type: string
          maxLength: 500
          description: 상담사가 View의 목적과 사용 범위를 기록하는 plain-text 설명입니다. 검색 조건으로 해석하지 않으며 제어문자를 허용하지 않습니다.
        conditions:
          $ref: '#/components/schemas/SavedViewConditions'
        columns:
          type: array
          minItems: 1
          maxItems: 12
          uniqueItems: true
          items:
            $ref: '#/components/schemas/SavedViewColumn'
        sort:
          $ref: '#/components/schemas/SavedViewSort'
      description: 상담사가 저장 또는 preview할 수 있는 version 1 saved-view definition입니다. 태그, arbitrary text, custom field, macro, SQL/script 조건은 version 1 범위 밖이며 field/operator/value 조합은 서버 allowlist가 검증합니다.
      x-deskseed-documentation-review: MANUAL
      example:
        name: 우선 처리할 위험 SLA
        description: 첫 답변 SLA가 임박했거나 위반된 티켓을 우선 확인합니다.
        conditions:
          version: 1
          all:
          - field: FIRST_REPLY_SLA_STATE
            operator: IN
            values:
            - AT_RISK
            - BREACHED
          any: []
        columns:
        - TICKET_NUMBER
        - SUBJECT
        - FIRST_REPLY_SLA
        - UPDATED_AT
        sort: updatedAt:desc,ticketNumber:desc
    CreateSavedView:
      type: object
      additionalProperties: false
      required:
      - scope
      - name
      - conditions
      - columns
      - sort
      properties:
        scope:
          type: string
          enum:
          - PERSONAL
          - SHARED
        name:
          type: string
          minLength: 1
          maxLength: 120
        description:
          type: string
          maxLength: 500
          description: 상담사가 View의 목적과 사용 범위를 기록하는 plain-text 설명입니다. 검색 조건으로 해석하지 않으며 제어문자를 허용하지 않습니다.
        conditions:
          $ref: '#/components/schemas/SavedViewConditions'
        columns:
          type: array
          minItems: 1
          maxItems: 12
          uniqueItems: true
          items:
            $ref: '#/components/schemas/SavedViewColumn'
        sort:
          $ref: '#/components/schemas/SavedViewSort'
      x-deskseed-documentation-review: MANUAL
      description: 현재 상담사가 새 PERSONAL definition을 만들거나, 명시적 shared-view capability를 가진 ADMIN이 SHARED definition을 만들 때 사용하는 요청입니다. SYSTEM scope는 생성할 수 없습니다.
      example:
        scope: PERSONAL
        name: 내 위험 SLA
        description: 내가 우선 확인할 위험 SLA 티켓입니다.
        conditions:
          version: 1
          all:
          - field: FIRST_REPLY_SLA_STATE
            operator: IN
            values:
            - AT_RISK
            - BREACHED
          any: []
        columns:
        - TICKET_NUMBER
        - SUBJECT
        - FIRST_REPLY_SLA
        - UPDATED_AT
        sort: updatedAt:desc,ticketNumber:desc
    UpdateSavedView:
      type: object
      additionalProperties: false
      required:
      - expectedVersion
      - name
      - conditions
      - columns
      - sort
      properties:
        expectedVersion:
          type: integer
          format: int64
          minimum: 1
        name:
          type: string
          minLength: 1
          maxLength: 120
        description:
          type: string
          maxLength: 500
          description: 상담사가 View의 목적과 사용 범위를 기록하는 plain-text 설명입니다. 검색 조건으로 해석하지 않으며 제어문자를 허용하지 않습니다.
        conditions:
          $ref: '#/components/schemas/SavedViewConditions'
        columns:
          type: array
          minItems: 1
          maxItems: 12
          uniqueItems: true
          items:
            $ref: '#/components/schemas/SavedViewColumn'
        sort:
          $ref: '#/components/schemas/SavedViewSort'
      x-deskseed-documentation-review: MANUAL
      description: owner의 PERSONAL 또는 권한 있는 ADMIN의 SHARED definition을 낙관적 version으로 갱신할 때 사용하는 요청입니다. scope·owner·SYSTEM immutable 상태는 이 요청으로 바꾸지 않습니다.
      example:
        expectedVersion: 3
        name: 내 위험 SLA
        description: SLA 위험 티켓 중 아직 해결되지 않은 요청을 확인합니다.
        conditions:
          version: 1
          all:
          - field: STATUS
            operator: LESS_THAN_SOLVED
            values: []
          any: []
        columns:
        - TICKET_NUMBER
        - SUBJECT
        - STATUS
        - FIRST_REPLY_SLA
        sort: updatedAt:desc,ticketNumber:desc
    SavedViewPreview:
      type: object
      additionalProperties: false
      required:
      - items
      - ticketCount
      - sort
      properties:
        items:
          type: array
          maxItems: 20
          items:
            $ref: '#/components/schemas/TicketSummary'
        ticketCount:
          type: integer
          format: int64
          minimum: 0
        ticketCountAsOf:
          type: string
          format: date-time
          description: preview의 exact 건수를 평가할 때 서버가 사용한 기준 시각입니다.
        sort:
          $ref: '#/components/schemas/SavedViewSort'
    ReorderSavedViews:
      type: object
      additionalProperties: false
      required:
      - scope
      - expectedOrderVersion
      - viewKeys
      properties:
        scope:
          type: string
          enum:
          - PERSONAL
          - SHARED
        expectedOrderVersion:
          type: integer
          format: int64
          minimum: 1
        viewKeys:
          type: array
          minItems: 1
          maxItems: 50
          uniqueItems: true
          items:
            type: string
            minLength: 1
            maxLength: 100
            pattern: ^[A-Za-z0-9._:-]+$
      x-deskseed-documentation-review: MANUAL
      description: PERSONAL 또는 SHARED mutable definition의 표시 순서를 그 scope의 orderVersion과 함께 원자적으로 바꾸는 요청입니다. SYSTEM key·누락·중복 key는 검증 오류입니다.
      example:
        scope: PERSONAL
        expectedOrderVersion: 4
        viewKeys:
        - pv-01J7SAVEDVIEW001
        - pv-01J7SAVEDVIEW002
    SavedViewOrder:
      type: object
      additionalProperties: false
      required:
      - scope
      - orderVersion
      - viewKeys
      properties:
        scope:
          type: string
          enum:
          - PERSONAL
          - SHARED
        orderVersion:
          type: integer
          format: int64
          minimum: 1
        viewKeys:
          type: array
          items:
            type: string
    TicketStatus:
      type: string
      description: 고객에게 공개할 수 있는 티켓 상태 projection입니다.
      enum:
      - NEW
      - OPEN
      - PENDING
      - SOLVED
    AgentTicketStatus:
      type: string
      description: 직원용 티켓 조회와 명령에서 사용하는 정규 상태 projection입니다.
      enum:
      - NEW
      - OPEN
      - PENDING
      - ON_HOLD
      - SOLVED
      - CLOSED
    TicketPriority:
      type: string
      enum:
      - LOW
      - NORMAL
      - HIGH
      - URGENT
    TicketVisibility:
      type: string
      enum:
      - PUBLIC
      - INTERNAL
    ActorSummary:
      type: object
      required:
      - type
      - displayName
      properties:
        id:
          type:
          - string
          - 'null'
          format: uuid
        type:
          type: string
          enum:
          - CUSTOMER
          - STAFF
          - INTEGRATION_CLIENT
          - TRIGGER
          - AUTOMATION
          - SYSTEM
        displayName:
          type: string
    TicketSummary:
      type: object
      required:
      - ticketNumber
      - subject
      - status
      - priority
      - requester
      - createdAt
      - updatedAt
      - version
      properties:
        ticketNumber:
          type: integer
          format: int64
          description: 사람이 식별하고 검색할 때 사용하는 티켓 번호입니다.
        subject:
          type: string
        status:
          $ref: '#/components/schemas/AgentTicketStatus'
        priority:
          $ref: '#/components/schemas/TicketPriority'
        requester:
          $ref: '#/components/schemas/ActorSummary'
        group:
          $ref: '#/components/schemas/GroupRef'
        assignee:
          $ref: '#/components/schemas/StaffRef'
        createdAt:
          type: string
          format: date-time
          description: 티켓이 최초 생성된 시각입니다. 정렬이나 현재 상태 변경으로 바뀌지 않습니다.
        updatedAt:
          type: string
          format: date-time
        version:
          type: integer
          format: int64
        isChild:
          type: boolean
        openChildCount:
          type: integer
        sla:
          $ref: '#/components/schemas/SlaBadge'
    TicketSummaryPage:
      type: object
      additionalProperties: false
      required:
      - items
      - sort
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/TicketSummary'
        nextCursor:
          type:
          - string
          - 'null'
        totalApproximate:
          type:
          - integer
          - 'null'
          format: int64
        sort:
          type: string
          const: updatedAt:desc,ticketNumber:desc
    StaffRef:
      type:
      - object
      - 'null'
      properties:
        id:
          type: string
          format: uuid
        displayName:
          type: string
    GroupRef:
      type:
      - object
      - 'null'
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
    SlaBadge:
      type:
      - object
      - 'null'
      additionalProperties: false
      required:
      - metric
      - state
      - dueAt
      - targetMinutes
      - policyVersion
      - scheduleVersion
      properties:
        metric:
          type: string
          const: FIRST_REPLY
        state:
          $ref: '#/components/schemas/FirstReplySlaDisplayState'
        dueAt:
          type:
          - string
          - 'null'
          format: date-time
        targetMinutes:
          type:
          - integer
          - 'null'
          minimum: 1
        policyVersion:
          type:
          - integer
          - 'null'
          minimum: 1
        scheduleVersion:
          type:
          - integer
          - 'null'
          minimum: 1
    FirstReplySlaDisplayState:
      type: string
      enum:
      - ACTIVE
      - AT_RISK
      - PAUSED
      - ACHIEVED
      - BREACHED
      - CANCELLED
      - NO_POLICY
    AgentComment:
      type: object
      required:
      - id
      - visibility
      - actor
      - body
      - createdAt
      - source
      properties:
        id:
          type: string
          format: uuid
        visibility:
          $ref: '#/components/schemas/TicketVisibility'
        actor:
          $ref: '#/components/schemas/ActorSummary'
        body:
          type: string
        createdAt:
          type: string
          format: date-time
        source:
          type: string
        redactionState:
          type: string
        attachments:
          type: array
          items:
            $ref: '#/components/schemas/TicketAttachment'
    AgentTicketDetail:
      type: object
      additionalProperties: false
      required:
      - ticket
      - comments
      - capabilities
      - assignmentOptions
      - context
      - history
      - warnings
      properties:
        ticket:
          $ref: '#/components/schemas/TicketSummary'
        comments:
          type: array
          items:
            $ref: '#/components/schemas/AgentComment'
        capabilities:
          type: array
          items:
            type: string
          description: 직원 상세 조회가 성공하면 READ가 항상 포함됩니다. UPDATE는 현재 actor가 별도 write authorization을 통과하고 티켓이 CLOSED가 아닐 때만 포함됩니다.
        assignmentOptions:
          $ref: '#/components/schemas/TicketAssignmentOptions'
        context:
          $ref: '#/components/schemas/TicketContext'
        history:
          type: array
          items:
            $ref: '#/components/schemas/TicketHistoryItem'
        warnings:
          type: array
          items:
            $ref: '#/components/schemas/CommandWarning'
    TicketAssignmentOptions:
      type: object
      additionalProperties: false
      required:
      - groups
      properties:
        groups:
          type: array
          description: 감사되는 직원 상세 화면에만 projection되는 활성 그룹과 활성 구성원입니다.
          items:
            $ref: '#/components/schemas/TicketAssignmentGroupOption'
    TicketAssignmentGroupOption:
      type: object
      additionalProperties: false
      required:
      - id
      - name
      - members
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        members:
          type: array
          items:
            $ref: '#/components/schemas/TicketAssignmentStaffOption'
    TicketAssignmentStaffOption:
      type: object
      additionalProperties: false
      required:
      - id
      - displayName
      properties:
        id:
          type: string
          format: uuid
        displayName:
          type: string
    TicketContext:
      type: object
      additionalProperties: false
      required:
      - customer
      - parent
      - children
      - externalReferenceCount
      properties:
        customer:
          description: 요청자가 없는 INTERNAL_WORK_ITEM 티켓에서는 null입니다.
          oneOf:
          - $ref: '#/components/schemas/TicketCustomer'
          - type: 'null'
        parent:
          oneOf:
          - $ref: '#/components/schemas/TicketSummary'
          - type: 'null'
        children:
          type: array
          items:
            $ref: '#/components/schemas/TicketSummary'
        externalReferenceCount:
          type: integer
          format: int32
          minimum: 0
          maximum: 100
          description: 이 ticket에 저장된 ExternalReference 수입니다. 전체 stored-only 상세 목록은 `listAgentTicketExternalReferences` lazy API가 정본이며 detail은 외부 URL을 fetch하지 않습니다.
    TicketCustomer:
      type: object
      additionalProperties: false
      required:
      - id
      - displayName
      - email
      properties:
        id:
          type: string
          format: uuid
        displayName:
          type: string
        email:
          type: string
          format: email
    TicketHistoryItem:
      type: object
      additionalProperties: false
      required:
      - id
      - eventType
      - actor
      - occurredAt
      properties:
        id:
          type: string
          format: uuid
        eventType:
          type: string
        actor:
          $ref: '#/components/schemas/ActorSummary'
        occurredAt:
          type: string
          format: date-time
    CommentDraft:
      type: object
      additionalProperties: false
      required:
      - visibility
      - body
      properties:
        visibility:
          $ref: '#/components/schemas/TicketVisibility'
        body:
          type: string
          minLength: 1
          maxLength: 20000
        attachmentIds:
          type: array
          maxItems: 5
          uniqueItems: true
          items:
            type: string
            format: uuid
          description: 현재 staff actor가 upload했고 아직 link되지 않은 CLEAN handle입니다. comment visibility와 attachment owner/visibility policy가 이 transaction에서 다시 검사됩니다.
    UpdateTicketCommand:
      type: object
      additionalProperties: false
      required:
      - expectedVersion
      - changedFields
      anyOf:
      - properties:
          changedFields:
            minItems: 1
      - required:
        - comment
        properties:
          comment:
            $ref: '#/components/schemas/CommentDraft'
      properties:
        expectedVersion:
          type: integer
          format: int64
          minimum: 0
        changedFields:
          type: array
          maxItems: 4
          uniqueItems: true
          items:
            type: string
            enum:
            - status
            - priority
            - groupId
            - assigneeId
        status:
          $ref: '#/components/schemas/AgentTicketStatus'
        priority:
          $ref: '#/components/schemas/TicketPriority'
        groupId:
          type:
          - string
          - 'null'
          format: uuid
        assigneeId:
          type:
          - string
          - 'null'
          format: uuid
        comment:
          oneOf:
          - $ref: '#/components/schemas/CommentDraft'
          - type: 'null'
        clientCommandId:
          type: string
          format: uuid
          description: 인증된 직원 actor의 정규 업데이트 요청 하나를 식별합니다. 동일 재시도에는 원래 결과를 반환하고, 다른 payload·티켓·작업에 재사용하면 변경 없이 409를 반환합니다.
      description: 상담사가 티켓의 상태·우선순위·소유권 변경과 댓글 작성을 하나의 원자적 명령으로 저장할 때 사용하는 요청입니다. expectedVersion과 changedFields는 실제 변경 필드와
        일치해야 합니다.
      x-deskseed-documentation-review: MANUAL
      example:
        expectedVersion: 7
        changedFields:
        - status
        - priority
        status: PENDING
        priority: HIGH
        comment:
          visibility: PUBLIC
          body: 결제 대행사에 승인 내역을 확인 중이며 결과를 안내드리겠습니다.
        clientCommandId: 77777777-7777-4777-8777-777777777777
    TransferTicketCommand:
      type: object
      additionalProperties: false
      required:
      - expectedVersion
      - groupId
      properties:
        expectedVersion:
          type: integer
          format: int64
        groupId:
          type: string
          format: uuid
        assigneeId:
          type:
          - string
          - 'null'
          format: uuid
        reason:
          type:
          - string
          - 'null'
          maxLength: 2000
        clientCommandId:
          type: string
          format: uuid
      description: 기존 티켓의 소유권을 대상 지원 그룹으로 이관할 때 사용하는 명령입니다. assigneeId를 함께 지정하면 대상 그룹의 활성 구성원이어야 하며, 자식 티켓을 만드는 작업과 다릅니다.
      x-deskseed-documentation-review: MANUAL
      example:
        expectedVersion: 7
        groupId: 11111111-1111-4111-8111-111111111111
        assigneeId: 22222222-2222-4222-8222-222222222222
        reason: 결제 승인 확인 권한이 있는 지원 그룹으로 이관
        clientCommandId: 77777777-7777-4777-8777-777777777777
    CreateChildTicketCommand:
      type: object
      additionalProperties: false
      required:
      - expectedVersion
      - subject
      - body
      - groupId
      - priority
      properties:
        expectedVersion:
          type: integer
          format: int64
        subject:
          type: string
          minLength: 1
          maxLength: 200
        body:
          type: string
          minLength: 1
          maxLength: 20000
        groupId:
          type: string
          format: uuid
        assigneeId:
          type:
          - string
          - 'null'
          format: uuid
        priority:
          $ref: '#/components/schemas/TicketPriority'
        clientCommandId:
          type: string
          format: uuid
      description: 현재 티켓의 소유권을 바꾸지 않고 내부 협업용 자식 티켓을 만드는 명령입니다. body는 자식 티켓의 첫 INTERNAL comment로 저장됩니다.
      x-deskseed-documentation-review: MANUAL
      example:
        expectedVersion: 7
        subject: ORD-2026-1042 결제 승인 중복 조사
        body: 결제 대행사 승인 로그와 주문 상태를 대조해 주세요.
        groupId: 11111111-1111-4111-8111-111111111111
        assigneeId: 22222222-2222-4222-8222-222222222222
        priority: HIGH
        clientCommandId: 77777777-7777-4777-8777-777777777777
    CommandWarning:
      type: object
      additionalProperties: false
      required:
      - code
      - message
      - count
      - relatedTicketNumbers
      properties:
        code:
          type: string
        message:
          type: string
        count:
          type: integer
          minimum: 1
        relatedTicketNumbers:
          type: array
          uniqueItems: true
          items:
            type: integer
            format: int64
    TicketCommandResult:
      type: object
      required:
      - ticketNumber
      - version
      - auditId
      - warnings
      properties:
        ticketNumber:
          type: integer
          format: int64
          description: 사람이 식별하고 검색할 때 사용하는 티켓 번호입니다.
        version:
          type: integer
          format: int64
        auditId:
          type: string
          format: uuid
        warnings:
          type: array
          items:
            $ref: '#/components/schemas/CommandWarning'
        ticket:
          $ref: '#/components/schemas/TicketSummary'
    CreateAgentTicket:
      type: object
      additionalProperties: false
      required:
      - requester
      - subject
      - firstComment
      - priority
      properties:
        requester:
          type: object
          additionalProperties: false
          properties:
            customerId:
              type: string
              format: uuid
            name:
              type: string
              minLength: 1
              maxLength: 100
            email:
              type: string
              format: email
              maxLength: 254
          description: customerId만 단독으로 지정해 검색으로 찾은 기존 고객을 요청자로 재사용하거나, name과 email을 함께 지정해 새 unverified
            고객을 생성합니다. 세 필드를 함께 보내거나 아무 필드도 보내지 않으면 검증 오류입니다.
        subject:
          type: string
          minLength: 1
          maxLength: 200
        firstComment:
          $ref: '#/components/schemas/CommentDraft'
        priority:
          $ref: '#/components/schemas/TicketPriority'
        groupId:
          type:
          - string
          - 'null'
          format: uuid
        assigneeId:
          type:
          - string
          - 'null'
          format: uuid
        clientCommandId:
          type: string
          format: uuid
      description: 상담사가 고객 문의를 직접 등록할 때 사용하는 요청입니다. firstComment의 visibility가 첫 댓글의 공개 범위를 결정하고, groupId와 assigneeId는 배정 권한과
        활성 구성원 조건을 만족해야 합니다.
      x-deskseed-documentation-review: MANUAL
      example:
        requester:
          name: 김민아
          email: mina.kim@example.test
        subject: 주문 ORD-2026-1042의 중복 결제 확인 요청
        firstComment:
          visibility: PUBLIC
          body: 같은 주문에 결제가 두 번 승인되었습니다. 결제 내역 확인 부탁드립니다.
        priority: HIGH
        groupId: 11111111-1111-4111-8111-111111111111
        assigneeId: 22222222-2222-4222-8222-222222222222
        clientCommandId: 77777777-7777-4777-8777-777777777777
    CreateChildTicketResult:
      type: object
      required:
      - parentTicketNumber
      - parentVersion
      - childTicketNumber
      - parentAuditId
      - childAuditId
      properties:
        parentTicketNumber:
          type: integer
          format: int64
        parentVersion:
          type: integer
          format: int64
        childTicketNumber:
          type: integer
          format: int64
        parentAuditId:
          type: string
          format: uuid
        childAuditId:
          type: string
          format: uuid
    TicketAuditEvent:
      type: object
      required:
      - sequence
      - type
      properties:
        sequence:
          type: integer
        type:
          type: string
        field:
          type:
          - string
          - 'null'
        before: {}
        after: {}
        metadata:
          type: object
          additionalProperties: true
    TicketAudit:
      type: object
      required:
      - id
      - ticketNumber
      - actor
      - source
      - createdAt
      - requestId
      - correlationId
      - commandId
      - expectedVersion
      - resultVersion
      - events
      properties:
        id:
          type: string
          format: uuid
        ticketNumber:
          type: integer
          format: int64
          description: 사람이 식별하고 검색할 때 사용하는 티켓 번호입니다.
        actor:
          $ref: '#/components/schemas/ActorSummary'
        source:
          type: string
        createdAt:
          type: string
          format: date-time
        correlationId:
          type: string
        requestId:
          type: string
        commandId:
          type: string
        expectedVersion:
          type: integer
          format: int64
        resultVersion:
          type: integer
          format: int64
        events:
          type: array
          items:
            $ref: '#/components/schemas/TicketAuditEvent'
    TicketAuditPage:
      type: object
      required:
      - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/TicketAudit'
        nextCursor:
          type:
          - string
          - 'null'
    AgentTicketBatchCommand:
      type: object
      additionalProperties: false
      required:
      - items
      properties:
        items:
          type: array
          minItems: 1
          maxItems: 100
          items:
            $ref: '#/components/schemas/AgentTicketBatchItem'
      description: 목록은 명시 선택 ticketNumber만 포함하며 동일 ticketNumber를 두 번 포함할 수 없습니다. item command는 comment body/visibility를 갖지 않아 bulk PUBLIC/INTERNAL comment를 만들 수 없습니다.
      x-deskseed-documentation-review: MANUAL
      example:
        items:
        - ticketNumber: 1042
          expectedVersion: 7
          clientCommandId: bulk-20260816-1042-priority
          command:
            type: UPDATE
            changedFields:
            - priority
            priority: HIGH
        - ticketNumber: 1043
          expectedVersion: 2
          clientCommandId: bulk-20260816-1043-transfer
          command:
            type: TRANSFER
            groupId: 11111111-1111-4111-8111-111111111111
            assigneeId: 22222222-2222-4222-8222-222222222222
            reason: 배송 확인 전담 그룹으로 이관
    AgentTicketBatchItem:
      type: object
      additionalProperties: false
      required:
      - ticketNumber
      - expectedVersion
      - clientCommandId
      - command
      properties:
        ticketNumber:
          type: integer
          format: int64
          minimum: 1
        expectedVersion:
          type: integer
          format: int64
          minimum: 0
        clientCommandId:
          type: string
          minLength: 1
          maxLength: 100
          pattern: ^[A-Za-z0-9._:-]+$
        command:
          oneOf:
          - $ref: '#/components/schemas/BatchTicketFieldUpdate'
          - $ref: '#/components/schemas/BatchTicketTransfer'
    BatchTicketFieldUpdate:
      type: object
      additionalProperties: false
      required:
      - type
      - changedFields
      properties:
        type:
          type: string
          const: UPDATE
        changedFields:
          type: array
          minItems: 1
          maxItems: 3
          uniqueItems: true
          items:
            type: string
            enum:
            - status
            - priority
            - assigneeId
        status:
          $ref: '#/components/schemas/AgentTicketStatus'
        priority:
          $ref: '#/components/schemas/TicketPriority'
        assigneeId:
          oneOf:
          - type: string
            format: uuid
          - type: 'null'
      description: changedFields와 값 property는 정확히 대응해야 합니다. groupId는 이 type에 없으며 group 변경은 reason을 가진 TRANSFER로만 가능합니다.
    BatchTicketTransfer:
      type: object
      additionalProperties: false
      required:
      - type
      - groupId
      - reason
      properties:
        type:
          type: string
          const: TRANSFER
        groupId:
          type: string
          format: uuid
        assigneeId:
          oneOf:
          - type: string
            format: uuid
          - type: 'null'
        reason:
          type: string
          minLength: 1
          maxLength: 1000
          pattern: ^[^\\x00-\\x1F\\x7F]*$
    AgentTicketBatchResult:
      type: object
      additionalProperties: false
      required:
      - correlationId
      - results
      properties:
        correlationId:
          type: string
          minLength: 1
          maxLength: 100
        results:
          type: array
          minItems: 1
          maxItems: 100
          items:
            $ref: '#/components/schemas/AgentTicketBatchItemResult'
    AgentTicketBatchItemResult:
      type: object
      additionalProperties: false
      required:
      - ticketNumber
      - clientCommandId
      - outcome
      - replayed
      properties:
        ticketNumber:
          type: integer
          format: int64
          minimum: 1
        clientCommandId:
          type: string
        outcome:
          type: string
          enum:
          - SUCCEEDED
          - CONFLICT
          - DENIED
          - NOT_FOUND
          - VALIDATION_FAILED
        replayed:
          type: boolean
        resultVersion:
          type:
          - integer
          - 'null'
          format: int64
        auditId:
          type:
          - string
          - 'null'
          format: uuid
        code:
          type:
          - string
          - 'null'
          enum:
          - TICKET_FIELD_CONFLICT
          - VERSION_PRECONDITION_FAILED
          - CLIENT_COMMAND_ID_REUSED
          - TICKET_WRITE_FORBIDDEN
          - TICKET_NOT_FOUND
          - VALIDATION_FAILED
          - 'null'
    AgentTicketSearchFilters:
      type: object
      additionalProperties: false
      properties:
        status:
          type: string
          enum:
          - NEW
          - OPEN
          - PENDING
          - ON_HOLD
          - SOLVED
          - CLOSED
        priority:
          type: string
          enum:
          - LOW
          - NORMAL
          - HIGH
          - URGENT
        groupId:
          type: string
          format: uuid
        assigneeId:
          oneOf:
          - type: string
            format: uuid
          - type: string
            enum:
            - me
            - unassigned
        slaState:
          $ref: '#/components/schemas/FirstReplySlaDisplayState'
    AgentTicketSearchRequest:
      type: object
      additionalProperties: false
      required:
      - query
      - filters
      - sort
      - limit
      properties:
        query:
          type: string
          minLength: 1
          maxLength: 500
        filters:
          $ref: '#/components/schemas/AgentTicketSearchFilters'
        sort:
          type: string
          enum:
          - updatedAt:desc,ticketNumber:desc
          - score:desc,ticketNumber:desc
          default: score:desc,ticketNumber:desc
        cursor:
          type:
          - string
          - 'null'
          minLength: 1
          maxLength: 2048
          description: raw query 대신 normalized query/filter/sort/snapshot fingerprint에 서명 결합된 opaque cursor입니다.
        limit:
          type: integer
          minimum: 1
          maximum: 100
          default: 25
      description: 상담사가 현재 권한으로 읽을 수 있는 티켓을 검색할 때 사용하는 요청입니다. query는 검색 감사에서 보호된 형태로 처리되고 filters는 결과 범위를 추가로 제한합니다.
      x-deskseed-documentation-review: MANUAL
      example:
        query: 중복 결제
        filters:
          status: OPEN
          priority: HIGH
          groupId: 11111111-1111-4111-8111-111111111111
          assigneeId: 22222222-2222-4222-8222-222222222222
          slaState: AT_RISK
        sort: score:desc,ticketNumber:desc
        limit: 25
    AgentTicketSearchPage:
      type: object
      additionalProperties: false
      required:
      - searchEventId
      - searchInteractionId
      - items
      - resultCount
      - sort
      - nextCursor
      properties:
        searchEventId:
          type: string
          format: uuid
        searchInteractionId:
          type: string
          format: uuid
        items:
          type: array
          items:
            $ref: '#/components/schemas/TicketSummary'
        resultCount:
          type: integer
          format: int64
          minimum: 0
          description: cursor snapshot과 filters에 일치하는 전체 exact row 수입니다. 현재 페이지 길이와 다를 수 있습니다.
        sort:
          type: string
          enum:
          - updatedAt:desc,ticketNumber:desc
          - score:desc,ticketNumber:desc
        nextCursor:
          type:
          - string
          - 'null'
          maxLength: 2048
      description: protected staff search 결과입니다. `score:desc,ticketNumber:desc`는 match score와 ticketNumber를 tie-breaker로 쓰며 cursor가 그 tuple과 snapshot을 포함합니다. raw query는 응답이나 cursor에 존재하지 않습니다.
      x-deskseed-documentation-review: MANUAL
    AgentCustomerSearchRequest:
      type: object
      additionalProperties: false
      required:
      - query
      - limit
      properties:
        query:
          type: string
          minLength: 1
          maxLength: 200
        limit:
          type: integer
          minimum: 1
          maximum: 25
          default: 10
      description: 상담사가 신규 티켓 생성 화면에서 요청자 후보를 찾기 위해 기존 고객의 이름 또는 이메일 일부 문자열로 검색할 때 사용하는 요청입니다. query는 검색 감사에서 보호된
        형태로 처리됩니다.
      x-deskseed-documentation-review: MANUAL
      example:
        query: 김민아
        limit: 10
    AgentCustomerSearchPage:
      type: object
      additionalProperties: false
      required:
      - searchEventId
      - searchInteractionId
      - items
      - resultCount
      properties:
        searchEventId:
          type: string
          format: uuid
        searchInteractionId:
          type: string
          format: uuid
        items:
          type: array
          items:
            $ref: '#/components/schemas/CustomerSummary'
        resultCount:
          type: integer
          format: int64
          minimum: 0
          description: query와 일치하는 고객의 전체 수입니다. items는 limit로 잘린 페이지이므로 resultCount가 items.length보다 클 수 있습니다.
      description: 고객 검색 결과 페이지입니다. searchEventId는 이 조회에 대한 검색 감사 이벤트를 가리키며, 결과 자체가 보호된 직원용 projection이므로 캐시하지 않습니다.
      x-deskseed-documentation-review: MANUAL
      example:
        searchEventId: 33333333-3333-4333-8333-333333333333
        searchInteractionId: 44444444-4444-4444-8444-444444444444
        items:
        - id: 22222222-2222-4222-8222-222222222222
          name: 김민아
          email: mina.kim@example.test
          verified: false
        resultCount: 1
    CustomerSummary:
      type: object
      additionalProperties: false
      required:
      - id
      - name
      - email
      - verified
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        email:
          type: string
          format: email
        verified:
          type: boolean
      description: 상담사가 요청자 후보를 구분할 수 있는 최소한의 고객 요약 정보입니다. 내부 전용 필드나 감사 메타데이터는 포함하지 않습니다.
      x-deskseed-documentation-review: MANUAL
      example:
        id: 22222222-2222-4222-8222-222222222222
        name: 김민아
        email: mina.kim@example.test
        verified: false
    StaffAccount:
      type: object
      additionalProperties: false
      required:
      - id
      - email
      - displayName
      - role
      - status
      - memberships
      - auditAuthorities
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
          format: email
        displayName:
          type: string
        role:
          $ref: '#/components/schemas/StaffRole'
        status:
          $ref: '#/components/schemas/StaffStatus'
        memberships:
          type: array
          items:
            $ref: '#/components/schemas/GroupRef'
        auditAuthorities:
          type: array
          uniqueItems: true
          description: 명시적으로 영속화한 고위험 감사 권한이며 비감사자와 새 감사자에게는 비어 있습니다.
          items:
            $ref: '#/components/schemas/GrantableAuditAuthority'
        lastLoginAt:
          type:
          - string
          - 'null'
          format: date-time
    ExternalSystemStatus:
      type: string
      enum:
      - ACTIVE
      - DISABLED
    ExternalSystem:
      type: object
      additionalProperties: false
      required:
      - id
      - systemKey
      - displayName
      - status
      - allowedHostnames
      - createdAt
      - updatedAt
      - version
      properties:
        id:
          type: string
          format: uuid
        systemKey:
          type: string
          minLength: 1
          maxLength: 64
          pattern: ^[a-z]([a-z0-9-]{0,62}[a-z0-9])?$
        displayName:
          type: string
          minLength: 1
          maxLength: 100
        status:
          $ref: '#/components/schemas/ExternalSystemStatus'
        allowedHostnames:
          type: array
          minItems: 1
          maxItems: 20
          uniqueItems: true
          description: 정확한 정규 public DNS 호스트명이며 wildcard, localhost, IP literal은 허용하지 않습니다.
          items:
            type: string
            minLength: 1
            maxLength: 253
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        version:
          type: integer
          format: int64
          minimum: 0
    CreateExternalSystem:
      type: object
      additionalProperties: false
      required:
      - systemKey
      - displayName
      - allowedHostnames
      properties:
        systemKey:
          type: string
          minLength: 1
          maxLength: 64
          pattern: ^[a-z]([a-z0-9-]{0,62}[a-z0-9])?$
        displayName:
          type: string
          minLength: 1
          maxLength: 100
        allowedHostnames:
          type: array
          minItems: 1
          maxItems: 20
          uniqueItems: true
          items:
            type: string
            minLength: 1
            maxLength: 253
      description: 외부 참조에서 사용할 시스템과 deep link 허용 host 목록을 등록할 때 사용하는 요청입니다. 허용 host는 public DNS의 정확한 이름만 등록합니다.
      x-deskseed-documentation-review: MANUAL
      example:
        systemKey: order-admin
        displayName: 주문 운영 시스템
        allowedHostnames:
        - orders.example.test
    UpdateExternalSystem:
      type: object
      additionalProperties: false
      required:
      - displayName
      - status
      - allowedHostnames
      - expectedVersion
      properties:
        displayName:
          type: string
          minLength: 1
          maxLength: 100
        status:
          $ref: '#/components/schemas/ExternalSystemStatus'
        allowedHostnames:
          type: array
          minItems: 1
          maxItems: 20
          uniqueItems: true
          items:
            type: string
            minLength: 1
            maxLength: 253
        expectedVersion:
          type: integer
          format: int64
          minimum: 0
      description: 등록된 외부 시스템의 표시명, 활성 상태, deep link 허용 host를 바꿀 때 사용하는 요청입니다. expectedVersion은 현재 설정 버전과 일치해야 합니다.
      x-deskseed-documentation-review: MANUAL
      example:
        displayName: 주문 운영 시스템
        status: ACTIVE
        allowedHostnames:
        - orders.example.test
        expectedVersion: 4
    ExternalObjectType:
      type: string
      enum:
      - ORDER
      - PAYMENT
      - REFUND
      - USER
      - STORE
      - OPS_CASE
      - CUSTOM
    ExternalReferenceLinkState:
      type: string
      enum:
      - AVAILABLE
      - SYSTEM_DISABLED
      - HOST_NOT_ALLOWED
    ExternalReferenceMetadataValue:
      oneOf:
      - type: string
        maxLength: 200
      - type: number
      - type: boolean
    ExternalReferenceMetadataSnapshot:
      type: object
      additionalProperties: false
      maxProperties: 8
      properties:
        status:
          $ref: '#/components/schemas/ExternalReferenceMetadataValue'
        storeName:
          $ref: '#/components/schemas/ExternalReferenceMetadataValue'
        amountDisplay:
          $ref: '#/components/schemas/ExternalReferenceMetadataValue'
        currency:
          $ref: '#/components/schemas/ExternalReferenceMetadataValue'
        occurredAt:
          $ref: '#/components/schemas/ExternalReferenceMetadataValue'
        ownerLabel:
          $ref: '#/components/schemas/ExternalReferenceMetadataValue'
        channel:
          $ref: '#/components/schemas/ExternalReferenceMetadataValue'
    ExternalReferenceActor:
      type: object
      additionalProperties: false
      required:
      - actorId
      - displayName
      properties:
        actorId:
          type: string
          format: uuid
        displayName:
          type: string
          minLength: 1
          maxLength: 100
    ExternalReference:
      type: object
      additionalProperties: false
      required:
      - id
      - system
      - objectType
      - externalId
      - displayLabel
      - linkState
      - metadata
      - metadataObservedAt
      - createdBy
      - createdAt
      properties:
        id:
          type: string
          format: uuid
        system:
          $ref: '#/components/schemas/ExternalSystem'
        objectType:
          $ref: '#/components/schemas/ExternalObjectType'
        externalId:
          type: string
          minLength: 1
          maxLength: 200
        displayLabel:
          type: string
          minLength: 1
          maxLength: 200
        linkState:
          $ref: '#/components/schemas/ExternalReferenceLinkState'
        safeDeepLink:
          type:
          - string
          - 'null'
          format: uri
          maxLength: 2048
          description: 시스템이 활성 상태이고 저장된 호스트가 계속 허용되는 동안에만 값이 존재합니다.
        metadata:
          $ref: '#/components/schemas/ExternalReferenceMetadataSnapshot'
        metadataObservedAt:
          type: string
          format: date-time
        createdBy:
          $ref: '#/components/schemas/ExternalReferenceActor'
        createdAt:
          type: string
          format: date-time
    ExternalReferenceContext:
      type: object
      additionalProperties: false
      required:
      - ticketVersion
      - canManage
      - availableSystems
      - items
      properties:
        ticketVersion:
          type: integer
          format: int64
          minimum: 0
        canManage:
          type: boolean
        availableSystems:
          type: array
          maxItems: 100
          items:
            $ref: '#/components/schemas/ExternalSystem'
        items:
          type: array
          maxItems: 100
          items:
            $ref: '#/components/schemas/ExternalReference'
    CreateExternalReferenceCommand:
      type: object
      additionalProperties: false
      required:
      - externalSystemId
      - objectType
      - externalId
      - displayLabel
      - safeDeepLink
      - metadata
      - metadataObservedAt
      - expectedVersion
      properties:
        externalSystemId:
          type: string
          format: uuid
        objectType:
          $ref: '#/components/schemas/ExternalObjectType'
        externalId:
          type: string
          minLength: 1
          maxLength: 200
        displayLabel:
          type: string
          minLength: 1
          maxLength: 200
        safeDeepLink:
          type: string
          format: uri
          minLength: 9
          maxLength: 2048
          description: 호스트가 정확히 일치하는 HTTPS URL이며 userinfo와 자격 증명 형태의 query key는 금지합니다.
        metadata:
          $ref: '#/components/schemas/ExternalReferenceMetadataSnapshot'
        metadataObservedAt:
          type: string
          format: date-time
        expectedVersion:
          type: integer
          format: int64
          minimum: 0
      description: 상담 티켓에 외부 시스템 객체를 연결하는 명령입니다. safeDeepLink는 등록된 host와 정확히 일치하는 HTTPS 주소여야 하고 metadata는 허용된 요약 필드만 스냅샷으로
        저장합니다.
      x-deskseed-documentation-review: MANUAL
      example:
        externalSystemId: 44444444-4444-4444-8444-444444444444
        objectType: ORDER
        externalId: ORD-2026-1042
        displayLabel: 주문 ORD-2026-1042
        safeDeepLink: https://orders.example.test/orders/ORD-2026-1042
        metadata:
          status: PAID
          storeName: Deskseed Mall
          amountDisplay: ₩32,000
          currency: KRW
          occurredAt: '2026-08-10T09:00:00Z'
          ownerLabel: 김민아
          channel: WEB
        metadataObservedAt: '2026-08-10T09:05:00Z'
        expectedVersion: 7
    ExternalReferenceCommandResult:
      type: object
      additionalProperties: false
      required:
      - ticketVersion
      - reference
      properties:
        ticketVersion:
          type: integer
          format: int64
          minimum: 0
        reference:
          $ref: '#/components/schemas/ExternalReference'
    DeleteExternalReferenceResult:
      type: object
      additionalProperties: false
      required:
      - ticketVersion
      - removedReferenceId
      properties:
        ticketVersion:
          type: integer
          format: int64
          minimum: 0
        removedReferenceId:
          type: string
          format: uuid
    ExternalSystemVersionPreconditionProblem:
      allOf:
      - $ref: '#/components/schemas/Problem'
      - type: object
        additionalProperties: true
        required:
        - currentVersion
        properties:
          currentVersion:
            type: integer
            format: int64
            minimum: 0
    IntegrationScope:
      type: string
      enum:
      - tickets:create
      - tickets:read
      - tickets:update
      - tickets:comment:internal
    IntegrationClientStatus:
      type: string
      enum:
      - ACTIVE
      - DISABLED
      - REVOKED
    IntegrationCredentialStatus:
      type: string
      enum:
      - ACTIVE
      - RETIRING
      - EXPIRED
      - REVOKED
    IntegrationResourceConstraints:
      type: object
      additionalProperties: false
      properties:
        allowedGroupIds:
          type: array
          maxItems: 100
          uniqueItems: true
          items:
            type: string
            format: uuid
        allowedTicketKinds:
          type: array
          maxItems: 2
          uniqueItems: true
          items:
            type: string
            enum:
            - CUSTOMER_REQUEST
            - INTERNAL_TASK
        allowedFields:
          type: array
          maxItems: 4
          uniqueItems: true
          items:
            type: string
            enum:
            - status
            - priority
            - groupId
            - assigneeId
        ipAllowlist:
          type: array
          maxItems: 32
          uniqueItems: true
          description: 선택적인 literal IP 또는 CIDR 허용 목록입니다. 값이 없으면 이 클라이언트 경계에서는 네트워크 제한을 적용하지 않습니다.
          items:
            type: string
            minLength: 2
            maxLength: 64
    IntegrationCredentialMetadata:
      type: object
      additionalProperties: false
      required:
      - id
      - sequence
      - publicKeyId
      - status
      - expiresAt
      - createdAt
      properties:
        id:
          type: string
          format: uuid
        sequence:
          type: integer
          minimum: 1
        publicKeyId:
          type: string
          minLength: 16
          maxLength: 32
        status:
          $ref: '#/components/schemas/IntegrationCredentialStatus'
        expiresAt:
          type: string
          format: date-time
        overlapExpiresAt:
          type:
          - string
          - 'null'
          format: date-time
        createdAt:
          type: string
          format: date-time
        revokedAt:
          type:
          - string
          - 'null'
          format: date-time
        lastUsedAt:
          type:
          - string
          - 'null'
          format: date-time
        lastUsedIp:
          type:
          - string
          - 'null'
    IntegrationClient:
      type: object
      additionalProperties: false
      required:
      - id
      - name
      - description
      - status
      - scopes
      - resourceConstraints
      - credentials
      - createdAt
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          minLength: 1
          maxLength: 100
        description:
          type: string
          maxLength: 500
        status:
          $ref: '#/components/schemas/IntegrationClientStatus'
        scopes:
          type: array
          minItems: 1
          uniqueItems: true
          items:
            $ref: '#/components/schemas/IntegrationScope'
        resourceConstraints:
          $ref: '#/components/schemas/IntegrationResourceConstraints'
        credentials:
          type: array
          items:
            $ref: '#/components/schemas/IntegrationCredentialMetadata'
        expiresAt:
          type:
          - string
          - 'null'
          format: date-time
          description: 목록 표시에 사용하는 현재 유효 자격 증명의 가장 늦은 만료 시각입니다.
        lastUsedAt:
          type:
          - string
          - 'null'
          format: date-time
        lastUsedIp:
          type:
          - string
          - 'null'
        createdAt:
          type: string
          format: date-time
    CreateIntegrationClient:
      type: object
      additionalProperties: false
      required:
      - name
      - description
      - scopes
      - resourceConstraints
      - expiresAt
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 100
        description:
          type: string
          maxLength: 500
        scopes:
          type: array
          minItems: 1
          maxItems: 4
          uniqueItems: true
          items:
            $ref: '#/components/schemas/IntegrationScope'
        resourceConstraints:
          $ref: '#/components/schemas/IntegrationResourceConstraints'
        expiresAt:
          type: string
          format: date-time
      description: 관리자가 기계 연동 클라이언트를 만들 때 사용하는 요청입니다. scopes와 resourceConstraints를 함께 적용해 접근 가능한 티켓과 변경 필드를 제한하며, API key
        원문은 이 요청이 아닌 생성 응답에서 한 번만 표시됩니다.
      x-deskseed-documentation-review: MANUAL
      example:
        name: order-operations
        description: 주문 상태를 조회하고 지정된 결제 지원 그룹의 티켓을 생성하는 사내 주문 운영 연동
        scopes:
        - tickets:create
        resourceConstraints:
          allowedGroupIds:
          - 11111111-1111-4111-8111-111111111111
          allowedTicketKinds:
          - CUSTOMER_REQUEST
          allowedFields:
          - status
          ipAllowlist:
          - 203.0.113.0/24
        expiresAt: '2027-08-13T00:00:00Z'
    RotateIntegrationCredential:
      type: object
      additionalProperties: false
      required:
      - expiresAt
      - overlapSeconds
      properties:
        expiresAt:
          type: string
          format: date-time
        overlapSeconds:
          type: integer
          minimum: 0
          maximum: 86400
      description: 연동 클라이언트의 API 자격 증명을 교체할 때 사용하는 요청입니다. overlapSeconds 동안 이전 자격 증명과 새 자격 증명을 함께 허용한 뒤 expiresAt에 이전 값을 만료시킵니다.
      x-deskseed-documentation-review: MANUAL
      example:
        expiresAt: '2027-08-13T00:00:00Z'
        overlapSeconds: 3600
    IntegrationCredentialIssue:
      type: object
      additionalProperties: false
      required:
      - client
      - credential
      - apiKey
      properties:
        client:
          $ref: '#/components/schemas/IntegrationClient'
        credential:
          $ref: '#/components/schemas/IntegrationCredentialMetadata'
        apiKey:
          type: string
          pattern: ^dsk_live_[A-Za-z0-9_-]{16,32}\.[A-Za-z0-9_-]{43}$
          readOnly: true
          x-deskseed-one-time-display: true
    CreateStaffAccount:
      type: object
      additionalProperties: false
      required:
      - email
      - displayName
      - role
      - password
      properties:
        email:
          type: string
          format: email
          maxLength: 254
        displayName:
          type: string
          minLength: 1
          maxLength: 100
        role:
          $ref: '#/components/schemas/StaffRole'
        password:
          type: string
          format: password
          minLength: 12
          maxLength: 128
          writeOnly: true
      description: 관리자가 새 직원 계정을 만들 때 사용하는 요청입니다. 초기 password는 전송 직후 안전하게 처리되며 문서에는 사용할 수 없는 합성 값만 둡니다.
      x-deskseed-documentation-review: MANUAL
      example:
        email: agent.park@example.test
        displayName: 박상담
        role: AGENT
        password: not-a-real-password
    StaffRole:
      type: string
      enum:
      - ADMIN
      - AGENT
      - SECURITY_AUDITOR
    StaffStatus:
      type: string
      enum:
      - ACTIVE
      - DISABLED
    GrantableAuditAuthority:
      type: string
      enum:
      - AUDIT_SEARCH_QUERY_REVEAL
      - AUDIT_EXPORT
      - AUDIT_PROJECTION_REBUILD
    Group:
      type: object
      additionalProperties: false
      required:
      - id
      - name
      - status
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        status:
          type: string
          enum:
          - ACTIVE
          - DISABLED
        memberCount:
          type: integer
          minimum: 0
    CreateGroup:
      type: object
      additionalProperties: false
      required:
      - name
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 100
      description: 관리자가 상담 티켓을 배정할 수 있는 새 지원 그룹을 만들 때 사용하는 요청입니다. 구성원은 별도 그룹 구성원 명령으로 추가합니다.
      x-deskseed-documentation-review: MANUAL
      example:
        name: 결제 지원팀
    UpdateGroup:
      type: object
      additionalProperties: false
      required:
      - name
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 100
      description: 지원 그룹의 표시 이름만 변경할 때 사용하는 요청입니다. 이 작업은 기존 구성원 관계나 티켓 소유권을 변경하지 않습니다.
      x-deskseed-documentation-review: MANUAL
      example:
        name: 결제·정산 지원팀
    CreateGroupMembership:
      type: object
      additionalProperties: false
      required:
      - staffId
      properties:
        staffId:
          type: string
          format: uuid
      description: 기존 지원 그룹에 활성 직원 계정을 구성원으로 추가할 때 사용하는 요청입니다. 이 관계가 있어야 해당 그룹의 티켓을 해당 직원에게 배정할 수 있습니다.
      x-deskseed-documentation-review: MANUAL
      example:
        staffId: 33333333-3333-4333-8333-333333333333
    GroupMembership:
      type: object
      additionalProperties: false
      required:
      - groupId
      - staffId
      - staffDisplayName
      - role
      properties:
        groupId:
          type: string
          format: uuid
        staffId:
          type: string
          format: uuid
        staffDisplayName:
          type: string
        role:
          $ref: '#/components/schemas/StaffRole'
    AdminSetting:
      type: object
      required:
      - key
      - valueType
      - version
      - secret
      properties:
        key:
          type: string
        valueType:
          type: string
        value: {}
        secret:
          type: boolean
        version:
          type: integer
          format: int64
        effectiveAt:
          type: string
          format: date-time
    UpdateAdminSetting:
      type: object
      required:
      - value
      - reason
      properties:
        value: {}
        reason:
          type: string
          minLength: 1
          maxLength: 1000
    AuditLedgerType:
      type: string
      enum:
      - TICKET_CHANGE
      - ACCESS_SEARCH
      - ADMIN_SECURITY
    AuditOutcome:
      type: string
      enum:
      - SUCCEEDED
      - DENIED
      - FAILED
    AuditFieldChange:
      type: object
      additionalProperties: false
      required:
      - field
      - before
      - after
      properties:
        field:
          type: string
        before: {}
        after: {}
    AuditActivity:
      type: object
      additionalProperties: false
      required:
      - id
      - ledger
      - action
      - actor
      - occurredAt
      - outcome
      - source
      properties:
        id:
          type: string
          format: uuid
        ledger:
          $ref: '#/components/schemas/AuditLedgerType'
        action:
          type: string
        actor:
          $ref: '#/components/schemas/ActorSummary'
        occurredAt:
          type: string
          format: date-time
        ticketNumber:
          type:
          - integer
          - 'null'
          format: int64
          description: 사람이 식별하고 검색할 때 사용하는 티켓 번호입니다.
        groupId:
          type:
          - string
          - 'null'
          format: uuid
        field:
          type:
          - string
          - 'null'
        resourceType:
          type:
          - string
          - 'null'
        resourceId:
          type:
          - string
          - 'null'
        summary:
          type: string
        source:
          type: string
        outcome:
          $ref: '#/components/schemas/AuditOutcome'
        requestId:
          type:
          - string
          - 'null'
        correlationId:
          type:
          - string
          - 'null'
        protectedContentAvailable:
          type: boolean
        searchFingerprint:
          type:
          - string
          - 'null'
    AuditActivityPage:
      type: object
      required:
      - items
      - snapshotAt
      - projection
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/AuditActivity'
        nextCursor:
          type:
          - string
          - 'null'
        snapshotAt:
          type: string
          format: date-time
        projection:
          $ref: '#/components/schemas/AuditProjectionStatus'
    AuditProjectionStatus:
      type: object
      additionalProperties: false
      required:
      - state
      - projectedCount
      - lastRebuiltAt
      properties:
        state:
          type: string
          enum:
          - CURRENT
          - DEGRADED
          - REBUILDING
        projectedCount:
          type: integer
          format: int64
          minimum: 0
        lastRebuiltAt:
          type:
          - string
          - 'null'
          format: date-time
    AuditSearchContext:
      type: object
      additionalProperties: false
      required:
      - queryRedacted
      - queryFingerprint
      - filters
      - resultCount
      - openedActivityCount
      - openedActivitiesTruncated
      - openedActivities
      properties:
        queryRedacted:
          type: string
          description: '입력값과 무관한 `[PROTECTED]` 일상 조회 표식에 남아 있는 레거시 필드명입니다. 정확한 검색어는 별도 권한과 사유가 필요한 공개 작업에서만 확인할 수 있습니다.

            '
        queryFingerprint:
          type: string
        filters:
          type: object
          additionalProperties:
            type: string
        sort:
          type: string
        resultCount:
          type: integer
          format: int64
          minimum: 0
        originSearchActivityId:
          type:
          - string
          - 'null'
          format: uuid
        openedActivityCount:
          type: integer
          format: int64
          minimum: 0
        openedActivitiesTruncated:
          type: boolean
          description: 연결된 열람 중 처음 100건만 반환했으면 true입니다.
        openedActivities:
          type: array
          maxItems: 100
          items:
            type: object
            additionalProperties: false
            required:
            - activityId
            - ticketNumber
            - occurredAt
            properties:
              activityId:
                type: string
                format: uuid
              ticketNumber:
                type: integer
                format: int64
              occurredAt:
                type: string
                format: date-time
    AuditActivityDetail:
      allOf:
      - $ref: '#/components/schemas/AuditActivity'
      - type: object
        required:
        - canonicalEventId
        - metadata
        properties:
          canonicalEventId:
            type: string
            format: uuid
          canonicalParentId:
            type:
            - string
            - 'null'
            format: uuid
          fieldChange:
            oneOf:
            - $ref: '#/components/schemas/AuditFieldChange'
            - type: 'null'
          interactionId:
            type:
            - string
            - 'null'
            format: uuid
          sessionFingerprint:
            type:
            - string
            - 'null'
          authType:
            type:
            - string
            - 'null'
          ipAddress:
            type:
            - string
            - 'null'
          userAgent:
            type:
            - string
            - 'null'
          search:
            oneOf:
            - $ref: '#/components/schemas/AuditSearchContext'
            - type: 'null'
          metadata:
            type: object
            additionalProperties: true
    SearchQueryRevealRequest:
      type: object
      additionalProperties: false
      required:
      - reason
      properties:
        reason:
          type: string
          minLength: 1
          maxLength: 1000
      description: 보호된 검색어 원문을 공개할 권한이 있는 감사자가 사유를 남길 때 사용하는 요청입니다. 공개 행위도 별도 접근 감사로 기록됩니다.
      x-deskseed-documentation-review: MANUAL
      example:
        reason: 고객 데이터 노출 의심 조사에서 검색 조건을 재현해야 합니다.
    SearchQueryRevealResult:
      type: object
      additionalProperties: false
      required:
      - activityId
      - state
      properties:
        activityId:
          type: string
          format: uuid
        state:
          type: string
          enum:
          - AVAILABLE
          - RETENTION_EXPIRED
          - KEY_UNAVAILABLE
        rawQuery:
          type:
          - string
          - 'null'
        keyVersion:
          type:
          - string
          - 'null'
        revealedAt:
          type:
          - string
          - 'null'
          format: date-time
    CreateAuditExport:
      type: object
      additionalProperties: false
      required:
      - format
      - filters
      - fields
      - reason
      properties:
        format:
          type: string
          enum:
          - CSV
          - JSONL
        filters:
          $ref: '#/components/schemas/AuditExportFilters'
        fields:
          type: array
          minItems: 1
          maxItems: 20
          uniqueItems: true
          items:
            type: string
            enum:
            - occurredAt
            - ledger
            - action
            - actor
            - ticketNumber
            - groupId
            - field
            - source
            - outcome
            - requestId
            - correlationId
            - searchFingerprint
        reason:
          type: string
          minLength: 1
          maxLength: 1000
      description: 감사 권한자가 필터 조건에 맞는 감사 활동을 내보내기 요청할 때 사용하는 본문입니다. reason과 선택한 fields는 내보내기 접근 자체에 대한 감사 근거가 됩니다.
      x-deskseed-documentation-review: MANUAL
      example:
        format: CSV
        filters:
          from: '2026-08-01T00:00:00Z'
          to: '2026-08-31T23:59:59Z'
          ledger: TICKET_CHANGE
          ticketNumber: 1042
          outcome: SUCCEEDED
        fields:
        - occurredAt
        - action
        - ticketNumber
        - outcome
        reason: 주문 ORD-2026-1042의 처리 이력 확인을 위한 내부 감사
    AuditExportFilters:
      type: object
      additionalProperties: false
      properties:
        from:
          type: string
          format: date-time
        to:
          type: string
          format: date-time
        ledger:
          $ref: '#/components/schemas/AuditLedgerType'
        action:
          type: string
          maxLength: 80
        actorType:
          type: string
        actorId:
          type: string
          format: uuid
        ticketNumber:
          type: integer
          format: int64
          minimum: 1
          description: 사람이 식별하고 검색할 때 사용하는 티켓 번호입니다.
        groupId:
          type: string
          format: uuid
        field:
          type: string
          maxLength: 60
        source:
          type: string
        outcome:
          $ref: '#/components/schemas/AuditOutcome'
        requestId:
          type: string
          maxLength: 100
        correlationId:
          type: string
          maxLength: 100
        searchFingerprint:
          type: string
          maxLength: 100
    ExportJob:
      type: object
      additionalProperties: false
      required:
      - id
      - status
      - createdAt
      - format
      - fields
      - artifact
      properties:
        id:
          type: string
          format: uuid
        status:
          type: string
          enum:
          - REQUESTED
          - RUNNING
          - READY
          - FAILED
          - EXPIRED
        createdAt:
          type: string
          format: date-time
        format:
          type: string
          enum:
          - CSV
          - JSONL
        fields:
          type: array
          items:
            type: string
        artifact:
          $ref: '#/components/schemas/AuditExportArtifact'
      description: requester-bound audit export job입니다. status가 READY여도 다운로드 순간 owner와 현재 `audit:export` capability를 다시 확인하므로 job 생성 당시 permission snapshot만으로 bytes를 받을 수 없습니다.
    AuditExportArtifact:
      type: object
      additionalProperties: false
      required:
      - state
      - rowCount
      - sizeBytes
      - checksumSha256
      - expiresAt
      properties:
        state:
          type: string
          enum:
          - PENDING
          - READY
          - FAILED
          - EXPIRED
          - DELETED
        rowCount:
          type:
          - integer
          - 'null'
          format: int64
          minimum: 0
        sizeBytes:
          type:
          - integer
          - 'null'
          format: int64
          minimum: 0
        checksumSha256:
          type:
          - string
          - 'null'
          pattern: ^[0-9a-f]{64}$
        expiresAt:
          type:
          - string
          - 'null'
          format: date-time
        contentType:
          type:
          - string
          - 'null'
          enum:
          - text/csv
          - application/x-ndjson
          - 'null'
        failureCode:
          type:
          - string
          - 'null'
          enum:
          - GENERATION_FAILED
          - ARTIFACT_STORE_UNAVAILABLE
          - EXPIRED
          - 'null'
      description: private artifact의 allowlisted metadata입니다. object key, bucket, provider response, temporary URL, export row content는 반환하지 않습니다.
    AuditProjectionRebuildResult:
      type: object
      additionalProperties: false
      required:
      - ticketChangeCount
      - accessSearchCount
      - adminSecurityCount
      - totalCount
      - completedAt
      - projection
      properties:
        ticketChangeCount:
          type: integer
          format: int64
          minimum: 0
        accessSearchCount:
          type: integer
          format: int64
          minimum: 0
        adminSecurityCount:
          type: integer
          format: int64
          minimum: 0
        totalCount:
          type: integer
          format: int64
          minimum: 0
        completedAt:
          type: string
          format: date-time
        projection:
          $ref: '#/components/schemas/AuditProjectionStatus'
    BusinessInterval:
      type: object
      additionalProperties: false
      required:
      - start
      - end
      properties:
        start:
          type: string
          pattern: ^([01]\d|2[0-3]):[0-5]\d$
        end:
          type: string
          pattern: ^([01]\d|2[0-3]):[0-5]\d$
    WeekdaySchedule:
      type: object
      additionalProperties: false
      required:
      - weekday
      - enabled
      - intervals
      properties:
        weekday:
          type: string
          enum:
          - MONDAY
          - TUESDAY
          - WEDNESDAY
          - THURSDAY
          - FRIDAY
          - SATURDAY
          - SUNDAY
        enabled:
          type: boolean
        intervals:
          type: array
          maxItems: 12
          items:
            $ref: '#/components/schemas/BusinessInterval'
    ScheduleException:
      type: object
      additionalProperties: false
      required:
      - date
      - mode
      properties:
        date:
          type: string
          format: date
        mode:
          type: string
          enum:
          - CLOSED
          - OPEN
        intervals:
          type: array
          maxItems: 12
          items:
            $ref: '#/components/schemas/BusinessInterval'
        label:
          type:
          - string
          - 'null'
          maxLength: 200
    BusinessScheduleInput:
      type: object
      additionalProperties: false
      required:
      - name
      - timeZone
      - weekdays
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 100
        timeZone:
          type: string
          minLength: 1
          maxLength: 100
        weekdays:
          type: array
          minItems: 7
          maxItems: 7
          items:
            $ref: '#/components/schemas/WeekdaySchedule'
        exceptions:
          type: array
          maxItems: 366
          items:
            $ref: '#/components/schemas/ScheduleException'
      description: 관리자가 영업 시간표의 새 버전을 만들거나 미리보기할 때 사용하는 일정 정의입니다. weekdays와 exceptions는 지정한 IANA timeZone을 기준으로 SLA 영업 시간을
        계산합니다.
      x-deskseed-documentation-review: MANUAL
      example: &id015
        name: 대한민국 고객지원 기본 운영시간
        timeZone: Asia/Seoul
        weekdays:
        - weekday: MONDAY
          enabled: true
          intervals:
          - start: 09:00
            end: '18:00'
        - weekday: TUESDAY
          enabled: true
          intervals:
          - start: 09:00
            end: '18:00'
        - weekday: WEDNESDAY
          enabled: true
          intervals:
          - start: 09:00
            end: '18:00'
        - weekday: THURSDAY
          enabled: true
          intervals:
          - start: 09:00
            end: '18:00'
        - weekday: FRIDAY
          enabled: true
          intervals:
          - start: 09:00
            end: '18:00'
        exceptions:
        - date: '2026-10-03'
          mode: CLOSED
          label: 개천절
    BusinessSchedule:
      allOf:
      - $ref: '#/components/schemas/BusinessScheduleInput'
      - type: object
        required:
        - id
        - version
        - activeVersion
        - activeTimeZone
        - aggregateVersion
        - active
        - createdAt
        - createdBy
        properties:
          id:
            type: string
            format: uuid
          version:
            type: integer
            minimum: 1
          activeVersion:
            type:
            - integer
            - 'null'
            minimum: 1
            description: 반환된 버전과 별개인 현재 활성 불변 버전입니다.
          activeTimeZone:
            type:
            - string
            - 'null'
            description: 현재 활성화된 불변 일정 버전의 IANA timezone입니다.
          aggregateVersion:
            type: integer
            format: int64
            minimum: 0
          active:
            type: boolean
          createdAt:
            type: string
            format: date-time
          createdBy:
            type: object
            additionalProperties: false
            required:
            - actorType
            - displayName
            properties:
              actorType:
                type: string
                enum:
                - STAFF
                - SYSTEM
              actorId:
                type:
                - string
                - 'null'
                format: uuid
              displayName:
                type: string
    BusinessTimePreviewRequest:
      type: object
      additionalProperties: false
      required:
      - schedule
      - startAt
      - endAt
      - businessMinutes
      properties:
        schedule:
          $ref: '#/components/schemas/BusinessScheduleInput'
        startAt:
          type: string
          format: date-time
        endAt:
          type: string
          format: date-time
          description: startAt 이후 최대 366일 이내여야 합니다.
        businessMinutes:
          type: integer
          minimum: 0
          maximum: 525600
      description: 저장 전 영업 시간표 정의로 두 시각 사이의 영업 분을 계산해 보는 요청입니다. 이 작업은 입력 schedule을 저장하거나 활성화하지 않습니다.
      x-deskseed-documentation-review: MANUAL
      example:
        schedule: *id015
        startAt: '2026-08-10T00:00:00Z'
        endAt: '2026-08-10T09:00:00Z'
        businessMinutes: 480
    BusinessTimePreviewResult:
      type: object
      additionalProperties: false
      required:
      - dueAt
      - elapsedBusinessMinutes
      - nextOpenAt
      - nextCloseAt
      - dstPolicy
      properties:
        dueAt:
          type:
          - string
          - 'null'
          format: date-time
        elapsedBusinessMinutes:
          type: integer
          minimum: 0
        nextOpenAt:
          type:
          - string
          - 'null'
          format: date-time
        nextCloseAt:
          type:
          - string
          - 'null'
          format: date-time
        dstPolicy:
          type: string
          enum:
          - GAP_SHIFT_FORWARD_OVERLAP_INCLUDE_BOTH
    PriorityTargets:
      type: object
      additionalProperties: false
      properties:
        LOW:
          type:
          - integer
          - 'null'
          minimum: 1
        NORMAL:
          type:
          - integer
          - 'null'
          minimum: 1
        HIGH:
          type:
          - integer
          - 'null'
          minimum: 1
        URGENT:
          type:
          - integer
          - 'null'
          minimum: 1
          maximum: 525600
    FirstReplySlaPolicyConditions:
      type: object
      additionalProperties: false
      properties:
        groupId:
          type:
          - string
          - 'null'
          format: uuid
        channel:
          type:
          - string
          - 'null'
          enum:
          - WEB
          - AGENT
          - EMAIL
          - CHAT
          - API
          - null
    FirstReplySlaPolicyInput:
      type: object
      additionalProperties: false
      required:
      - name
      - position
      - scheduleId
      - conditions
      - targets
      - pauseStatuses
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 100
        position:
          type: integer
          minimum: 1
          maximum: 10000
        scheduleId:
          type: string
          format: uuid
        conditions:
          $ref: '#/components/schemas/FirstReplySlaPolicyConditions'
        targets:
          $ref: '#/components/schemas/PriorityTargets'
        pauseStatuses:
          type: array
          items:
            type: string
            enum:
            - NEW
            - OPEN
            - PENDING
            - ON_HOLD
          uniqueItems: true
          maxItems: 4
          default:
          - PENDING
      description: 관리자가 최초 PUBLIC 답변 목표를 계산할 SLA 정책 버전을 정의할 때 사용하는 요청입니다. scheduleId, 조건, 우선순위별 targets, pauseStatuses가 함께
        적용 순서를 결정합니다.
      x-deskseed-documentation-review: MANUAL
      example: &id016
        name: 결제 문의 기본 최초 답변 SLA
        position: 10
        scheduleId: 55555555-5555-4555-8555-555555555555
        conditions:
          groupId: 11111111-1111-4111-8111-111111111111
          channel: WEB
        targets:
          LOW: 480
          NORMAL: 240
          HIGH: 120
          URGENT: 60
        pauseStatuses:
        - PENDING
    FirstReplySlaPolicy:
      allOf:
      - $ref: '#/components/schemas/FirstReplySlaPolicyInput'
      - type: object
        required:
        - id
        - version
        - activeVersion
        - aggregateVersion
        - scheduleVersion
        - active
        - createdAt
        properties:
          id:
            type: string
            format: uuid
          version:
            type: integer
            minimum: 1
          activeVersion:
            type:
            - integer
            - 'null'
            minimum: 1
            description: 반환된 버전과 별개인 현재 활성 불변 정책 버전입니다.
          aggregateVersion:
            type: integer
            format: int64
            minimum: 0
          scheduleVersion:
            type: integer
            minimum: 1
          active:
            type: boolean
          createdAt:
            type: string
            format: date-time
    FirstReplySlaPreviewRequest:
      type: object
      additionalProperties: false
      required:
      - ticket
      - startAt
      properties:
        candidatePolicyId:
          type:
          - string
          - 'null'
          format: uuid
          description: 순서가 있는 미리보기 평가에서 후보 정책으로 대체할 기존 정책입니다.
        candidate:
          oneOf:
          - $ref: '#/components/schemas/FirstReplySlaPolicyInput'
          - type: 'null'
        ticket:
          type: object
          additionalProperties: false
          required:
          - priority
          - channel
          properties:
            priority:
              $ref: '#/components/schemas/TicketPriority'
            groupId:
              type:
              - string
              - 'null'
              format: uuid
            channel:
              type: string
              enum:
              - WEB
              - AGENT
              - EMAIL
              - CHAT
              - API
        startAt:
          type: string
          format: date-time
      description: 후보 최초 답변 SLA 정책과 샘플 티켓에 대해 적용 결과를 저장 전 미리보기할 때 사용하는 요청입니다. candidate는 candidatePolicyId가 가리키는 기존 정책을 평가
        중에만 대체합니다.
      x-deskseed-documentation-review: MANUAL
      example:
        candidatePolicyId: 66666666-6666-4666-8666-666666666666
        candidate: *id016
        ticket:
          priority: HIGH
          groupId: 11111111-1111-4111-8111-111111111111
          channel: WEB
        startAt: '2026-08-10T00:00:00Z'
    FirstReplySlaPreviewResult:
      type: object
      additionalProperties: false
      required:
      - matched
      - dueAt
      - targetMinutes
      - policyId
      - policyVersion
      - scheduleId
      - scheduleVersion
      - dstPolicy
      properties:
        matched:
          type: boolean
        dueAt:
          type:
          - string
          - 'null'
          format: date-time
        targetMinutes:
          type:
          - integer
          - 'null'
          minimum: 1
        policyId:
          type:
          - string
          - 'null'
          format: uuid
        policyVersion:
          type:
          - integer
          - 'null'
          minimum: 1
        scheduleId:
          type:
          - string
          - 'null'
          format: uuid
        scheduleVersion:
          type:
          - integer
          - 'null'
          minimum: 1
        dstPolicy:
          type: string
          enum:
          - GAP_SHIFT_FORWARD_OVERLAP_INCLUDE_BOTH
    FirstReplySlaAnalytics:
      type: object
      additionalProperties: false
      required:
      - metric
      - calculationVersion
      - active
      - paused
      - achieved
      - breached
      - cancelled
      - noPolicy
      - achievedRateDenominator
      - achievedRate
      properties:
        metric:
          type: string
          const: FIRST_REPLY
        calculationVersion:
          type: string
        active:
          type: integer
          format: int64
          minimum: 0
        paused:
          type: integer
          format: int64
          minimum: 0
        achieved:
          type: integer
          format: int64
          minimum: 0
        breached:
          type: integer
          format: int64
          minimum: 0
        cancelled:
          type: integer
          format: int64
          minimum: 0
        noPolicy:
          type: integer
          format: int64
          minimum: 0
        achievedRateDenominator:
          type: integer
          format: int64
          minimum: 0
        achievedRate:
          type:
          - number
          - 'null'
          format: double
          minimum: 0
          maximum: 1
x-deskseed-documentation-ownership: MANUAL
