openapi: 3.1.0
info:
  title: Deskseed Platform API v1
  version: 1.0.0-contract
  description: Implementation-ready private-network machine API for ticket create/read/update and INTERNAL comments. Network
    placement is defense in depth and never replaces scoped API-key authentication, resource constraints, rate limiting, idempotency,
    concurrency control, and audit.
  x-deskseed-status: implementation-ready
  x-deskseed-network-boundary: private-only
servers:
- url: https://deskseed.internal.example/api/v1/platform
security:
- bearerApiKey: []
tags:
- name: Platform Tickets
paths:
  /tickets:
    post:
      tags:
      - Platform Tickets
      operationId: platformCreateTicket
      summary: Create a customer request or an internal work item
      x-deskseed-required-scope: tickets:create
      parameters:
      - $ref: '#/components/parameters/IdempotencyKey'
      - $ref: '#/components/parameters/RequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTicketRequest'
      responses:
        '201':
          description: Ticket created
          headers:
            ETag:
              $ref: '#/components/headers/ETag'
            X-Request-Id:
              $ref: '#/components/headers/RequestId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlatformTicket'
        '400':
          $ref: '#/components/responses/Problem'
        '401':
          $ref: '#/components/responses/Problem'
        '403':
          $ref: '#/components/responses/Problem'
        '409':
          $ref: '#/components/responses/Problem'
        '429':
          $ref: '#/components/responses/RateLimited'
  /tickets/{ticketNumber}:
    parameters:
    - $ref: '#/components/parameters/TicketNumber'
    get:
      tags:
      - Platform Tickets
      operationId: platformGetTicket
      summary: Read the integration-safe ticket projection
      x-deskseed-required-scope: tickets:read
      parameters:
      - $ref: '#/components/parameters/RequestId'
      responses:
        '200':
          description: Ticket
          headers:
            ETag:
              $ref: '#/components/headers/ETag'
            X-Request-Id:
              $ref: '#/components/headers/RequestId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlatformTicket'
        '401':
          $ref: '#/components/responses/Problem'
        '403':
          $ref: '#/components/responses/Problem'
        '404':
          $ref: '#/components/responses/Problem'
        '429':
          $ref: '#/components/responses/RateLimited'
    patch:
      tags:
      - Platform Tickets
      operationId: platformUpdateTicket
      summary: Update the allowlisted ticket fields
      x-deskseed-required-scope: tickets:update
      parameters:
      - $ref: '#/components/parameters/IdempotencyKey'
      - $ref: '#/components/parameters/IfMatch'
      - $ref: '#/components/parameters/RequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTicketRequest'
      responses:
        '200':
          description: Updated ticket
          headers:
            ETag:
              $ref: '#/components/headers/ETag'
            X-Request-Id:
              $ref: '#/components/headers/RequestId'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlatformTicket'
        '400':
          $ref: '#/components/responses/Problem'
        '401':
          $ref: '#/components/responses/Problem'
        '403':
          $ref: '#/components/responses/Problem'
        '404':
          $ref: '#/components/responses/Problem'
        '409':
          $ref: '#/components/responses/Problem'
        '412':
          $ref: '#/components/responses/Problem'
        '429':
          $ref: '#/components/responses/RateLimited'
  /tickets/{ticketNumber}/internal-comments:
    parameters:
    - $ref: '#/components/parameters/TicketNumber'
    post:
      tags:
      - Platform Tickets
      operationId: platformAddInternalComment
      summary: Add an INTERNAL comment; PUBLIC visibility is not part of Platform API v1
      x-deskseed-required-scope: tickets:comment:internal
      parameters:
      - $ref: '#/components/parameters/IdempotencyKey'
      - $ref: '#/components/parameters/RequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddInternalCommentRequest'
      responses:
        '201':
          description: Internal comment created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalComment'
        '400':
          $ref: '#/components/responses/Problem'
        '401':
          $ref: '#/components/responses/Problem'
        '403':
          $ref: '#/components/responses/Problem'
        '404':
          $ref: '#/components/responses/Problem'
        '409':
          $ref: '#/components/responses/Problem'
        '429':
          $ref: '#/components/responses/RateLimited'
components:
  securitySchemes:
    bearerApiKey:
      type: http
      scheme: bearer
      bearerFormat: Deskseed scoped API key
  parameters:
    TicketNumber:
      name: ticketNumber
      in: path
      required: true
      schema:
        type: integer
        format: int64
        minimum: 1
    IdempotencyKey:
      name: Idempotency-Key
      in: header
      required: true
      schema:
        type: string
        minLength: 8
        maxLength: 200
    IfMatch:
      name: If-Match
      in: header
      required: true
      schema:
        type: string
        minLength: 3
        maxLength: 100
    RequestId:
      name: X-Request-Id
      in: header
      required: false
      schema:
        type: string
        maxLength: 100
  headers:
    ETag:
      schema:
        type: string
    RequestId:
      schema:
        type: string
    RetryAfter:
      schema:
        type: integer
        minimum: 1
  responses:
    Problem:
      description: RFC 9457 Problem Details
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    RateLimited:
      description: Client rate limit exceeded
      headers:
        Retry-After:
          $ref: '#/components/headers/RetryAfter'
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
  schemas:
    CreateTicketRequest:
      type: object
      additionalProperties: false
      required:
      - kind
      - subject
      - message
      properties:
        kind:
          type: string
          enum:
          - CUSTOMER_REQUEST
          - INTERNAL_WORK_ITEM
        subject:
          type: string
          minLength: 1
          maxLength: 300
        message:
          type: string
          minLength: 1
          maxLength: 50000
          description: Creates the first PUBLIC comment for CUSTOMER_REQUEST and the first INTERNAL comment for INTERNAL_WORK_ITEM.
        requester:
          $ref: '#/components/schemas/RequesterInput'
        priority:
          type: string
          enum:
          - LOW
          - NORMAL
          - HIGH
          - URGENT
          default: NORMAL
        groupId:
          type: string
          format: uuid
        assigneeId:
          type: string
          format: uuid
        externalOperationId:
          type: string
          maxLength: 200
      allOf:
      - if:
          properties:
            kind:
              const: CUSTOMER_REQUEST
        then:
          required:
          - requester
    RequesterInput:
      type: object
      additionalProperties: false
      required:
      - name
      - email
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
        email:
          type: string
          format: email
          maxLength: 320
    UpdateTicketRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        status:
          type: string
          enum:
          - NEW
          - OPEN
          - PENDING
          - HOLD
          - SOLVED
        priority:
          type: string
          enum:
          - LOW
          - NORMAL
          - HIGH
          - URGENT
        groupId:
          type:
          - string
          - 'null'
          format: uuid
        assigneeId:
          type:
          - string
          - 'null'
          format: uuid
        subject:
          type: string
          minLength: 1
          maxLength: 300
    AddInternalCommentRequest:
      type: object
      additionalProperties: false
      required:
      - body
      properties:
        body:
          type: string
          minLength: 1
          maxLength: 50000
    PlatformTicket:
      type: object
      additionalProperties: false
      required:
      - ticketNumber
      - subject
      - status
      - priority
      - version
      - createdAt
      - updatedAt
      properties:
        ticketNumber:
          type: integer
          format: int64
        subject:
          type: string
        status:
          type: string
        priority:
          type: string
        groupId:
          type:
          - string
          - 'null'
          format: uuid
        assigneeId:
          type:
          - string
          - 'null'
          format: uuid
        version:
          type: integer
          format: int64
          minimum: 0
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    InternalComment:
      type: object
      additionalProperties: false
      required:
      - id
      - ticketNumber
      - visibility
      - body
      - createdAt
      properties:
        id:
          type: string
          format: uuid
        ticketNumber:
          type: integer
          format: int64
        visibility:
          type: string
          const: INTERNAL
        body:
          type: string
        createdAt:
          type: string
          format: date-time
    Problem:
      type: object
      additionalProperties: true
      required:
      - type
      - title
      - status
      properties:
        type:
          type: string
          format: uri-reference
        title:
          type: string
        status:
          type: integer
          minimum: 400
          maximum: 599
        detail:
          type: string
        requestId:
          type: string
